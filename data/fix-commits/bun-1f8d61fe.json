{
  "vcs_url": "https://github.com/oven-sh/bun",
  "vulnerabilities": {
    "CVE-2023-44487": {
      "9390b3986fe58cb236d9e5e3ccc9248ce42c5958": "node:http2: rate-limit stream resets per connection (CVE-2023-44487 rapid reset, CVE-2025-8671 MadeYouReset) (#36230)\n\n### Problem\n- One connection that loops `HEADERS` + `RST_STREAM(CANCEL)` makes a\n`node:http2` server run the `'stream'` handler and serialize a response\nfor all 20,000 reset streams, at 60-80% CPU. It never sends `GOAWAY`.\nNode closes that connection after about 1,400 resets (CVE-2023-44487).\n- The h2 engine (`src/runtime/api/bun/h2/connection.rs`) does not limit\nstream resets. `streamResetBurst` and `streamResetRate` have no effect.\n\n### Fix\n- Server sessions get nghttp2's token bucket: 1000 tokens, 33 more per\nsecond, set by `streamResetBurst` / `streamResetRate`. An empty bucket\nsends `GOAWAY(ENHANCE_YOUR_CALM)`, and the session fails with\n`ERR_HTTP2_ERROR`.\n- A second, fixed bucket counts the resets that the engine sends itself\n(CVE-2025-8671, MadeYouReset).\n- A reset takes a token only when it cancels a live stream. After the\nserver's own `GOAWAY`, resets of older streams are free, as in nghttp2.\n- Verified: `test/js/node/http2/h2-conformance.test.ts` (14 new tests, 8\nfail on main), `node-http2.test.js`, node's 261 `test-http2-*.js`.\n\n### Background\n- Rapid reset: a client cancels each request at once, so no stream limit\napplies while the handlers run.\n- MadeYouReset: an invalid frame makes the server reset the stream\nitself.\n- nghttp2 (node) charges each inbound `RST_STREAM` to a token bucket and\nsends `GOAWAY` when it is empty.\n\n### Downsides\n- A connection that cancels more than 1000 live streams in a burst, then\n33 per second, now fails with `ERR_HTTP2_ERROR`, as in node. The two\noptions raise the limit.\n- About 1,000 cancelled requests per connection still run the handler\nand get a response serialized (1,133 measured). Node writes 0 responses,\nbecause it emits `'stream'` one tick later.\n- The `GOAWAY` code is `ENHANCE_YOUR_CALM` with debug text. Node sends\n`INTERNAL_ERROR` with none.\n\n<details><summary>What Node does, and where this PR differs</summary>\n\nSources: nghttp2 v1.69.0 `lib/nghttp2_session.c` (the version in node\nv26.3.0), node v26.3.0 `src/node_http2.cc`, and\n`lib/internal/http2/util.js` as extracted from the node 26.3 binary.\nMeasurements are against node v26.3.0.\n\n**The bucket.** `session_update_stream_reset_ratelim` returns early\nunless the session is a server that has not submitted a GOAWAY\n(`NGHTTP2_GOAWAY_SUBMITTED`). It then updates and drains\n`stream_reset_ratelim` (`NGHTTP2_DEFAULT_STREAM_RESET_BURST` 1000,\n`NGHTTP2_DEFAULT_STREAM_RESET_RATE` 33, whole-second timestamps). When\nthe bucket is empty it calls `nghttp2_session_add_goaway(...,\nNGHTTP2_INTERNAL_ERROR, NULL, 0, ...)`. Its only caller is the last line\nof `nghttp2_session_on_rst_stream_received`, after\n`nghttp2_session_close_stream`. So every inbound `RST_STREAM` on a\nstream that is not idle is charged, also when the stream is already\ngone.\n\n**The options.** `updateOptionsBuffer` in `util.js` (verbatim,\n`optionsBuffer` is a `Uint32Array`):\n\n```js\n  if (typeof options.streamResetRate === 'number') {\n    flags |= (1 << IDX_OPTIONS_STREAM_RESET_RATE);\n    optionsBuffer[IDX_OPTIONS_STREAM_RESET_RATE] =\n      MathMax(1, options.streamResetRate);\n  }\n  if (typeof options.streamResetBurst === 'number') {\n    flags |= (1 << IDX_OPTIONS_STREAM_RESET_BURST);\n    optionsBuffer[IDX_OPTIONS_STREAM_RESET_BURST] =\n      MathMax(1, options.streamResetBurst);\n  }\n```\n\nThere is no validation: `-1` becomes 1, `33.5` becomes 33, a value that\nis not a number is ignored. `node_http2.cc` calls\n`nghttp2_option_set_stream_reset_rate_limit` only when both flags are\nset.\n\n**After its GOAWAY.** `nghttp2_session_on_request_headers_received`\nreturns `NGHTTP2_ERR_IGN_HEADER_BLOCK` when\n`session_allow_incoming_new_stream()` is false\n(`NGHTTP2_GOAWAY_TERM_ON_SEND | NGHTTP2_GOAWAY_SENT`). The block is\ndecoded for HPACK state, no stream is opened, no callback runs.\nMeasured: 3,000 `HEADERS`+`RST_STREAM` pairs after node's GOAWAY give 0\nhandler runs and 0% CPU.\n\n**Resets nghttp2 sends itself.** nghttp2 answers a stream\n`WINDOW_UPDATE` with a 0 increment, a stream window overflow, and `DATA`\non a half-closed (remote) stream with a connection error, not with\n`RST_STREAM`. `session_on_data_received_fail_fast` returns\n`NGHTTP2_ERR_IGN_PAYLOAD` for `DATA` on a stream that no longer exists:\nmeasured, node answers 1,200 late `DATA` frames with 0 `RST_STREAM`.\nnghttp2 1.67.0 added a separate `glitch_ratelim`\n(`session_update_glitch_ratelim`, 1000 / 33 in 1.67 and 1.68, 10000 /\n330 in 1.69). It is charged for malformed header blocks, ignored header\nblocks, empty `DATA` without `END_STREAM`, `PRIORITY` and unknown\nframes. Node does not set it. Measured: with `maxSessionInvalidFrames:\n5000` node answers 1,500 malformed header blocks with\n`RST_STREAM(PROTOCOL_ERROR)` and then serves the next request.\n\n**A request reset in the same read.** Node still runs the `'stream'`\nhandler (its `test-http2-client-rststream-before-connect` depends on\nthat). It emits the event from `process.nextTick` (`onSessionHeaders` in\n`lib/internal/http2/core.js`), after nghttp2 consumed the whole read.\nThe stream is already reset by then, so nothing the handler writes\nreaches the wire: 10,000 pairs give about 1,700 handler runs and 0\nresponse frames.\n\n| | node | this PR |\n|---|---|---|\n| bucket size and refill | 1000 burst / 33 per whole second, server only\n| same (`RateLimit`, `Connection::stream_reset_limit`) |\n| `streamResetBurst` / `streamResetRate` | per-key number check,\n`MathMax(1)`, uint32, applied only when both are given | same\n(`to_u32().max(1)`, both or neither, `http2.ts` is not touched). Values\nabove 2^32 saturate, node wraps |\n| which inbound `RST_STREAM` is charged | every one, also for a stream\nthat is already gone | **differs**: only one that closes a stream that\nis still open. Bun's own http2 client answers each late `DATA` frame on\na cancelled stream with `RST_STREAM(STREAM_CLOSED)` (measured: 2,900 for\n2,900 late frames, node sends 1). With node's rule one cancelled\nresponse with many small frames in flight drains the bucket of a Bun\nserver. A frame for a closed stream cancels nothing |\n| after the server's GOAWAY | no reset is charged, and no new stream is\nopened | **differs**: resets of streams opened before the GOAWAY are not\ncharged. This engine still opens streams after its GOAWAY (not changed\nhere), so a reset of such a stream is charged. Each stream carries\n`opened_after_goaway`, because the engine also accepts a new id below\nthe GOAWAY's last stream id. Without this a client sends\n`GOAWAY(NO_ERROR)`, holds one stream open and floods free of charge:\n380,000 pairs in 5 s at 100% CPU on the previous head. The GOAWAY flag\nis asked per frame (`Sink::goaway_sent`, set by the parser's\n`send_go_away`), so a `close()` from inside a handler counts for the\nrest of that read |\n| GOAWAY on exhaustion | `INTERNAL_ERROR`, no debug data | **differs**:\n`ENHANCE_YOUR_CALM` + `\"too many stream resets\"`, the code and shape of\nthis engine's other flood guard (`note_outbound_ack`). JS sees\n`ERR_HTTP2_ERROR` in both |\n| resets the server sends itself | connection errors for the\nMadeYouReset shapes, and the glitch bucket | **differs**: this engine\nanswers those shapes with the RFC 9113 stream error, so a client can\nprovoke resets. A second fixed bucket (`sent_reset_limit`, 1000 / 33) is\ncharged in `send_rst_stream` when the reset cancels a request that the\nembedder already has (`recv_final_headers`, state not `Closed`). Not\ncharged: late `DATA` for a stream the server closed (a rejected upload),\nframes queued behind a reset, and header blocks refused before dispatch,\nwhich `maxSessionInvalidFrames` and `maxSessionRejectedStreams` bound as\non main |\n| request reset in the same read | handler runs one tick later on a\nreset stream, 0 responses | **differs**: Bun emits `'stream'` from the\nHEADERS dispatch, before the `RST_STREAM` is parsed, so the response is\nserialized. This PR keeps that order and the bucket bounds it: 10,000\npairs give 1,133 handler runs and responses, then the GOAWAY (main:\n10,000 of both) |\n\nA dry bucket sets a flag. `receive()` tears the session down between\nframes, so the frame that emptied the bucket finishes its own dispatch\nfirst.\n\n</details>\n\n<details><summary>Notes</summary>\n\n**Tests.** `stream-reset floods` block in\n`test/js/node/http2/h2-conformance.test.ts`, 14 tests. On bun 1.4.2\neight fail: the RST_STREAM flood, `streamResetBurst`, the refill, the\nthree server-sent-reset shapes, resets of streams opened after the\nGOAWAY, and server-sent resets after the GOAWAY. The other six pass on\nmain by design. Each is pinned by a mutation that I built and ran. Each\nrun failed exactly the test named (the refill mutations are from the\nprevious round, that code did not change):\n\n* `recv_final_headers` clause removed from `send_rst_stream`: \"a header\nblock refused before dispatch is not charged\" fails\n* `state != Closed` clause removed from `send_rst_stream`: \"a stream\nerror is charged once\" fails\n* the previous head, which charged every server-sent reset: \"late DATA\nfor a stream the server already closed is not charged\" fails\n* `state != Closed` clause removed from `handle_rst_stream`: \"RST_STREAM\nframes for a stream that is already closed are not charged\" fails\n* GOAWAY exemption removed: \"resets are not charged once the server has\nsent its own GOAWAY\" fails\n* `opened_after_goaway` clause removed, or replaced by a comparison with\nthe GOAWAY's last stream id: \"resets of streams opened after the\nserver's GOAWAY are charged\" fails (its flood uses ids below the held\nstream)\n* GOAWAY exemption applied to server-sent resets: \"server-sent resets\nstay charged after the server has sent its GOAWAY\" fails\n* window-overflow site closes the stream before `send_rst_stream`: the\n\"WINDOW_UPDATE past 2^31-1\" flood fails\n* refill block removed, or `last_refill +=` removed: \"the bucket\nrefills\" fails. That test uses `streamResetBurst: 0`, so it also fails\nwithout the floor to 1. It waits 1.5 s of wall clock on purpose, because\nthe refill is per whole second.\n\nAlso passing: the rest of `h2-conformance.test.ts` (84 in total),\n`node-http2.test.js` (390), seven other test files in\n`test/js/node/http2/`, and node's 261 `test-http2-*.js`.\n\n**Measurements** (release build of an earlier head, the flood path is\nthe same): the flooding connection is closed after 28 ms and a fresh\nrequest is served 1-2 ms later, for floods of 1, 3 and 10 s. bun 1.4.2\nnever closes it and is unreachable for 20-31 s. Node closes after 88 ms.\n\n**Seen on main, not changed here.**\n* The server opens streams after its own GOAWAY. `streamStart` allocates\na `ServerHttp2Stream` and counts it, `streamHeaders` drops it because\nthe session is closed, and nothing answers or resets it. A client that\nopens one stream after the GOAWAY keeps `session.close()` from ever\nfinishing. Node ignores such streams and closes.\n* The http2 client sends one `RST_STREAM(STREAM_CLOSED)` per late `DATA`\nframe after it cancels a request. nghttp2 servers charge each of them.\n* The engine accepts a new client stream id that is lower than an\nearlier one (RFC 9113 5.1.1 asks for a connection error).\n\nThe branch is 55a6599 (the change, on main 87466cf), dc32b9d (makes\n`ResetBy` `Copy` for clippy's `needless_pass_by_value`), and merges of\nmain up to a0a71e2.\n\n</details>\n\n<!-- robobun:evidence:begin -->\n\n---\n\n**[human-review]** gate passed \u00b7 iteration 11 \u00b7 3 files touched\n\n<details><summary>fails on main (without fix)</summary>\n\n```console\nASAN without fix: 9 FAILED\n$ BUN_DEBUG_QUIET_LOGS=1 bun scripts/build.ts --profile=debug --quiet test \"--reporter=junit\" \"--reporter-outfile=/tmp/pr_gate.xml\" \"test/js/node/http2/h2-conformance.test.ts\"\nbun test v1.4.3 (367d939d9)\n\ntest/js/node/http2/h2-conformance.test.ts:\n(pass) connection preface & SETTINGS handshake (checklist \u00a71) > server sends a SETTINGS frame first (\u00a71.4) [387.56ms]\n(pass) connection preface & SETTINGS handshake (checklist \u00a71) > server ACKs the client's SETTINGS frame (\u00a73.5) [118.99ms]\n(pass) connection preface & SETTINGS handshake (checklist \u00a71) > a SETTINGS frame with a non-zero stream id is a PROTOCOL_ERROR (\u00a73.5) [84.79ms]\n(pass) connection preface & SETTINGS handshake (checklist \u00a71) > a SETTINGS frame whose length is not a multiple of 6 is a FRAME_SIZE_ERROR (\u00a73.5) [43.35ms]\n(pass) connection preface & SETTINGS handshake (checklist \u00a71) > a SETTINGS ACK that carries a payload is a FRAME_SIZE_ERROR (\u00a73.5) [83.46ms]\n(pass) PING (checklist \u00a73.7) > server replies to PING with a PING ACK echoing the payload [38.88ms]\n(pass) PING (checklist \u00a73.7) > a PING with length != 8 is a FRAME_SIZE_ERROR [38.89ms]\n(pass) PING (checklist \u00a73.7) > a PING on a non-zero str\n... (truncated)\n\nrelease without fix: 8 FAILED\nbun test v1.4.3-canary.1 (367d939d9)\n\ntest/js/node/http2/h2-conformance.test.ts:\n(pass) connection preface & SETTINGS handshake (checklist \u00a71) > server sends a SETTINGS frame first (\u00a71.4) [6.61ms]\n(pass) connection preface & SETTINGS handshake (checklist \u00a71) > server ACKs the client's SETTINGS frame (\u00a73.5) [1.92ms]\n(pass) connection preface & SETTINGS handshake (checklist \u00a71) > a SETTINGS frame with a non-zero stream id is a PROTOCOL_ERROR (\u00a73.5) [1.59ms]\n(pass) connection preface & SETTINGS handshake (checklist \u00a71) > a SETTINGS frame whose length is not a multiple of 6 is a FRAME_SIZE_ERROR (\u00a73.5) [0.86ms]\n(pass) connection preface & SETTINGS handshake (checklist \u00a71) > a SETTINGS ACK that carries a payload is a FRAME_SIZE_ERROR (\u00a73.5) [0.73ms]\n(pass) PING (checklist \u00a73.7) > server replies to PING with a PING ACK echoing the payload [0.74ms]\n(pass) PING (checklist \u00a73.7) > a PING with length != 8 is a FRAME_SIZE_ERROR [0.75ms]\n(pass) PING (checklist \u00a73.7) > a PING on a non-zero stream id is a PROTOCOL_ERROR [0.60ms]\n(pass) WINDOW_UPDATE (checklist \u00a76) > a connection-level WINDOW_UPDATE with a 0 increment is a PROTOCOL_ERROR [0.69ms]\n(pass) WINDOW_UPDATE\n... (truncated)\n```\n\n</details>\n\n<details><summary>passes on PR (with fix)</summary>\n\n```console\nASAN with fix: all passed\n$ BUN_DEBUG_QUIET_LOGS=1 bun scripts/build.ts --profile=debug --quiet test \"--reporter=junit\" \"--reporter-outfile=/tmp/pr_gate.xml\" \"test/js/node/http2/h2-conformance.test.ts\"\nbun test v1.4.3 (367d939d9)\n\ntest/js/node/http2/h2-conformance.test.ts:\n(pass) connection preface & SETTINGS handshake (checklist \u00a71) > server sends a SETTINGS frame first (\u00a71.4) [845.40ms]\n(pass) connection preface & SETTINGS handshake (checklist \u00a71) > server ACKs the client's SETTINGS frame (\u00a73.5) [263.62ms]\n(pass) connection preface & SETTINGS handshake (checklist \u00a71) > a SETTINGS frame with a non-zero stream id is a PROTOCOL_ERROR (\u00a73.5) [214.04ms]\n(pass) connection preface & SETTINGS handshake (checklist \u00a71) > a SETTINGS frame whose length is not a multiple of 6 is a FRAME_SIZE_ERROR (\u00a73.5) [100.75ms]\n(pass) connection preface & SETTINGS handshake (checklist \u00a71) > a SETTINGS ACK that carries a payload is a FRAME_SIZE_ERROR (\u00a73.5) [132.76ms]\n(pass) PING (checklist \u00a73.7) > server replies to PING with a PING ACK echoing the payload [164.67ms]\n(pass) PING (checklist \u00a73.7) > a PING with length != 8 is a FRAME_SIZE_ERROR [114.15ms]\n(pass) PING (checklist \u00a73.7) > a PING on a non-zer\n... (truncated)\n\nrelease with fix: all passed\n$ bun scripts/build.ts --profile=release\n[configured] bun-profile \u2192 bun (stripped)\n  target       linux-x64-gnu\n  build type   Release\n  build dir    ./build/release\n  revision     a402383276\n  features     lto, baseline\n\n23 deps, 136 codegen, 1176 objects in 960ms\n\nninja: Entering directory `/workspace/bun/build/release'\n[1/4] fetch lolhtml\n[lolhtml] up to date\n[2/4] fetch rust-argon2\n[rust-argon2] up to date\n[2/4] cargo plan \u2192 /workspace/bun/build/release/rust-target/plan.json\n244 units: 172 lib, 16 proc-macro (host), 19 custom-build (host), 15 run custom-build, 17 lib (host), 4 run custom-build (host), 1 rlib\n[3/4] reconfigure\n[1/1499] mkdir stamps\n[2/1499] mkdir codegen\n[3/1499] install /workspace/bun\nbun install v1.4.3-canary.1 (367d939d9)\n\nChecked 26 installs across 65 packages (no changes) [34.00ms]\n[4/1499] install /workspace/bun/packages/bun-error\nbun install v1.4.3-canary.1 (367d939d9)\n\nChecked 1 install across 2 packages (no changes) [2.00ms]\n[5/1499] gen bake.{client,server,error}.js\n-> bake.client.js, bake.server.js, bake.error.js\n[6/1499] install /workspace/bun/src/node-fallbacks\nbun install v1.4.3-canary.1 (367d939d9)\n\nChecked 111 installs across \n... (truncated)\n```\n\n</details>\n\n<details><summary>diff hotspot</summary>\n\n```\nsrc/runtime/api/bun/h2/connection.rs      | 138 ++++++++++++++++-\n src/runtime/api/bun/h2_frame_parser.rs    |  24 +++\n test/js/node/http2/h2-conformance.test.ts | 240 ++++++++++++++++++++++++++++++\n 3 files changed, 398 insertions(+), 4 deletions(-)\n```\n\n</details>\n\n**gate history** \u00b7 15 passed \u00b7 0 rejected \u00b7 iteration 11\n\n<details><summary>evidence per changed file</summary>\n\n```\nfile                                       reads  edits  tests\nsrc/runtime/api/bun/h2/connection.rs          49     78    122\nsrc/runtime/api/bun/h2_frame_parser.rs        48     41    122\ntest/js/node/http2/h2-conformance.test.ts     19     26    121\n```\n\n</details>\n\n<!-- robobun:evidence:end -->\n\n---------\n\nCo-authored-by: Ciro Spaciari <ciro.spaciari@gmail.com>",
      "13805dd3b0bd92aea64ce18777b3c3b5c200497e": "h2: Rapid Reset cap, MAX_HEADER_LIST on initial HEADERS, \u00a78.2.1 comment accuracy\n\n- handleRstStream: bound cumulative client resets per connection at 1000\n  (CVE-2023-44487); a HEADERS->RST flood keeps the concurrent-stream count\n  near zero so MAX_STREAMS never trips. GOAWAY(ENHANCE_YOUR_CALM) past the\n  cap, matching nginx/Go/nghttp2. Adds a regression that packs 1200\n  HEADERS+RST pairs and asserts the GOAWAY code.\n- handleHeaders: enforce MAX_HEADER_LIST on the initial HEADERS fragment\n  before headerBlock.assign(), mirroring the check handleContinuation\n  already runs on the same accumulator so a single large-frame block and\n  a split block are bounded the same way.\n- decodeHeaderBlock comment: describe what the name loop actually checks\n  (the \u00a78.2.1 MUST-level byte ranges) and note the tchar delimiter set as\n  a deferred SHOULD alongside the existing \u00a78.2.2/\u00a78.3.1 gaps.",
      "4fd91fb70f81c6d9705c931acf9e651151c6bca4": "http2: rate-limit rapid reset with a token bucket, make the concurrency check O(1)\n\nThe rapid-reset guard was a net-outstanding gauge: +1 per inbound peer\nRST_STREAM, -1 per odd-id close_stream. The canonical CVE-2023-44487 flood\n(HEADERS then RST_STREAM, holding nothing open) nets to zero across read\nbatches and never trips, and the refund fired for every odd-id close, so one\ncompleted request per batch kept the counter pinned near zero.\n\nReplace it with nghttp2's stream_reset_rate_limit model: a token bucket of\nstreamResetBurst tokens regenerated at streamResetRate per second on a\nmonotonic clock, drained once per inbound RST_STREAM on a known stream.\nRunning dry answers GOAWAY(ENHANCE_YOUR_CALM). close_stream no longer\ntouches the budget. streamResetRate is read from the session options next to\nstreamResetBurst; both default to nghttp2's values (1000 and 33).\n\nThe SETTINGS_MAX_CONCURRENT_STREAMS check no longer rescans the stream map on\nevery HEADERS: peer_active_count is maintained incrementally by routing every\nstream state write, insert, and removal through set_stream_state,\ninsert_stream, and remove_stream. The check now applies to both roles (a push\nresponse promoting a reserved-remote stream counts against the client's\nadvertised limit), and inbound PUSH_PROMISE reservations, which section 5.1.2\nexempts from that limit, are capped at nghttp2's\nmax_incoming_reserved_streams (200) and answered with\nRST_STREAM(REFUSED_STREAM) past it.\n\nThe peer_resets_are_refunded_by_close_stream unit test asserted the broken\nrefund (32 HEADERS/RST batches, no GOAWAY); it is replaced by tests that pace\nHEADERS then RST across read batches with a completed legitimate request\nbetween each and assert the GOAWAY, plus tests for the timed refill, the\nincremental gauge, the reserved-stream cap, and the client-side limit. The\npaced scenario is also covered at the wire level in h2-conformance.test.ts,\nwhere it fails against the previous design.",
      "52fdb1f68a306099f654b250ebf60a9163691d85": "http2: enforce max_concurrent_streams and bound rapid-reset in the engine\n\nThe h2 connection engine inserted a Stream entry and dispatched\non_stream_open for every inbound HEADERS without checking the\nadvertised SETTINGS_MAX_CONCURRENT_STREAMS (RFC 9113 5.1.2), and\ninbound RST_STREAM was uncounted, so a HEADERS-then-RST flood\n(CVE-2023-44487 shape) churned native and JS per-stream state\nwithout bound.\n\nhandle_headers now refuses an over-cap new stream with\nRST_STREAM(REFUSED_STREAM) before inserting any map entry or\nfiring on_stream_open; the header block is still HPACK-decoded\nfor table sync (4.3), last_stream_id advances so a follow-up\nRST on the refused id is tolerated, and the refusal routes\nthrough on_stream_rejected so maxSessionRejectedStreams budgets\nthe flood. handle_rst_stream counts server-side peer resets\nagainst the invalid-frame budget (maxSessionInvalidFrames,\ndefault 1000) so a rapid-reset flood that stays under the\nconcurrency cap still tears the session down."
    },
    "CVE-2025-8671": {
      "9390b3986fe58cb236d9e5e3ccc9248ce42c5958": "node:http2: rate-limit stream resets per connection (CVE-2023-44487 rapid reset, CVE-2025-8671 MadeYouReset) (#36230)\n\n### Problem\n- One connection that loops `HEADERS` + `RST_STREAM(CANCEL)` makes a\n`node:http2` server run the `'stream'` handler and serialize a response\nfor all 20,000 reset streams, at 60-80% CPU. It never sends `GOAWAY`.\nNode closes that connection after about 1,400 resets (CVE-2023-44487).\n- The h2 engine (`src/runtime/api/bun/h2/connection.rs`) does not limit\nstream resets. `streamResetBurst` and `streamResetRate` have no effect.\n\n### Fix\n- Server sessions get nghttp2's token bucket: 1000 tokens, 33 more per\nsecond, set by `streamResetBurst` / `streamResetRate`. An empty bucket\nsends `GOAWAY(ENHANCE_YOUR_CALM)`, and the session fails with\n`ERR_HTTP2_ERROR`.\n- A second, fixed bucket counts the resets that the engine sends itself\n(CVE-2025-8671, MadeYouReset).\n- A reset takes a token only when it cancels a live stream. After the\nserver's own `GOAWAY`, resets of older streams are free, as in nghttp2.\n- Verified: `test/js/node/http2/h2-conformance.test.ts` (14 new tests, 8\nfail on main), `node-http2.test.js`, node's 261 `test-http2-*.js`.\n\n### Background\n- Rapid reset: a client cancels each request at once, so no stream limit\napplies while the handlers run.\n- MadeYouReset: an invalid frame makes the server reset the stream\nitself.\n- nghttp2 (node) charges each inbound `RST_STREAM` to a token bucket and\nsends `GOAWAY` when it is empty.\n\n### Downsides\n- A connection that cancels more than 1000 live streams in a burst, then\n33 per second, now fails with `ERR_HTTP2_ERROR`, as in node. The two\noptions raise the limit.\n- About 1,000 cancelled requests per connection still run the handler\nand get a response serialized (1,133 measured). Node writes 0 responses,\nbecause it emits `'stream'` one tick later.\n- The `GOAWAY` code is `ENHANCE_YOUR_CALM` with debug text. Node sends\n`INTERNAL_ERROR` with none.\n\n<details><summary>What Node does, and where this PR differs</summary>\n\nSources: nghttp2 v1.69.0 `lib/nghttp2_session.c` (the version in node\nv26.3.0), node v26.3.0 `src/node_http2.cc`, and\n`lib/internal/http2/util.js` as extracted from the node 26.3 binary.\nMeasurements are against node v26.3.0.\n\n**The bucket.** `session_update_stream_reset_ratelim` returns early\nunless the session is a server that has not submitted a GOAWAY\n(`NGHTTP2_GOAWAY_SUBMITTED`). It then updates and drains\n`stream_reset_ratelim` (`NGHTTP2_DEFAULT_STREAM_RESET_BURST` 1000,\n`NGHTTP2_DEFAULT_STREAM_RESET_RATE` 33, whole-second timestamps). When\nthe bucket is empty it calls `nghttp2_session_add_goaway(...,\nNGHTTP2_INTERNAL_ERROR, NULL, 0, ...)`. Its only caller is the last line\nof `nghttp2_session_on_rst_stream_received`, after\n`nghttp2_session_close_stream`. So every inbound `RST_STREAM` on a\nstream that is not idle is charged, also when the stream is already\ngone.\n\n**The options.** `updateOptionsBuffer` in `util.js` (verbatim,\n`optionsBuffer` is a `Uint32Array`):\n\n```js\n  if (typeof options.streamResetRate === 'number') {\n    flags |= (1 << IDX_OPTIONS_STREAM_RESET_RATE);\n    optionsBuffer[IDX_OPTIONS_STREAM_RESET_RATE] =\n      MathMax(1, options.streamResetRate);\n  }\n  if (typeof options.streamResetBurst === 'number') {\n    flags |= (1 << IDX_OPTIONS_STREAM_RESET_BURST);\n    optionsBuffer[IDX_OPTIONS_STREAM_RESET_BURST] =\n      MathMax(1, options.streamResetBurst);\n  }\n```\n\nThere is no validation: `-1` becomes 1, `33.5` becomes 33, a value that\nis not a number is ignored. `node_http2.cc` calls\n`nghttp2_option_set_stream_reset_rate_limit` only when both flags are\nset.\n\n**After its GOAWAY.** `nghttp2_session_on_request_headers_received`\nreturns `NGHTTP2_ERR_IGN_HEADER_BLOCK` when\n`session_allow_incoming_new_stream()` is false\n(`NGHTTP2_GOAWAY_TERM_ON_SEND | NGHTTP2_GOAWAY_SENT`). The block is\ndecoded for HPACK state, no stream is opened, no callback runs.\nMeasured: 3,000 `HEADERS`+`RST_STREAM` pairs after node's GOAWAY give 0\nhandler runs and 0% CPU.\n\n**Resets nghttp2 sends itself.** nghttp2 answers a stream\n`WINDOW_UPDATE` with a 0 increment, a stream window overflow, and `DATA`\non a half-closed (remote) stream with a connection error, not with\n`RST_STREAM`. `session_on_data_received_fail_fast` returns\n`NGHTTP2_ERR_IGN_PAYLOAD` for `DATA` on a stream that no longer exists:\nmeasured, node answers 1,200 late `DATA` frames with 0 `RST_STREAM`.\nnghttp2 1.67.0 added a separate `glitch_ratelim`\n(`session_update_glitch_ratelim`, 1000 / 33 in 1.67 and 1.68, 10000 /\n330 in 1.69). It is charged for malformed header blocks, ignored header\nblocks, empty `DATA` without `END_STREAM`, `PRIORITY` and unknown\nframes. Node does not set it. Measured: with `maxSessionInvalidFrames:\n5000` node answers 1,500 malformed header blocks with\n`RST_STREAM(PROTOCOL_ERROR)` and then serves the next request.\n\n**A request reset in the same read.** Node still runs the `'stream'`\nhandler (its `test-http2-client-rststream-before-connect` depends on\nthat). It emits the event from `process.nextTick` (`onSessionHeaders` in\n`lib/internal/http2/core.js`), after nghttp2 consumed the whole read.\nThe stream is already reset by then, so nothing the handler writes\nreaches the wire: 10,000 pairs give about 1,700 handler runs and 0\nresponse frames.\n\n| | node | this PR |\n|---|---|---|\n| bucket size and refill | 1000 burst / 33 per whole second, server only\n| same (`RateLimit`, `Connection::stream_reset_limit`) |\n| `streamResetBurst` / `streamResetRate` | per-key number check,\n`MathMax(1)`, uint32, applied only when both are given | same\n(`to_u32().max(1)`, both or neither, `http2.ts` is not touched). Values\nabove 2^32 saturate, node wraps |\n| which inbound `RST_STREAM` is charged | every one, also for a stream\nthat is already gone | **differs**: only one that closes a stream that\nis still open. Bun's own http2 client answers each late `DATA` frame on\na cancelled stream with `RST_STREAM(STREAM_CLOSED)` (measured: 2,900 for\n2,900 late frames, node sends 1). With node's rule one cancelled\nresponse with many small frames in flight drains the bucket of a Bun\nserver. A frame for a closed stream cancels nothing |\n| after the server's GOAWAY | no reset is charged, and no new stream is\nopened | **differs**: resets of streams opened before the GOAWAY are not\ncharged. This engine still opens streams after its GOAWAY (not changed\nhere), so a reset of such a stream is charged. Each stream carries\n`opened_after_goaway`, because the engine also accepts a new id below\nthe GOAWAY's last stream id. Without this a client sends\n`GOAWAY(NO_ERROR)`, holds one stream open and floods free of charge:\n380,000 pairs in 5 s at 100% CPU on the previous head. The GOAWAY flag\nis asked per frame (`Sink::goaway_sent`, set by the parser's\n`send_go_away`), so a `close()` from inside a handler counts for the\nrest of that read |\n| GOAWAY on exhaustion | `INTERNAL_ERROR`, no debug data | **differs**:\n`ENHANCE_YOUR_CALM` + `\"too many stream resets\"`, the code and shape of\nthis engine's other flood guard (`note_outbound_ack`). JS sees\n`ERR_HTTP2_ERROR` in both |\n| resets the server sends itself | connection errors for the\nMadeYouReset shapes, and the glitch bucket | **differs**: this engine\nanswers those shapes with the RFC 9113 stream error, so a client can\nprovoke resets. A second fixed bucket (`sent_reset_limit`, 1000 / 33) is\ncharged in `send_rst_stream` when the reset cancels a request that the\nembedder already has (`recv_final_headers`, state not `Closed`). Not\ncharged: late `DATA` for a stream the server closed (a rejected upload),\nframes queued behind a reset, and header blocks refused before dispatch,\nwhich `maxSessionInvalidFrames` and `maxSessionRejectedStreams` bound as\non main |\n| request reset in the same read | handler runs one tick later on a\nreset stream, 0 responses | **differs**: Bun emits `'stream'` from the\nHEADERS dispatch, before the `RST_STREAM` is parsed, so the response is\nserialized. This PR keeps that order and the bucket bounds it: 10,000\npairs give 1,133 handler runs and responses, then the GOAWAY (main:\n10,000 of both) |\n\nA dry bucket sets a flag. `receive()` tears the session down between\nframes, so the frame that emptied the bucket finishes its own dispatch\nfirst.\n\n</details>\n\n<details><summary>Notes</summary>\n\n**Tests.** `stream-reset floods` block in\n`test/js/node/http2/h2-conformance.test.ts`, 14 tests. On bun 1.4.2\neight fail: the RST_STREAM flood, `streamResetBurst`, the refill, the\nthree server-sent-reset shapes, resets of streams opened after the\nGOAWAY, and server-sent resets after the GOAWAY. The other six pass on\nmain by design. Each is pinned by a mutation that I built and ran. Each\nrun failed exactly the test named (the refill mutations are from the\nprevious round, that code did not change):\n\n* `recv_final_headers` clause removed from `send_rst_stream`: \"a header\nblock refused before dispatch is not charged\" fails\n* `state != Closed` clause removed from `send_rst_stream`: \"a stream\nerror is charged once\" fails\n* the previous head, which charged every server-sent reset: \"late DATA\nfor a stream the server already closed is not charged\" fails\n* `state != Closed` clause removed from `handle_rst_stream`: \"RST_STREAM\nframes for a stream that is already closed are not charged\" fails\n* GOAWAY exemption removed: \"resets are not charged once the server has\nsent its own GOAWAY\" fails\n* `opened_after_goaway` clause removed, or replaced by a comparison with\nthe GOAWAY's last stream id: \"resets of streams opened after the\nserver's GOAWAY are charged\" fails (its flood uses ids below the held\nstream)\n* GOAWAY exemption applied to server-sent resets: \"server-sent resets\nstay charged after the server has sent its GOAWAY\" fails\n* window-overflow site closes the stream before `send_rst_stream`: the\n\"WINDOW_UPDATE past 2^31-1\" flood fails\n* refill block removed, or `last_refill +=` removed: \"the bucket\nrefills\" fails. That test uses `streamResetBurst: 0`, so it also fails\nwithout the floor to 1. It waits 1.5 s of wall clock on purpose, because\nthe refill is per whole second.\n\nAlso passing: the rest of `h2-conformance.test.ts` (84 in total),\n`node-http2.test.js` (390), seven other test files in\n`test/js/node/http2/`, and node's 261 `test-http2-*.js`.\n\n**Measurements** (release build of an earlier head, the flood path is\nthe same): the flooding connection is closed after 28 ms and a fresh\nrequest is served 1-2 ms later, for floods of 1, 3 and 10 s. bun 1.4.2\nnever closes it and is unreachable for 20-31 s. Node closes after 88 ms.\n\n**Seen on main, not changed here.**\n* The server opens streams after its own GOAWAY. `streamStart` allocates\na `ServerHttp2Stream` and counts it, `streamHeaders` drops it because\nthe session is closed, and nothing answers or resets it. A client that\nopens one stream after the GOAWAY keeps `session.close()` from ever\nfinishing. Node ignores such streams and closes.\n* The http2 client sends one `RST_STREAM(STREAM_CLOSED)` per late `DATA`\nframe after it cancels a request. nghttp2 servers charge each of them.\n* The engine accepts a new client stream id that is lower than an\nearlier one (RFC 9113 5.1.1 asks for a connection error).\n\nThe branch is 55a6599 (the change, on main 87466cf), dc32b9d (makes\n`ResetBy` `Copy` for clippy's `needless_pass_by_value`), and merges of\nmain up to a0a71e2.\n\n</details>\n\n<!-- robobun:evidence:begin -->\n\n---\n\n**[human-review]** gate passed \u00b7 iteration 11 \u00b7 3 files touched\n\n<details><summary>fails on main (without fix)</summary>\n\n```console\nASAN without fix: 9 FAILED\n$ BUN_DEBUG_QUIET_LOGS=1 bun scripts/build.ts --profile=debug --quiet test \"--reporter=junit\" \"--reporter-outfile=/tmp/pr_gate.xml\" \"test/js/node/http2/h2-conformance.test.ts\"\nbun test v1.4.3 (367d939d9)\n\ntest/js/node/http2/h2-conformance.test.ts:\n(pass) connection preface & SETTINGS handshake (checklist \u00a71) > server sends a SETTINGS frame first (\u00a71.4) [387.56ms]\n(pass) connection preface & SETTINGS handshake (checklist \u00a71) > server ACKs the client's SETTINGS frame (\u00a73.5) [118.99ms]\n(pass) connection preface & SETTINGS handshake (checklist \u00a71) > a SETTINGS frame with a non-zero stream id is a PROTOCOL_ERROR (\u00a73.5) [84.79ms]\n(pass) connection preface & SETTINGS handshake (checklist \u00a71) > a SETTINGS frame whose length is not a multiple of 6 is a FRAME_SIZE_ERROR (\u00a73.5) [43.35ms]\n(pass) connection preface & SETTINGS handshake (checklist \u00a71) > a SETTINGS ACK that carries a payload is a FRAME_SIZE_ERROR (\u00a73.5) [83.46ms]\n(pass) PING (checklist \u00a73.7) > server replies to PING with a PING ACK echoing the payload [38.88ms]\n(pass) PING (checklist \u00a73.7) > a PING with length != 8 is a FRAME_SIZE_ERROR [38.89ms]\n(pass) PING (checklist \u00a73.7) > a PING on a non-zero str\n... (truncated)\n\nrelease without fix: 8 FAILED\nbun test v1.4.3-canary.1 (367d939d9)\n\ntest/js/node/http2/h2-conformance.test.ts:\n(pass) connection preface & SETTINGS handshake (checklist \u00a71) > server sends a SETTINGS frame first (\u00a71.4) [6.61ms]\n(pass) connection preface & SETTINGS handshake (checklist \u00a71) > server ACKs the client's SETTINGS frame (\u00a73.5) [1.92ms]\n(pass) connection preface & SETTINGS handshake (checklist \u00a71) > a SETTINGS frame with a non-zero stream id is a PROTOCOL_ERROR (\u00a73.5) [1.59ms]\n(pass) connection preface & SETTINGS handshake (checklist \u00a71) > a SETTINGS frame whose length is not a multiple of 6 is a FRAME_SIZE_ERROR (\u00a73.5) [0.86ms]\n(pass) connection preface & SETTINGS handshake (checklist \u00a71) > a SETTINGS ACK that carries a payload is a FRAME_SIZE_ERROR (\u00a73.5) [0.73ms]\n(pass) PING (checklist \u00a73.7) > server replies to PING with a PING ACK echoing the payload [0.74ms]\n(pass) PING (checklist \u00a73.7) > a PING with length != 8 is a FRAME_SIZE_ERROR [0.75ms]\n(pass) PING (checklist \u00a73.7) > a PING on a non-zero stream id is a PROTOCOL_ERROR [0.60ms]\n(pass) WINDOW_UPDATE (checklist \u00a76) > a connection-level WINDOW_UPDATE with a 0 increment is a PROTOCOL_ERROR [0.69ms]\n(pass) WINDOW_UPDATE\n... (truncated)\n```\n\n</details>\n\n<details><summary>passes on PR (with fix)</summary>\n\n```console\nASAN with fix: all passed\n$ BUN_DEBUG_QUIET_LOGS=1 bun scripts/build.ts --profile=debug --quiet test \"--reporter=junit\" \"--reporter-outfile=/tmp/pr_gate.xml\" \"test/js/node/http2/h2-conformance.test.ts\"\nbun test v1.4.3 (367d939d9)\n\ntest/js/node/http2/h2-conformance.test.ts:\n(pass) connection preface & SETTINGS handshake (checklist \u00a71) > server sends a SETTINGS frame first (\u00a71.4) [845.40ms]\n(pass) connection preface & SETTINGS handshake (checklist \u00a71) > server ACKs the client's SETTINGS frame (\u00a73.5) [263.62ms]\n(pass) connection preface & SETTINGS handshake (checklist \u00a71) > a SETTINGS frame with a non-zero stream id is a PROTOCOL_ERROR (\u00a73.5) [214.04ms]\n(pass) connection preface & SETTINGS handshake (checklist \u00a71) > a SETTINGS frame whose length is not a multiple of 6 is a FRAME_SIZE_ERROR (\u00a73.5) [100.75ms]\n(pass) connection preface & SETTINGS handshake (checklist \u00a71) > a SETTINGS ACK that carries a payload is a FRAME_SIZE_ERROR (\u00a73.5) [132.76ms]\n(pass) PING (checklist \u00a73.7) > server replies to PING with a PING ACK echoing the payload [164.67ms]\n(pass) PING (checklist \u00a73.7) > a PING with length != 8 is a FRAME_SIZE_ERROR [114.15ms]\n(pass) PING (checklist \u00a73.7) > a PING on a non-zer\n... (truncated)\n\nrelease with fix: all passed\n$ bun scripts/build.ts --profile=release\n[configured] bun-profile \u2192 bun (stripped)\n  target       linux-x64-gnu\n  build type   Release\n  build dir    ./build/release\n  revision     a402383276\n  features     lto, baseline\n\n23 deps, 136 codegen, 1176 objects in 960ms\n\nninja: Entering directory `/workspace/bun/build/release'\n[1/4] fetch lolhtml\n[lolhtml] up to date\n[2/4] fetch rust-argon2\n[rust-argon2] up to date\n[2/4] cargo plan \u2192 /workspace/bun/build/release/rust-target/plan.json\n244 units: 172 lib, 16 proc-macro (host), 19 custom-build (host), 15 run custom-build, 17 lib (host), 4 run custom-build (host), 1 rlib\n[3/4] reconfigure\n[1/1499] mkdir stamps\n[2/1499] mkdir codegen\n[3/1499] install /workspace/bun\nbun install v1.4.3-canary.1 (367d939d9)\n\nChecked 26 installs across 65 packages (no changes) [34.00ms]\n[4/1499] install /workspace/bun/packages/bun-error\nbun install v1.4.3-canary.1 (367d939d9)\n\nChecked 1 install across 2 packages (no changes) [2.00ms]\n[5/1499] gen bake.{client,server,error}.js\n-> bake.client.js, bake.server.js, bake.error.js\n[6/1499] install /workspace/bun/src/node-fallbacks\nbun install v1.4.3-canary.1 (367d939d9)\n\nChecked 111 installs across \n... (truncated)\n```\n\n</details>\n\n<details><summary>diff hotspot</summary>\n\n```\nsrc/runtime/api/bun/h2/connection.rs      | 138 ++++++++++++++++-\n src/runtime/api/bun/h2_frame_parser.rs    |  24 +++\n test/js/node/http2/h2-conformance.test.ts | 240 ++++++++++++++++++++++++++++++\n 3 files changed, 398 insertions(+), 4 deletions(-)\n```\n\n</details>\n\n**gate history** \u00b7 15 passed \u00b7 0 rejected \u00b7 iteration 11\n\n<details><summary>evidence per changed file</summary>\n\n```\nfile                                       reads  edits  tests\nsrc/runtime/api/bun/h2/connection.rs          49     78    122\nsrc/runtime/api/bun/h2_frame_parser.rs        48     41    122\ntest/js/node/http2/h2-conformance.test.ts     19     26    121\n```\n\n</details>\n\n<!-- robobun:evidence:end -->\n\n---------\n\nCo-authored-by: Ciro Spaciari <ciro.spaciari@gmail.com>"
    },
    "CVE-2025-23419": {
      "e32be5c66bbd8b57677536dd63fe9a9d9b24e5e7": "node:tls: do not resume a session under a differently configured SNI context (#42998)\n\n### Problem\n\n- `tls.Server` with `ca: caA` plus `addContext(\"b.test\", { ca: caB, key,\ncert })` refuses a caA client certificate at `b.test`\n(`UNABLE_TO_VERIFY_LEAF_SIGNATURE`). The same client keeps `a.test`'s\n`session`, offers it as `servername: \"b.test\"`, and the handler runs\nwith `authorized: true`. TLS 1.3 and 1.2.\n- A resumed handshake skips client authentication. BoringSSL resumes\nonly when the session's id context equals the connection's\n(`vendor/boringssl/ssl/ssl_session.cc:474`). No `node:tls` context sets\none.\n- #36174 fixed this for `Bun.serve` `serverName` entries only. nginx\nfixed the class as CVE-2025-23419.\n\n### Fix\n\n- Each `SSL_CTX` a server can serve gets the SHA-256 of its options as\nits session id context (`create_ssl_context_with_digest`): the\n`tls.Server` and `Bun.listen` listeners, `SSLContextCache`,\n`tls.createSecureContext()`. `addCACert` folds its certificate in.\n- BoringSSL checks the id after the SNI switch, and `SSL_set_SSL_CTX`\ncopies the selected context's id. A mismatch is not an error: a full\nhandshake runs and verifies the certificate against that context's CA.\n- The id is content-derived, so an `SNICallback` that builds a context\nper handshake still resumes. Client sockets keep no id: one `SSL_CTX`\nbacks clients and servers, and a client aborts a resume when the ids\ndiffer.\n- Verified: `test/js/node/tls/node-tls-context.test.ts`, 12 new tests, 8\nfail on main, each client clear covered by one that fails without it.\nOther suites and vendored tests: see the notes.\n\n### Background\n\n- Session resumption: the client presents a ticket from an earlier\nconnection, both sides skip the certificate exchange, and the server\nreports the verdict stored in the session.\n- Session id context: an opaque string on an `SSL_CTX`, copied into each\nnew session. BoringSSL refuses a session whose id differs, and tells\nservers to partition sessions between SNI hosts this way\n(`include/openssl/ssl.h:2199-2207`). RFC 6066 section 3 allows resuming\nonly a session established for the requested name.\n- SNI context: `addContext()` and `SNICallback` pick an `SSL_CTX` per\nClientHello. The ticket keys belong to the listener default context, so\nevery context of that listener decrypts its tickets.\n\n<details><summary>Notes</summary>\n\n**Outcome table** (client certificate issued by caA, session from\n`a.test` offered at `b.test`, which trusts only caB):\n\n| | bun main | this PR |\n|---|---|---|\n| `authorized` | `true` | `false` |\n| `authorizationError` | none | `UNABLE_TO_VERIFY_LEAF_SIGNATURE` |\n| `isSessionReused()` | `true` | `false` |\n| handler reached with the default `rejectUnauthorized` | yes | no |\n| `a.test` session at `a.test` | resumed | resumed |\n\nAn independent client (`openssl s_client -servername a.test -sess_out`,\nthen `-servername b.test -sess_in`) shows the same before and after, so\nthe door does not depend on Bun's client.\n\n**Why the id is derived from the options and not from the context\nobject.** #36174 uses a counter, which fits `Bun.serve` because those\ncontexts are built once per server. A `node:tls` `SNICallback` commonly\ncalls `tls.createSecureContext()` for each handshake. With an id per\nobject such a server would never resume. With the options digest, two\ncontexts built from the same options share sessions, which is safe\nbecause they authenticate clients the same way. The digest is the\nexisting `BunSocketContextOptions::digest()`, the key of\n`SSLContextCache`: inline PEM content, and path plus mtime and size for\nthe file options. It is broader than the client-certificate policy, so a\nchange to an unrelated option (`ciphers`, `sigalgs`) also ends\nresumption for old sessions. That errs toward a full handshake.\n\n**Why the server side needs no code at the SNI switch.**\n`SSL_set_SSL_CTX` replaces the connection's `CERT` with a copy of the\nnew context's `CERT`, and the session id context is a field of `CERT`.\nSo the servername callback, the static SNI tree, the asynchronous\n`SNICallback` resume and `socket.setKeyCert()` all move the connection\nto the id of the selected context with no extra call.\n\n**Context sharing.** `SSLContextCache` shares one `SSL_CTX` between all\nusers of the same options, clients and servers, which is why the client\nside clears the id per socket.\n\n**Client clears.** The two places that build an `SSL` are\n`us_internal_ssl_attach` (socket) and `SSLWrapper::init_with_ctx`\n(duplex, named pipe). `setKeyCert` is the only other caller of\n`SSL_set_SSL_CTX`, and on a client Bun applies it (Node ignores it\nthere), so it needs the clear too. Tests: `lets a client offer a session\nunder other client options` covers the first two (TCP and Duplex), `lets\na client that calls setKeyCert() still resume` the third. Each fails\nwith `ERR_SSL_ATTEMPT_TO_REUSE_SESSION_IN_DIFFERENT_CONTEXT` when its\nclear is removed. A future `SSL_new` site for a client needs the same\nline. There is no lint for that.\n\n**Landing order.** #28691 (server `ticketKeys`) should land after this\none: shared ticket keys let one process decrypt another's tickets, which\nextends the same door across servers and processes, and it needs a\nnegative test for that. The options digest keeps cross-process\nresumption working for identical options, which a counter or a pointer\nwould not. #33483 and #42050 touch the SNI switch and\n`setSecureContext`; they do not set a session id context and do not\nconflict.\n\n**Not covered.** `addCACert()` on a context that already serves\nconnections: a handshake that selected the context before the call, and\nverifies its client certificate after it, issues a session under the old\nid. A second context built from the same options without that CA can\nthen resume it. The window is one round trip and needs that sibling\ncontext. On `main` any context resumes any session, so this PR narrows\nthat door. Node allows `addCACert()` at any time, so this PR adds no\nrefusal.\n\nTLS 1.2 with an `ALPNCallback` that calls `socket.setKeyCert(ctx)`:\nBoringSSL decides TLS 1.2 resumption before the ALPN callback runs, so a\nsession id context cannot reach that path. TLS 1.3 with the same setup\nis covered, because ALPN runs before session selection there. The\n`sessionIdContext` option is still accepted and dropped; honoring it\nbelongs with #28691. `node:quic` has its own context path and is not\nchanged.\n\n**Suites run on the debug build:** `node-tls-context.test.ts` (29 pass),\n`node-tls-connect.test.ts` (57 pass), `node-tls-server.test.ts` (77\npass, 1 failure that also fails without this diff: `localhost` resolves\nto `::1` first in this container), `node-tls-cert.test.ts`,\n`node-tls-namedpipes.test.ts`, `ssl-ctx-cache.test.ts`,\n`node-tls-upgrade.test.ts`, `node-tls-internals.test.ts`,\n`renegotiation.test.ts`,\n`node-tls-connect-hostname-verification.test.ts`,\n`bun-serve-ssl.test.ts` (18 pass), `fetch.tls.test.ts` (41 pass),\n`fetch-session.test.ts` (32 pass), `proxy.test.ts` (90 pass). Vendored\nNode tests: `test-tls-add-context`, `test-tls-sni-option`,\n`test-tls-sni-server-client`, `test-tls-sni-servername`,\n`test-tls-snicallback-error`, `test-tls-empty-sni-context`,\n`test-tls-client-resume`, `test-tls-client-resume-12`,\n`test-https-client-resume`, `test-tls-ticket`,\n`test-tls-ticket-cluster`, `test-tls-session-cache`,\n`test-tls-secure-session`, `test-tls-connect-secure-context`,\n`test-tls-secure-context-usage-order`, `test-tls-alpn-server-client`,\n`test-tls-psk-circuit`, `test-tls-psk-server`,\n`test-tls-reuse-host-from-socket`, `test-https-agent-session-reuse`,\n`test-https-agent-disable-session-reuse`,\n`test-https-agent-session-eviction`,\n`test-https-agent-session-injection`, `test-https-agent-sni`.\n\n**Comments.** The rationale for the deliberate refusal (RFC 6066 section\n3, the BoringSSL header) sits on `us_ssl_apply_selected_ctx` in\n`openssl.c`, next to the SNI switch, and on the test block. The Rust\ncall sites carry one line each.\n\n**Self-reviewed:** 6 concerns raised, 6 addressed\n(default-`rejectUnauthorized` coverage, the Windows named-pipe listener,\nthe `setKeyCert` clear test, the scope claim above, the\ndeliberate-refusal comments at the helper and the test, and the landing\norder).\n\n</details>\n\n<!-- robobun:evidence:begin -->\n\n---\n\n**[human-review]** gate passed \u00b7 iteration 1 \u00b7 9 files touched\n\n<details><summary>fails on main (without fix)</summary>\n\n```console\nASAN without fix: 8 FAILED\n$ BUN_DEBUG_QUIET_LOGS=1 bun scripts/build.ts --profile=debug --quiet test \"--reporter=junit\" \"--reporter-outfile=/tmp/pr_gate.xml\" test/js/node/tls/node-tls-context.test.ts\nbun test v1.4.3 (c6b7fcb5b)\n\ntest/js/node/tls/node-tls-context.test.ts:\n(pass) tls.Server > addContext [898.64ms]\n(pass) tls.Server > should select the most recently added SecureContext [175.29ms]\n(pass) tls.Server > should allow multiple CA [175.40ms]\n(pass) tls.Server > should allow multiple CA in newline-separated strings [53.03ms]\n(pass) tls.Server > SNI tls.Server + tls.connect [286.16ms]\n475 | \n476 |       // b.test verifies the certificate again, against ca2, whichever\n477 |       // context's session the client offers.\n478 |       const fromDefault = await connect(server, \"b.test\", { session: atDefault.session });\n479 |       const fromA = await connect(server, \"b.test\", { session: atA.session });\n480 |       expect([fromDefault.seen, fromA.seen]).toEqual([refused(\"b.test\"), refused(\"b.test\")]);\n                                                   ^\nerror: expect(received).toEqual(expected)\n\n  [\n    {\n-     \"authorized\": false,\n-     \"error\": \"UNABLE_TO_VERIFY_LEAF_SIGNATURE\",\n+     \"autho\n... (truncated)\n\nrelease without fix: all passed\nbun test v1.4.3-canary.1 (e0d54a6d7)\n\ntest/js/node/tls/node-tls-context.test.ts:\n(pass) tls.Server > addContext [29.23ms]\n(pass) tls.Server > should select the most recently added SecureContext [6.86ms]\n(pass) tls.Server > should allow multiple CA [4.38ms]\n(pass) tls.Server > should allow multiple CA in newline-separated strings [2.53ms]\n(pass) tls.Server > SNI tls.Server + tls.connect [11.38ms]\n(pass) session resumption across SNI contexts (TLSv1.3) > runs a full handshake under a context that trusts another CA [20.26ms]\n(pass) session resumption across SNI contexts (TLSv1.3) > keeps a refused client out of the handler with the default rejectUnauthorized [9.98ms]\n(pass) session resumption across SNI contexts (TLSv1.3) > covers the contexts an SNICallback builds for each handshake [10.63ms]\n(pass) session resumption across SNI contexts (TLSv1.3) > covers contexts that differ only by addCACert [11.52ms]\n(pass) session resumption across SNI contexts (TLSv1.3) > lets a client offer a session under other client options [9.06ms]\n(pass) session resumption across SNI contexts (TLSv1.3) > lets a client that calls setKeyCert() still resume [4.72ms]\n(pass) session resumption \n... (truncated)\n```\n\n</details>\n\n<details><summary>passes on PR (with fix)</summary>\n\n```console\nASAN with fix: all passed\n$ BUN_DEBUG_QUIET_LOGS=1 bun scripts/build.ts --profile=debug --quiet test \"--reporter=junit\" \"--reporter-outfile=/tmp/pr_gate.xml\" test/js/node/tls/node-tls-context.test.ts\nbun test v1.4.3 (c6b7fcb5b)\n\ntest/js/node/tls/node-tls-context.test.ts:\n(pass) tls.Server > addContext [765.80ms]\n(pass) tls.Server > should select the most recently added SecureContext [124.51ms]\n(pass) tls.Server > should allow multiple CA [201.14ms]\n(pass) tls.Server > should allow multiple CA in newline-separated strings [75.66ms]\n(pass) tls.Server > SNI tls.Server + tls.connect [278.18ms]\n(pass) session resumption across SNI contexts (TLSv1.3) > runs a full handshake under a context that trusts another CA [495.27ms]\n(pass) session resumption across SNI contexts (TLSv1.3) > keeps a refused client out of the handler with the default rejectUnauthorized [221.28ms]\n(pass) session resumption across SNI contexts (TLSv1.3) > covers the contexts an SNICallback builds for each handshake [164.83ms]\n(pass) session resumption across SNI contexts (TLSv1.3) > covers contexts that differ only by addCACert [121.09ms]\n(pass) session resumption across SNI contexts (TLSv1.3) > lets a client offer a session und\n... (truncated)\n\nrelease with fix: all passed\n$ bun scripts/build.ts --profile=release\n[configured] bun-profile \u2192 bun (stripped) in 783ms (unchanged)\nninja: Entering directory `/workspace/bun/build/release'\n[1/129] gen generated_host_exports.rs\ngenerated_host_exports.rs: 121 exports (host=5, lazy=10, generic=106, rust=0); 242 extern-C blocks audited\n[2/129] gen cpp.rs (cppbind)\n[3/129] gen JSSink.{cpp,h,lut.h,rs}\ngenerated_jssink.rs: 7 sinks, 84 exported symbols\nGenerating /workspace/bun/build/release/codegen/JSSink.lut.h from /workspace/bun/build/release/codegen/JSSink.lut.txt\n[4/129] gen JS modules (bundle-modules)\nPreprocess modules (12205ms)\nBundle modules (134ms)\nPostprocesss modules (434ms)\nBundle Functions (731ms)\nGenerate Code (51ms)\n\n[13.57s] Bundled \"src/js\" for production\n  2600 kb\n  197 internal modules\n  13 native modules\n  50 internal functions across 16 files\n[4/27] cargo bun_runtime \u2192 libbun_runtime.a\n\u001b[1m\u001b[33mwarning\u001b[0m\u001b[1m: binary `bun_shim_impl` should have a kebab-case name\u001b[0m\n   \u001b[1m\u001b[94m|\u001b[0m\n\u001b[1m\u001b[94m 1\u001b[0m \u001b[1m\u001b[94m|\u001b[0m /workspace/bun/build/release/rust-target/.../bun_shim_impl\n   \u001b[1m\u001b[94m|\u001b[0m                                              \u001b[1m\u001b[33m^^^^^^^^^^^^^\u001b[0m\n   \u001b[1m\u001b[94m|\u001b\n... (truncated)\n```\n\n</details>\n\n<details><summary>diff hotspot</summary>\n\n```\npackages/bun-usockets/src/crypto/openssl.c |  15 +-\n src/boringssl_sys/boringssl.rs             |  10 ++\n src/runtime/api/bun/SSLContextCache.rs     |   2 +-\n src/runtime/api/bun/SecureContext.rs       |  27 ++-\n src/runtime/socket/Listener.rs             |   5 +-\n src/runtime/socket/tls_socket_functions.rs |   4 +\n src/uws/lib.rs                             |   6 +-\n src/uws_sys/SocketContext.rs               |  19 ++\n test/js/node/tls/node-tls-context.test.ts  | 280 ++++++++++++++++++++++++++++-\n 9 files changed, 356 insertions(+), 12 deletions(-)\n```\n\n</details>\n\n**gate history** \u00b7 2 passed \u00b7 0 rejected \u00b7 iteration 1\n\n<details><summary>evidence per changed file</summary>\n\n```\nfile                                        reads  edits  tests\npackages/bun-usockets/src/crypto/openssl.c      5      2     23\nsrc/boringssl_sys/boringssl.rs                  1      3     23\nsrc/runtime/api/bun/SSLContextCache.rs          1      1     23\nsrc/runtime/api/bun/SecureContext.rs            2      3     23\nsrc/runtime/socket/Listener.rs                  3      2     23\nsrc/runtime/socket/tls_socket_functions.rs      1      1     23\nsrc/uws/lib.rs                                  1      2     24\nsrc/uws_sys/SocketContext.rs                    3      2     23\ntest/js/node/tls/node-tls-context.test.ts       6      7     23\n```\n\n</details>\n\n<!-- robobun:evidence:end -->"
    },
    "CVE-2026-48615": {
      "6e2c74ad34084e20839e251bf7fc1fc7ea699179": "node:http: redact proxy credentials in proxy error messages (#42947)\n\n### Problem\n- Two `node:http` error messages print the proxy username and password.\nError messages reach logs and error trackers.\n- `ERR_PROXY_TUNNEL`, when a proxy rejects `CONNECT`: `Failed to\nestablish tunnel to example.com:443 via\nhttp://user:s3cret@127.0.0.1:8080: HTTP/1.1 407 Proxy Authentication\nRequired`. `ProxyConfig` stores the raw URL in `href`\n(`src/js/internal/http.ts:405`), and `establishTunnel()` prints it\n(`src/js/node/https.ts:168`). Node fixed this as CVE-2026-48615 in\nnodejs/node@3e9954a88b (v26.3.1).\n- `ERR_PROXY_INVALID_CONFIG`, when the proxy URL does not parse:\n`Invalid proxy URL: http://user:s3cret@proxy.example.com:99999`. Four\nthrow sites print the raw string. Node still does this.\n\n### Fix\n- `ProxyConfig` clears the username and password on the parsed URL and\nstores that `href` (the upstream change). The `Proxy-Authorization`\nheader uses the values read before the clear.\n- For a string that does not parse, `redactInvalidProxyUrl()` removes\neverything between `scheme://` and the last `@` (an unescaped `/` in a\npassword ends the authority early). This diverges from Node on purpose,\nonly for a URL with `@`. `http.setGlobalProxyFromEnv()` also loses its\n`Invalid proxy URL: ` prefix, which Node does not have.\n- Verified: 4 tunnel cases in `node-http.test.ts` and 7 config cases in\n`node-http-proxy-url.node.mts` (runs under Node and Bun). 10 of the 11\nfail without the fix.\n- Self-reviewed: 1 concern raised, 1 addressed (see Notes).\n\n### Background\n- `https.Agent` reads proxy settings from `proxyEnv` (`process.env` with\n`NODE_USE_ENV_PROXY=1`). `ProxyConfig` holds the parsed result.\n- An HTTPS request through a proxy starts with `CONNECT host:port`. Any\nstatus other than `200` produces `ERR_PROXY_TUNNEL`.\n- Userinfo is the `user:password@` part of a URL. It becomes the\n`Proxy-Authorization` header.\n\n<details><summary>Notes</summary>\n\n- Commit 1 is the port of nodejs/node@3e9954a88b. The\n`ERR_PROXY_INVALID_CONFIG` part came from review. It diverges from Node,\nso it is in its own commits and can be dropped without the tunnel fix.\n- Node v26.3.0 leaks in the tunnel message. v26.3.1 and v24.17.0 have\nthe fix.\n`test/js/node/test/parallel/test-https-proxy-request-auth-failure.mjs`\nis now the upstream file, byte for byte.\n- Tunnel message before: `via http://user:s3cret@127.0.0.1:8080`. After:\n`via http://127.0.0.1:8080/`. The trailing slash comes from URL\nserialization, same as Node v26.3.1. The upstream assertion is `/via\nhttp:\\/\\/localhost:\\d+\\/?/`.\n- Tunnel cases covered: `user:password@`, `user@`, `:password@`, and\npercent-encoded userinfo. Each case also checks that the proxy still\nreceives the credentials.\n- No other code reads `ProxyConfig.href`. `_http_agent.ts` and\n`_http_client.ts` read only `auth`, `protocol` and\n`proxyConnectionOptions`.\n- The four `Invalid proxy URL` sites: the `ProxyConfig` constructor and\nthe CR/LF check in `parseProxyUrl` (`src/js/internal/http.ts:401` and\n`:481`), and two in `http.setGlobalProxyFromEnv`\n(`src/js/node/http.ts:101` and `:104`). Node:\n[lib/internal/http.js#L114](https://github.com/nodejs/node/blob/3e9954a88b6a291fc4041aa7ff2b9725f4f86025/lib/internal/http.js#L114)\nand\n[lib/http.js#L142-L146](https://github.com/nodejs/node/blob/v26.3.0/lib/http.js#L142-L146).\n- Message shapes, same as Node v26.3.0: `new http.Agent` and `new\nhttps.Agent` print `Invalid proxy URL: <url>`. `setGlobalProxyFromEnv`\nprints `<url>` alone. A URL with CR or LF gets the prefix from both,\nbecause the shared environment parser rejects it first.\n- `node-http-proxy-url.test.ts` runs `node-http-proxy-url.node.mts` with\n`node --test` and with `bun test`. The expected messages are Node's own.\nOne line depends on the runtime: Node prints the userinfo, Bun removes\nit.\n- Config cases covered: no credentials, no credentials with LF in the\nhost (the control, it passes without the fix), port out of range, LF in\nthe password, unescaped `/` in the password, unescaped `@` in the\npassword, space in the host.\n- `redactInvalidProxyUrl()` can remove too much, never too little.\n`http://proxy:99999/a@b` prints as `http://b`. A prefix that is not\n`scheme://` is dropped too: `http:\\\\user:pw@host:99999` prints as\n`host:99999`.\n- The vendored tests still pass.\n`test-http-proxy-request-invalid-proxy.mjs` asserts the exact message\nonly for URLs without `@`. The `invalid-credentials` tests match\n`/Invalid proxy URL/`.\n`test-http-set-global-proxy-from-env-invalid-url.mjs` checks only the\ncode.\n- Self-review concern: other sites print the raw proxy URL. 4 are fixed\nhere. The rest are not changed, on purpose:\n- `src/js/node/_http_agent.ts:615` and `:618`. With\n`NODE_USE_ENV_PROXY=1`, an invalid proxy URL in the environment throws\n`ERR_INVALID_URL` at startup. The message is `Invalid URL`. The `input`\nproperty holds the raw string, as it does for every `new URL()` failure.\nNode chose this split in nodejs/node#38614: no URL in the message, the\nURL in `input`.\n- `util.inspect(agent)` shows the `Proxy-Authorization` value in the\n`auth` field of the proxy config. This is not an error, and Node shows\nthe same.\n- The `$debug` lines that print the proxy config. They do not exist in\nrelease builds.\n- `src/jsc/bindings/webcore/WebSocket.cpp:219`. The WebSocket `proxy`\noption prints the raw string in its own `Invalid proxy URL: `\nSyntaxError. The open PR #41698 already changes that line\n(`urlForErrorMessage(proxyUrl)`), so this PR does not touch it.\n- Suites run with the debug build: all of `node-http.test.ts` (167 pass,\n1 skip), `node-http-proxy-url.test.ts` (Node 10 of 10, Bun 10 of 10),\nand 47 vendored proxy tests (`test-http-proxy-request*.mjs`,\n`test-https-proxy-request*.mjs`, `test-http-request-proxy-post*.mjs`,\n`test-https-request-proxy-post.mjs`,\n`test-http-set-global-proxy-from-env-*.mjs`).\n- Repro for the tunnel message, without a network:\n\n```js\nconst https = require(\"https\");\nconst http = require(\"http\");\nconst proxy = http.createServer();\nproxy.on(\"connect\", (req, socket) => socket.end(\"HTTP/1.1 502 Bad Gateway\\r\\nconnection: close\\r\\n\\r\\n\"));\nproxy.listen(0, \"127.0.0.1\", () => {\n  const agent = new https.Agent({\n    proxyEnv: { https_proxy: `http://user:s3cret@127.0.0.1:${proxy.address().port}` },\n  });\n  const req = https.request({ host: \"example.invalid\", port: 443, agent });\n  req.on(\"error\", e => {\n    console.log(e.code, e.message);\n    agent.destroy();\n    proxy.close();\n  });\n  req.end();\n});\n```\n\n- Repro for the config message: `new (require(\"http\").Agent)({ proxyEnv:\n{ http_proxy: \"http://user:s3cret@proxy.example.com:99999\" } })`.\n\n</details>\n\n<!-- robobun:evidence:begin -->\n\n---\n\n**[human-review]** gate passed \u00b7 iteration 0 \u00b7 5 files touched\n\n<details><summary>fails on main (without fix)</summary>\n\n```console\nASAN without fix: 4 failed, 1 skipped\n$ BUN_DEBUG_QUIET_LOGS=1 bun scripts/build.ts --profile=debug --quiet test \"--reporter=junit\" \"--reporter-outfile=/tmp/pr_gate.xml\" test/js/node/http/node-http.test.ts\nbun test v1.4.3 (367d939d9)\n\ntest/js/node/http/node-http.test.ts:\n(pass) node:http > createServer > hello world [465.97ms]\n(pass) node:http > createServer > is not marked encrypted (#5867) [72.27ms]\n(pass) node:http > createServer > request & response body streaming (large) [125.22ms]\n(pass) node:http > createServer > request & response body streaming (small) [76.62ms]\n(pass) node:http > createServer > listen should return server [43.55ms]\n(pass) node:http > createServer > listen callback should be bound to server [21.19ms]\n(pass) node:http > createServer > emits 'listening' on the next tick, before the event loop polls [42.57ms]\n(pass) node:http > createServer > emits a listen() error on the next tick, before the event loop polls [55.48ms]\n(pass) node:http > createServer > calls the listen() callback after a retry from the EADDRINUSE 'error' handler [54.50ms]\n(pass) node:http > createServer > http: closing a server listened from 'beforeExit' > re-emits 'beforeExit' [1584.70ms]\n(pass) node:http > crea\n... (truncated)\n\nrelease without fix: 5 failed, 1 skipped\nbun test v1.4.3-canary.1 (367d939d9)\n\ntest/js/node/http/node-http.test.ts:\n(pass) node:http > createServer > hello world [11.25ms]\n(pass) node:http > createServer > is not marked encrypted (#5867) [2.72ms]\n(pass) node:http > createServer > request & response body streaming (large) [5.21ms]\n(pass) node:http > createServer > request & response body streaming (small) [3.50ms]\n(pass) node:http > createServer > listen should return server [1.38ms]\n(pass) node:http > createServer > listen callback should be bound to server [1.22ms]\n(pass) node:http > createServer > emits 'listening' on the next tick, before the event loop polls [1.59ms]\n(pass) node:http > createServer > emits a listen() error on the next tick, before the event loop polls [4.87ms]\n(pass) node:http > createServer > calls the listen() callback after a retry from the EADDRINUSE 'error' handler [2.76ms]\n(pass) node:http > createServer > http: closing a server listened from 'beforeExit' > re-emits 'beforeExit' [24.66ms]\n(pass) node:http > createServer > https: closing a server listened from 'beforeExit' > re-emits 'beforeExit' [29.72ms]\n(pass) node:http > createServer > should use the provided port [1.16ms]\n(pa\n... (truncated)\n```\n\n</details>\n\n<details><summary>passes on PR (with fix)</summary>\n\n```console\nASAN with fix: 1 skipped\n$ BUN_DEBUG_QUIET_LOGS=1 bun scripts/build.ts --profile=debug --quiet test \"--reporter=junit\" \"--reporter-outfile=/tmp/pr_gate.xml\" test/js/node/http/node-http.test.ts\nbun test v1.4.3 (367d939d9)\n\ntest/js/node/http/node-http.test.ts:\n(pass) node:http > createServer > hello world [434.51ms]\n(pass) node:http > createServer > is not marked encrypted (#5867) [67.62ms]\n(pass) node:http > createServer > request & response body streaming (large) [154.08ms]\n(pass) node:http > createServer > request & response body streaming (small) [77.19ms]\n(pass) node:http > createServer > listen should return server [35.65ms]\n(pass) node:http > createServer > listen callback should be bound to server [49.85ms]\n(pass) node:http > createServer > emits 'listening' on the next tick, before the event loop polls [41.13ms]\n(pass) node:http > createServer > emits a listen() error on the next tick, before the event loop polls [54.91ms]\n(pass) node:http > createServer > calls the listen() callback after a retry from the EADDRINUSE 'error' handler [49.71ms]\n(pass) node:http > createServer > http: closing a server listened from 'beforeExit' > re-emits 'beforeExit' [1395.64ms]\n(pass) node:http > crea\n... (truncated)\n\nrelease with fix: 1 skipped\n$ bun scripts/build.ts --profile=release\n[configured] bun-profile \u2192 bun (stripped)\n  target       linux-x64-gnu\n  build type   Release\n  build dir    ./build/release\n  revision     10471f4c5f\n  features     lto, baseline\n\n23 deps, 136 codegen, 1176 objects in 873ms\n\nninja: Entering directory `/workspace/bun/build/release'\n[1/4] fetch rust-argon2\n[rust-argon2] up to date\n[2/4] fetch lolhtml\n[lolhtml] up to date\n[2/4] cargo plan \u2192 /workspace/bun/build/release/rust-target/plan.json\n244 units: 172 lib, 16 proc-macro (host), 19 custom-build (host), 15 run custom-build, 17 lib (host), 4 run custom-build (host), 1 rlib\n[3/4] reconfigure\n[1/1499] mkdir stamps\n[2/1499] mkdir codegen\n[3/1499] install /workspace/bun\nbun install v1.4.3-canary.1 (367d939d9)\n\nChecked 26 installs across 65 packages (no changes) [13.00ms]\n[4/1499] install /workspace/bun/packages/bun-error\nbun install v1.4.3-canary.1 (367d939d9)\n\nChecked 1 install across 2 packages (no changes) [3.00ms]\n[5/1499] install /workspace/bun/src/node-fallbacks\nbun install v1.4.3-canary.1 (367d939d9)\n\nChecked 111 installs across 104 packages (no changes) [6.00ms]\n[6/1499] esbuild bun-error\n\n  ../../build/release/codegen\n... (truncated)\n```\n\n</details>\n\n<details><summary>diff hotspot</summary>\n\n```\nsrc/js/internal/http.ts                            | 23 ++++++++--\n src/js/node/http.ts                                |  6 +--\n test/js/node/http/node-http-proxy-url.node.mts     | 52 +++++++++++++++++++++-\n test/js/node/http/node-http.test.ts                | 43 ++++++++++++++++++\n .../test-https-proxy-request-auth-failure.mjs      |  3 ++\n 5 files changed, 120 insertions(+), 7 deletions(-)\n```\n\n</details>\n\n**gate history** \u00b7 2 passed \u00b7 0 rejected \u00b7 iteration 0\n\n<details><summary>evidence per changed file</summary>\n\n```\nfile                                                      reads  edits  tests\nsrc/js/internal/http.ts                                       6      9     34\nsrc/js/node/http.ts                                           3      2     33\ntest/js/node/http/node-http-proxy-url.node.mts                1      1     38\ntest/js/node/http/node-http.test.ts                           4      5     30\n\u2026test/parallel/test-https-proxy-request-auth-failure.mjs      0      0     42\n```\n\n</details>\n\n<!-- robobun:evidence:end -->",
      "dc078c8bc4d52b73c1755ba35f1fe8b0f88d35ac": "node:https: do not reuse a connection across requests that have their own checkServerIdentity (#42946)\n\n### Problem\n- An `https.Agent` lets a request use a connection that a different\nidentity check approved. Request 1 passes a permissive\n`checkServerIdentity`, or none. Request 2 passes a rejecting one and\ngets `200`. Its callback never runs.\n- The cause is `Agent#getName()` (`src/js/node/https.ts:371`). The name\nhas no `checkServerIdentity` part. It keys the keep-alive pool,\n`_sessionCache`, and the request queue. (#36131 since closed the\nresumed-session part.)\n- Node fixed this as CVE-2026-58040 (nodejs/node@52a8ace880).\n\n### Fix\n- Port of 52a8ace880. A request with its own `checkServerIdentity` gets\na unique Agent name and uses no cached session (nor Bun's\n`establishTunnel` cache). An Agent-level callback still pools.\n- Stricter than Node twice. `ClientRequest` marks the request, so\n`http.request({ protocol: \"https:\", agent })` is covered. The Agent's\n`'free'` handler refuses the socket, so agent-base style Agents are\ncovered.\n- The unique name leaves the Agent's bookkeeping intact: no stale\n`agent.sockets` entry, no dead queue head, no wait behind an idle socket\nunder `maxTotalSockets` (Notes).\n- Verified:\n`test/js/node/http/node-https-agent-checkserveridentity-reuse.test.ts`.\nMain b8eacead4d fails 20 of 30. Node v26.5.1 passes the 21 it runs.\n\n### Background\n- `https.Agent` pools sockets in `freeSockets[name]`. `getName(options)`\nbuilds `name` from host, port and TLS options.\n- `_sessionCache` holds one TLS session per name. A new socket offers it\nto skip the full handshake.\n- `checkServerIdentity(hostname, cert)` is the certificate pinning hook.\nIt runs once per full handshake.\n- Cost, as in Node: a full handshake per such request, and `maxSockets`\ndoes not bound them. A callback on the Agent keeps reuse.\n\n<details><summary>Notes</summary>\n\n**Repro.** One Agent, vendored `agent1` certificate. Request 2 carries\n`checkServerIdentity: () => new Error(\"PIN MISMATCH\")`.\n\n```\n                                    bun canary 09bb54630      this branch        node v26.3.0   node v26.5.1\nrequest 1 default,    keepAlive     200 on conn 1, 0 calls    ERR PIN MISMATCH   200            ERR PIN MISMATCH\nrequest 1 default,    no keepAlive  200, session resumed      ERR PIN MISMATCH   200            ERR PIN MISMATCH\nrequest 1 permissive, keepAlive     200 on conn 1             ERR PIN MISMATCH   200            ERR PIN MISMATCH\nrequest 1 permissive, no keepAlive  200, session resumed      ERR PIN MISMATCH   200            ERR PIN MISMATCH\n```\n\nThe reverse direction and the queue have the same hole on stock bun. A\ndefault-check request for `servername: \"not-agent1\"` gets `200` after a\npermissive callback approved that name. With `maxSockets: 1`,\n`Agent#removeSocket` makes the socket of a queued request from the\noptions of the socket that closed, so the queued request runs the\ncallback of the request ahead of it.\n\n**The test file under each runtime.**\n\n| runtime | pass | fail | skipped |\n|---|---|---|---|\n| this branch (debug build) | 30 | 0 | 0 |\n| main b8eacead4d (has #36131), without this PR | 10 | 20 | 0 |\n| bun canary c6b7fcb5b (before #36131) | 6 | 24 | 0 |\n| node v26.5.1 (has 52a8ace880) | 21 | 0 | 8 (Bun-only) |\n| node v26.3.0 (does not) | 5 | 0 | 24 |\n\nThe cases that pass everywhere are controls: an Agent-level callback and\na request that passes `tls.checkServerIdentity` itself still share the\nsocket or the session, and `agent.sockets` has its entry while a proxy\ntunnel connects. The other cases are gated on the Node versions that\ncarry the fix (v22.23.2, v24.18.1, v26.5.1). Bun always runs them.\n\n**Difference 1: where the socket is refused.** Node marks the socket in\n`https.Agent`'s `createConnection` and refuses it in\n`https.Agent#keepSocketAlive`. An `https.Agent` subclass that replaces\n`createConnection`, and an `http.Agent` that borrows\n`https.Agent#getName` (agent-base: https-proxy-agent,\nsocks-proxy-agent), get the unique name without the mark. Three requests\nwith their own callback on a `keepAlive` Agent:\n\n```\n                                              parked sockets after\n                                              stock   straight port   node v26.5.1   this branch\nhttps.Agent                                   1       0               0              0\nhttp.Agent that borrows https getName         1       3               3              0\nhttps.Agent that replaces createConnection    1       3               3              0\n```\n\nA parked socket under a unique name is never taken. It counts against\n`maxTotalSockets` until the origin closes it. The request options are\nwhat make the name unique, and `installListeners()` already hands them\nto the `'free'` handler, so the handler reads the mark there. The socket\nmark and the `keepSocketAlive` override of the upstream patch are then\nnot needed.\n\n**Difference 2: where the mark is set.** Node sets it in\n`https.request()`. `http.request({ protocol: \"https:\", agent })` and\n`new http.ClientRequest()` reach the same Agent without it, and still\nshare on Node v26.5.1. Here the `ClientRequest` constructor sets it,\nafter it resolves the agent, so no route skips it.\n\n**Difference 3: what a failed socket creation leaves behind.**\n`Agent#addRequest` makes `this.sockets[name] = []` before a socket\nexists, in Node and here. If no socket arrives (a refused proxy tunnel,\na `createConnection` that throws) nothing in Node removes the entry.\nWith one name per Agent that was one stale entry. With one name per\nrequest it is one per failed request, and each key holds the PEM text of\n`ca`, `cert` and `key`. Five refused tunnels: stock 1 entry, straight\nport 5, node v26.5.1 5, this branch 0. The branch removes the empty\nentry in the error path of both creation callbacks and around a\n`createSocket()` that throws. A queued request whose socket could not be\nmade also leaves its queue. In Node it stays at the head until a socket\nof its name frees. No socket ever has the name of such a request, and\n`removeSocket()` only looks at the first queue, so every queue behind it\nstopped for good. The same path now gives a `createConnection()` that\nthrows to the queued request. Before, and in Node, that is an uncaught\nexception from a `'close'` handler.\n\n**Difference 4: an idle socket gives up its slot.** `new https.Agent({\nkeepAlive: true, maxTotalSockets: 1 })`, one finished request, then a\nrequest with its own callback. The pooled socket holds the only slot and\nthe new request cannot take it. Node v26.5.1 waits until the server\ncloses the idle socket. Stock bun reused the socket at once. Here\n`addRequest()` destroys one idle socket when such a request has to\nqueue. No other request does this, so the rest of the scheduling is\nNode's.\n\n**The tunnel path.** `establishTunnel()` in Bun attaches the session\ncache listeners to the tunneled socket. Upstream caches no session\nthere. Through an `https:` proxy the cached session is resumed, and on\nstock bun request 2 gets `200` without its callback. Through an `http:`\nproxy the session is cached and not resumed today (a bug in\n`tls.connect({ socket, session })` that is tracked apart from this PR).\nThe branch caches no session for a marked request on either path.\n\n**Costs in full.**\n- `maxSockets` is a limit per name, so it does not bound these requests:\n`maxSockets: 1` and 20 concurrent requests open 20 connections here and\non node v26.5.1, 1 on stock. `maxTotalSockets` still bounds them, to N+1\nin one case that Node's Agent has for every pair of names: the `'free'`\nhandler pools a socket at the limit, and `removeSocket()` then makes a\nsocket for a queued request of another name.\n- No Bun user reported this hole. The report that exists, #40308, asked\nfor more reuse with a callback on `fetch`, and #42692 (item B4) later\ngave that reuse up for the same reason as this PR.\n\n**Why Node's own regression test is not added.**\n`test-https-agent-checkserveridentity-reuse.js` ends with\n`second.socket.isSessionReused()` one promise hop after the response\n`'end'` of a `Connection: close` request. Bun has destroyed that socket\nby then (`Socket.prototype._final` ends in a `nextTick`, Node waits for\nthe shutdown callback), so the call reads `false`. The session is\nresumed: the same call at `'response'` reads `true`. Files under\n`test/js/node/test/parallel/` are not edited, so the file stays out. The\nnew test covers each of its cases, and it runs under Node.\n\n**Other open PRs.**\n- #36131 merged while this PR was open. It runs the identity check on\nresumed sessions too, so on main the four plain `keepAlive: false` cases\nof the new test already pass. The pooled socket, the queue, both proxy\npaths and the bookkeeping cases still fail there (20 of 30). The session\nrule of this PR stays: it keeps one cache entry per request out of the\n100-entry `_sessionCache`, and it matches the upstream patch. The new\ntest passes 30 of 30 on this branch rebased over #36131.\n- #42498 appends to the same tail of `getName()`. The second one to land\nhas a small text conflict. Both suffixes must stay, the per-request one\nlast.\n\n**Suites.** 182 files of\n`test/js/node/test/{parallel,sequential}/test-http*` that mention\nagents, sockets or keep-alive, `test-tls-check-server-identity.js`,\n`test-tls-client-resume*.js`, all of `node-http.test.ts` (159 pass),\n`node-https-checkServerIdentity.test.ts`,\n`node-http-agent-free-socket.test.ts`,\n`node-http-agent-tls-options.test.mts`, `node-http-proxy-url.test.ts`.\nTwo vendored files fail on the debug build with and without this change,\nand pass on the release canary: `test-https-timeout.js` (a 10 ms request\ntimeout never fires) and `test-http-agent-keepalive.js` (an assertion\nbehind a 1 ms timer).\n\n**Review history.** A review of the first version (the straight port)\nasked for four changes: no stale `agent.sockets` entries, a test through\nan `https:` proxy, the full cost statement, and its execution findings\n(the parked sockets above, the queue cases). The review on the PR then\nfound the `http.request()` route, the wait behind an idle socket, and\nthe dead queue head, and asked to keep Node's `agent.sockets` entry. All\nare in this version. The `maxSockets` cost and the N+1 case are not\nchanged. It also named two unrelated Node security fixes that Bun lacks\n(CVE-2026-48615, CVE-2026-48618). They are tracked apart from this PR.\n\n</details>\n\n<!-- robobun:evidence:begin -->\n\n---\n\n**[human-review]** gate passed \u00b7 iteration 0 \u00b7 5 files touched\n\n<details><summary>fails on main (without fix)</summary>\n\n```console\nASAN without fix: 24 FAILED\n$ BUN_DEBUG_QUIET_LOGS=1 bun scripts/build.ts --profile=debug --quiet test \"--reporter=junit\" \"--reporter-outfile=/tmp/pr_gate.xml\" test/js/node/http/node-https-agent-checkserveridentity-reuse.test.ts\nbun test v1.4.3 (c6b7fcb5b)\n\ntest/js/node/http/node-https-agent-checkserveridentity-reuse.test.ts:\n195 |         const calls: string[] = [];\n196 |         const first = await exchange({ checkServerIdentity: () => void calls.push(\"permissive\") });\n197 |         const second = await exchange({\n198 |           checkServerIdentity: () => (calls.push(\"rejecting\"), new Error(\"PIN MISMATCH\")),\n199 |         });\n200 |         assert.deepStrictEqual(\n                     ^\nAssertionError: Expected values to be strictly deep-equal:\n+ actual - expected\n\n  {\n    calls: [\n      'permissive',\n-     'rejecting'\n    ],\n    first: {\n      reusedSocket: false,\n      sessionReused: false,\n      status: 200\n    },\n    second: {\n+     reusedSocket: true,\n+     sessionReused: false,\n+     status: 200\n-     error: 'PIN MISMATCH'\n    }\n  }\n\n generatedMessage: true,\n     actual: {\n  first: [Object ...],\n  second: [Object ...],\n  calls: [ \"permissive\" ],\n},\n   expected: {\n  first: [Object ...],\n... (truncated)\n\nrelease without fix: 24 FAILED\nbun test v1.4.3-canary.1 (c6b7fcb5b)\n\ntest/js/node/http/node-https-agent-checkserveridentity-reuse.test.ts:\n195 |         const calls: string[] = [];\n196 |         const first = await exchange({ checkServerIdentity: () => void calls.push(\"permissive\") });\n197 |         const second = await exchange({\n198 |           checkServerIdentity: () => (calls.push(\"rejecting\"), new Error(\"PIN MISMATCH\")),\n199 |         });\n200 |         assert.deepStrictEqual(\n                     ^\nAssertionError: Expected values to be strictly deep-equal:\n+ actual - expected\n\n  {\n    calls: [\n      'permissive',\n-     'rejecting'\n    ],\n    first: {\n      reusedSocket: false,\n      sessionReused: false,\n      status: 200\n    },\n    second: {\n+     reusedSocket: true,\n+     sessionReused: false,\n+     status: 200\n-     error: 'PIN MISMATCH'\n    }\n  }\n\n generatedMessage: true,\n     actual: {\n  first: [Object ...],\n  second: [Object ...],\n  calls: [ \"permissive\" ],\n},\n   expected: {\n  first: [Object ...],\n  second: [Object ...],\n  calls: [ \"permissive\", \"rejecting\" ],\n},\n   operator: \"deepStrictEqual\",\n       diff: \"simple\",\n       code: \"ERR_ASSERTION\"\n\n      at /workspace/bun/test/js/node/ht\n... (truncated)\n```\n\n</details>\n\n<details><summary>passes on PR (with fix)</summary>\n\n```console\nASAN with fix: all passed\n$ BUN_DEBUG_QUIET_LOGS=1 bun scripts/build.ts --profile=debug --quiet test \"--reporter=junit\" \"--reporter-outfile=/tmp/pr_gate.xml\" test/js/node/http/node-https-agent-checkserveridentity-reuse.test.ts\nbun test v1.4.3 (c6b7fcb5b)\n\ntest/js/node/http/node-https-agent-checkserveridentity-reuse.test.ts:\n(pass) https.Agent({ keepAlive: true }) and a request's own checkServerIdentity > a rejecting callback does not ride the pooled socket of a permissive callback [1208.38ms]\n(pass) https.Agent({ keepAlive: true }) and a request's own checkServerIdentity > a rejecting callback does not ride the pooled socket of the default check [306.99ms]\n(pass) https.Agent({ keepAlive: true }) and a request's own checkServerIdentity > the default check does not ride the pooled socket of a permissive callback [285.12ms]\n(pass) https.Agent({ keepAlive: true }) and a request's own checkServerIdentity > http.request({ protocol: \"https:\", agent }) gets the same rule for the pooled socket [349.83ms]\n(pass) https.Agent({ keepAlive: true }) and a request's own checkServerIdentity > every request with its own callback gets its own connection, parked nowhere [257.69ms]\n(pass) https.Agent({ keepAlive\n... (truncated)\n\nrelease with fix: all passed\n$ bun scripts/build.ts --profile=release\n[configured] bun-profile \u2192 bun (stripped)\n  target       linux-x64-gnu\n  build type   Release\n  build dir    ./build/release\n  revision     6481f6ac8f\n  features     lto, baseline\n\n23 deps, 131 codegen, 1176 objects in 713ms\n\nninja: Entering directory `/workspace/bun/build/release'\n[1/1250] install /workspace/bun\nbun install v1.4.3-canary.1 (c6b7fcb5b)\n\nChecked 22 installs across 61 packages (no changes) [11.00ms]\n[2/1250] gen ErrorCode+*.h\n[3/1250] install /workspace/bun/packages/bun-error\nbun install v1.4.3-canary.1 (c6b7fcb5b)\n\nChecked 1 install across 2 packages (no changes) [3.00ms]\n[4/1250] gen bindgenv2\n[5/1250] fetch libjpeg-turbo\n[libjpeg-turbo] up to date\n[6/1223] install /workspace/bun/src/node-fallbacks\nbun install v1.4.3-canary.1 (c6b7fcb5b)\n\nChecked 111 installs across 104 packages (no changes) [7.00ms]\n[7/1223] fetch zlib\n[zlib] up to date\n[8/1223] gen node-fallbacks/react-refresh.js\nBundled 1 module in 10ms\n\n  react-refresh.js  4.81 KB  (entry point)\n\n[9/1223] esbuild bun-error\n\n  ../../build/release/codegen/bun-error/index.js       34.9kb\n  ../../build/release/codegen/bun-error/bun-error.css  12.8kb\n\n\u26a1 Do\n... (truncated)\n```\n\n</details>\n\n<details><summary>diff hotspot</summary>\n\n```\nsrc/js/internal/http.ts                            |   2 +\n src/js/node/_http_agent.ts                         |  63 ++-\n src/js/node/_http_client.ts                        |  20 +-\n src/js/node/https.ts                               |  17 +-\n ...e-https-agent-checkserveridentity-reuse.test.ts | 532 +++++++++++++++++++++\n 5 files changed, 623 insertions(+), 11 deletions(-)\n```\n\n</details>\n\n**gate history** \u00b7 1 passed \u00b7 0 rejected \u00b7 iteration 0\n\n<details><summary>evidence per changed file</summary>\n\n```\nfile                                                      reads  edits  tests\nsrc/js/internal/http.ts                                       0      0     40\nsrc/js/node/_http_agent.ts                                    2      0     42\nsrc/js/node/_http_client.ts                                   0      0     38\nsrc/js/node/https.ts                                          7      8     44\n\u2026http/node-https-agent-checkserveridentity-reuse.test.ts      1      4     37\n```\n\n</details>\n\n<!-- robobun:evidence:end -->"
    },
    "CVE-2026-58040": {
      "dc078c8bc4d52b73c1755ba35f1fe8b0f88d35ac": "node:https: do not reuse a connection across requests that have their own checkServerIdentity (#42946)\n\n### Problem\n- An `https.Agent` lets a request use a connection that a different\nidentity check approved. Request 1 passes a permissive\n`checkServerIdentity`, or none. Request 2 passes a rejecting one and\ngets `200`. Its callback never runs.\n- The cause is `Agent#getName()` (`src/js/node/https.ts:371`). The name\nhas no `checkServerIdentity` part. It keys the keep-alive pool,\n`_sessionCache`, and the request queue. (#36131 since closed the\nresumed-session part.)\n- Node fixed this as CVE-2026-58040 (nodejs/node@52a8ace880).\n\n### Fix\n- Port of 52a8ace880. A request with its own `checkServerIdentity` gets\na unique Agent name and uses no cached session (nor Bun's\n`establishTunnel` cache). An Agent-level callback still pools.\n- Stricter than Node twice. `ClientRequest` marks the request, so\n`http.request({ protocol: \"https:\", agent })` is covered. The Agent's\n`'free'` handler refuses the socket, so agent-base style Agents are\ncovered.\n- The unique name leaves the Agent's bookkeeping intact: no stale\n`agent.sockets` entry, no dead queue head, no wait behind an idle socket\nunder `maxTotalSockets` (Notes).\n- Verified:\n`test/js/node/http/node-https-agent-checkserveridentity-reuse.test.ts`.\nMain b8eacead4d fails 20 of 30. Node v26.5.1 passes the 21 it runs.\n\n### Background\n- `https.Agent` pools sockets in `freeSockets[name]`. `getName(options)`\nbuilds `name` from host, port and TLS options.\n- `_sessionCache` holds one TLS session per name. A new socket offers it\nto skip the full handshake.\n- `checkServerIdentity(hostname, cert)` is the certificate pinning hook.\nIt runs once per full handshake.\n- Cost, as in Node: a full handshake per such request, and `maxSockets`\ndoes not bound them. A callback on the Agent keeps reuse.\n\n<details><summary>Notes</summary>\n\n**Repro.** One Agent, vendored `agent1` certificate. Request 2 carries\n`checkServerIdentity: () => new Error(\"PIN MISMATCH\")`.\n\n```\n                                    bun canary 09bb54630      this branch        node v26.3.0   node v26.5.1\nrequest 1 default,    keepAlive     200 on conn 1, 0 calls    ERR PIN MISMATCH   200            ERR PIN MISMATCH\nrequest 1 default,    no keepAlive  200, session resumed      ERR PIN MISMATCH   200            ERR PIN MISMATCH\nrequest 1 permissive, keepAlive     200 on conn 1             ERR PIN MISMATCH   200            ERR PIN MISMATCH\nrequest 1 permissive, no keepAlive  200, session resumed      ERR PIN MISMATCH   200            ERR PIN MISMATCH\n```\n\nThe reverse direction and the queue have the same hole on stock bun. A\ndefault-check request for `servername: \"not-agent1\"` gets `200` after a\npermissive callback approved that name. With `maxSockets: 1`,\n`Agent#removeSocket` makes the socket of a queued request from the\noptions of the socket that closed, so the queued request runs the\ncallback of the request ahead of it.\n\n**The test file under each runtime.**\n\n| runtime | pass | fail | skipped |\n|---|---|---|---|\n| this branch (debug build) | 30 | 0 | 0 |\n| main b8eacead4d (has #36131), without this PR | 10 | 20 | 0 |\n| bun canary c6b7fcb5b (before #36131) | 6 | 24 | 0 |\n| node v26.5.1 (has 52a8ace880) | 21 | 0 | 8 (Bun-only) |\n| node v26.3.0 (does not) | 5 | 0 | 24 |\n\nThe cases that pass everywhere are controls: an Agent-level callback and\na request that passes `tls.checkServerIdentity` itself still share the\nsocket or the session, and `agent.sockets` has its entry while a proxy\ntunnel connects. The other cases are gated on the Node versions that\ncarry the fix (v22.23.2, v24.18.1, v26.5.1). Bun always runs them.\n\n**Difference 1: where the socket is refused.** Node marks the socket in\n`https.Agent`'s `createConnection` and refuses it in\n`https.Agent#keepSocketAlive`. An `https.Agent` subclass that replaces\n`createConnection`, and an `http.Agent` that borrows\n`https.Agent#getName` (agent-base: https-proxy-agent,\nsocks-proxy-agent), get the unique name without the mark. Three requests\nwith their own callback on a `keepAlive` Agent:\n\n```\n                                              parked sockets after\n                                              stock   straight port   node v26.5.1   this branch\nhttps.Agent                                   1       0               0              0\nhttp.Agent that borrows https getName         1       3               3              0\nhttps.Agent that replaces createConnection    1       3               3              0\n```\n\nA parked socket under a unique name is never taken. It counts against\n`maxTotalSockets` until the origin closes it. The request options are\nwhat make the name unique, and `installListeners()` already hands them\nto the `'free'` handler, so the handler reads the mark there. The socket\nmark and the `keepSocketAlive` override of the upstream patch are then\nnot needed.\n\n**Difference 2: where the mark is set.** Node sets it in\n`https.request()`. `http.request({ protocol: \"https:\", agent })` and\n`new http.ClientRequest()` reach the same Agent without it, and still\nshare on Node v26.5.1. Here the `ClientRequest` constructor sets it,\nafter it resolves the agent, so no route skips it.\n\n**Difference 3: what a failed socket creation leaves behind.**\n`Agent#addRequest` makes `this.sockets[name] = []` before a socket\nexists, in Node and here. If no socket arrives (a refused proxy tunnel,\na `createConnection` that throws) nothing in Node removes the entry.\nWith one name per Agent that was one stale entry. With one name per\nrequest it is one per failed request, and each key holds the PEM text of\n`ca`, `cert` and `key`. Five refused tunnels: stock 1 entry, straight\nport 5, node v26.5.1 5, this branch 0. The branch removes the empty\nentry in the error path of both creation callbacks and around a\n`createSocket()` that throws. A queued request whose socket could not be\nmade also leaves its queue. In Node it stays at the head until a socket\nof its name frees. No socket ever has the name of such a request, and\n`removeSocket()` only looks at the first queue, so every queue behind it\nstopped for good. The same path now gives a `createConnection()` that\nthrows to the queued request. Before, and in Node, that is an uncaught\nexception from a `'close'` handler.\n\n**Difference 4: an idle socket gives up its slot.** `new https.Agent({\nkeepAlive: true, maxTotalSockets: 1 })`, one finished request, then a\nrequest with its own callback. The pooled socket holds the only slot and\nthe new request cannot take it. Node v26.5.1 waits until the server\ncloses the idle socket. Stock bun reused the socket at once. Here\n`addRequest()` destroys one idle socket when such a request has to\nqueue. No other request does this, so the rest of the scheduling is\nNode's.\n\n**The tunnel path.** `establishTunnel()` in Bun attaches the session\ncache listeners to the tunneled socket. Upstream caches no session\nthere. Through an `https:` proxy the cached session is resumed, and on\nstock bun request 2 gets `200` without its callback. Through an `http:`\nproxy the session is cached and not resumed today (a bug in\n`tls.connect({ socket, session })` that is tracked apart from this PR).\nThe branch caches no session for a marked request on either path.\n\n**Costs in full.**\n- `maxSockets` is a limit per name, so it does not bound these requests:\n`maxSockets: 1` and 20 concurrent requests open 20 connections here and\non node v26.5.1, 1 on stock. `maxTotalSockets` still bounds them, to N+1\nin one case that Node's Agent has for every pair of names: the `'free'`\nhandler pools a socket at the limit, and `removeSocket()` then makes a\nsocket for a queued request of another name.\n- No Bun user reported this hole. The report that exists, #40308, asked\nfor more reuse with a callback on `fetch`, and #42692 (item B4) later\ngave that reuse up for the same reason as this PR.\n\n**Why Node's own regression test is not added.**\n`test-https-agent-checkserveridentity-reuse.js` ends with\n`second.socket.isSessionReused()` one promise hop after the response\n`'end'` of a `Connection: close` request. Bun has destroyed that socket\nby then (`Socket.prototype._final` ends in a `nextTick`, Node waits for\nthe shutdown callback), so the call reads `false`. The session is\nresumed: the same call at `'response'` reads `true`. Files under\n`test/js/node/test/parallel/` are not edited, so the file stays out. The\nnew test covers each of its cases, and it runs under Node.\n\n**Other open PRs.**\n- #36131 merged while this PR was open. It runs the identity check on\nresumed sessions too, so on main the four plain `keepAlive: false` cases\nof the new test already pass. The pooled socket, the queue, both proxy\npaths and the bookkeeping cases still fail there (20 of 30). The session\nrule of this PR stays: it keeps one cache entry per request out of the\n100-entry `_sessionCache`, and it matches the upstream patch. The new\ntest passes 30 of 30 on this branch rebased over #36131.\n- #42498 appends to the same tail of `getName()`. The second one to land\nhas a small text conflict. Both suffixes must stay, the per-request one\nlast.\n\n**Suites.** 182 files of\n`test/js/node/test/{parallel,sequential}/test-http*` that mention\nagents, sockets or keep-alive, `test-tls-check-server-identity.js`,\n`test-tls-client-resume*.js`, all of `node-http.test.ts` (159 pass),\n`node-https-checkServerIdentity.test.ts`,\n`node-http-agent-free-socket.test.ts`,\n`node-http-agent-tls-options.test.mts`, `node-http-proxy-url.test.ts`.\nTwo vendored files fail on the debug build with and without this change,\nand pass on the release canary: `test-https-timeout.js` (a 10 ms request\ntimeout never fires) and `test-http-agent-keepalive.js` (an assertion\nbehind a 1 ms timer).\n\n**Review history.** A review of the first version (the straight port)\nasked for four changes: no stale `agent.sockets` entries, a test through\nan `https:` proxy, the full cost statement, and its execution findings\n(the parked sockets above, the queue cases). The review on the PR then\nfound the `http.request()` route, the wait behind an idle socket, and\nthe dead queue head, and asked to keep Node's `agent.sockets` entry. All\nare in this version. The `maxSockets` cost and the N+1 case are not\nchanged. It also named two unrelated Node security fixes that Bun lacks\n(CVE-2026-48615, CVE-2026-48618). They are tracked apart from this PR.\n\n</details>\n\n<!-- robobun:evidence:begin -->\n\n---\n\n**[human-review]** gate passed \u00b7 iteration 0 \u00b7 5 files touched\n\n<details><summary>fails on main (without fix)</summary>\n\n```console\nASAN without fix: 24 FAILED\n$ BUN_DEBUG_QUIET_LOGS=1 bun scripts/build.ts --profile=debug --quiet test \"--reporter=junit\" \"--reporter-outfile=/tmp/pr_gate.xml\" test/js/node/http/node-https-agent-checkserveridentity-reuse.test.ts\nbun test v1.4.3 (c6b7fcb5b)\n\ntest/js/node/http/node-https-agent-checkserveridentity-reuse.test.ts:\n195 |         const calls: string[] = [];\n196 |         const first = await exchange({ checkServerIdentity: () => void calls.push(\"permissive\") });\n197 |         const second = await exchange({\n198 |           checkServerIdentity: () => (calls.push(\"rejecting\"), new Error(\"PIN MISMATCH\")),\n199 |         });\n200 |         assert.deepStrictEqual(\n                     ^\nAssertionError: Expected values to be strictly deep-equal:\n+ actual - expected\n\n  {\n    calls: [\n      'permissive',\n-     'rejecting'\n    ],\n    first: {\n      reusedSocket: false,\n      sessionReused: false,\n      status: 200\n    },\n    second: {\n+     reusedSocket: true,\n+     sessionReused: false,\n+     status: 200\n-     error: 'PIN MISMATCH'\n    }\n  }\n\n generatedMessage: true,\n     actual: {\n  first: [Object ...],\n  second: [Object ...],\n  calls: [ \"permissive\" ],\n},\n   expected: {\n  first: [Object ...],\n... (truncated)\n\nrelease without fix: 24 FAILED\nbun test v1.4.3-canary.1 (c6b7fcb5b)\n\ntest/js/node/http/node-https-agent-checkserveridentity-reuse.test.ts:\n195 |         const calls: string[] = [];\n196 |         const first = await exchange({ checkServerIdentity: () => void calls.push(\"permissive\") });\n197 |         const second = await exchange({\n198 |           checkServerIdentity: () => (calls.push(\"rejecting\"), new Error(\"PIN MISMATCH\")),\n199 |         });\n200 |         assert.deepStrictEqual(\n                     ^\nAssertionError: Expected values to be strictly deep-equal:\n+ actual - expected\n\n  {\n    calls: [\n      'permissive',\n-     'rejecting'\n    ],\n    first: {\n      reusedSocket: false,\n      sessionReused: false,\n      status: 200\n    },\n    second: {\n+     reusedSocket: true,\n+     sessionReused: false,\n+     status: 200\n-     error: 'PIN MISMATCH'\n    }\n  }\n\n generatedMessage: true,\n     actual: {\n  first: [Object ...],\n  second: [Object ...],\n  calls: [ \"permissive\" ],\n},\n   expected: {\n  first: [Object ...],\n  second: [Object ...],\n  calls: [ \"permissive\", \"rejecting\" ],\n},\n   operator: \"deepStrictEqual\",\n       diff: \"simple\",\n       code: \"ERR_ASSERTION\"\n\n      at /workspace/bun/test/js/node/ht\n... (truncated)\n```\n\n</details>\n\n<details><summary>passes on PR (with fix)</summary>\n\n```console\nASAN with fix: all passed\n$ BUN_DEBUG_QUIET_LOGS=1 bun scripts/build.ts --profile=debug --quiet test \"--reporter=junit\" \"--reporter-outfile=/tmp/pr_gate.xml\" test/js/node/http/node-https-agent-checkserveridentity-reuse.test.ts\nbun test v1.4.3 (c6b7fcb5b)\n\ntest/js/node/http/node-https-agent-checkserveridentity-reuse.test.ts:\n(pass) https.Agent({ keepAlive: true }) and a request's own checkServerIdentity > a rejecting callback does not ride the pooled socket of a permissive callback [1208.38ms]\n(pass) https.Agent({ keepAlive: true }) and a request's own checkServerIdentity > a rejecting callback does not ride the pooled socket of the default check [306.99ms]\n(pass) https.Agent({ keepAlive: true }) and a request's own checkServerIdentity > the default check does not ride the pooled socket of a permissive callback [285.12ms]\n(pass) https.Agent({ keepAlive: true }) and a request's own checkServerIdentity > http.request({ protocol: \"https:\", agent }) gets the same rule for the pooled socket [349.83ms]\n(pass) https.Agent({ keepAlive: true }) and a request's own checkServerIdentity > every request with its own callback gets its own connection, parked nowhere [257.69ms]\n(pass) https.Agent({ keepAlive\n... (truncated)\n\nrelease with fix: all passed\n$ bun scripts/build.ts --profile=release\n[configured] bun-profile \u2192 bun (stripped)\n  target       linux-x64-gnu\n  build type   Release\n  build dir    ./build/release\n  revision     6481f6ac8f\n  features     lto, baseline\n\n23 deps, 131 codegen, 1176 objects in 713ms\n\nninja: Entering directory `/workspace/bun/build/release'\n[1/1250] install /workspace/bun\nbun install v1.4.3-canary.1 (c6b7fcb5b)\n\nChecked 22 installs across 61 packages (no changes) [11.00ms]\n[2/1250] gen ErrorCode+*.h\n[3/1250] install /workspace/bun/packages/bun-error\nbun install v1.4.3-canary.1 (c6b7fcb5b)\n\nChecked 1 install across 2 packages (no changes) [3.00ms]\n[4/1250] gen bindgenv2\n[5/1250] fetch libjpeg-turbo\n[libjpeg-turbo] up to date\n[6/1223] install /workspace/bun/src/node-fallbacks\nbun install v1.4.3-canary.1 (c6b7fcb5b)\n\nChecked 111 installs across 104 packages (no changes) [7.00ms]\n[7/1223] fetch zlib\n[zlib] up to date\n[8/1223] gen node-fallbacks/react-refresh.js\nBundled 1 module in 10ms\n\n  react-refresh.js  4.81 KB  (entry point)\n\n[9/1223] esbuild bun-error\n\n  ../../build/release/codegen/bun-error/index.js       34.9kb\n  ../../build/release/codegen/bun-error/bun-error.css  12.8kb\n\n\u26a1 Do\n... (truncated)\n```\n\n</details>\n\n<details><summary>diff hotspot</summary>\n\n```\nsrc/js/internal/http.ts                            |   2 +\n src/js/node/_http_agent.ts                         |  63 ++-\n src/js/node/_http_client.ts                        |  20 +-\n src/js/node/https.ts                               |  17 +-\n ...e-https-agent-checkserveridentity-reuse.test.ts | 532 +++++++++++++++++++++\n 5 files changed, 623 insertions(+), 11 deletions(-)\n```\n\n</details>\n\n**gate history** \u00b7 1 passed \u00b7 0 rejected \u00b7 iteration 0\n\n<details><summary>evidence per changed file</summary>\n\n```\nfile                                                      reads  edits  tests\nsrc/js/internal/http.ts                                       0      0     40\nsrc/js/node/_http_agent.ts                                    2      0     42\nsrc/js/node/_http_client.ts                                   0      0     38\nsrc/js/node/https.ts                                          7      8     44\n\u2026http/node-https-agent-checkserveridentity-reuse.test.ts      1      4     37\n```\n\n</details>\n\n<!-- robobun:evidence:end -->"
    },
    "CVE-2026-48618": {
      "dc078c8bc4d52b73c1755ba35f1fe8b0f88d35ac": "node:https: do not reuse a connection across requests that have their own checkServerIdentity (#42946)\n\n### Problem\n- An `https.Agent` lets a request use a connection that a different\nidentity check approved. Request 1 passes a permissive\n`checkServerIdentity`, or none. Request 2 passes a rejecting one and\ngets `200`. Its callback never runs.\n- The cause is `Agent#getName()` (`src/js/node/https.ts:371`). The name\nhas no `checkServerIdentity` part. It keys the keep-alive pool,\n`_sessionCache`, and the request queue. (#36131 since closed the\nresumed-session part.)\n- Node fixed this as CVE-2026-58040 (nodejs/node@52a8ace880).\n\n### Fix\n- Port of 52a8ace880. A request with its own `checkServerIdentity` gets\na unique Agent name and uses no cached session (nor Bun's\n`establishTunnel` cache). An Agent-level callback still pools.\n- Stricter than Node twice. `ClientRequest` marks the request, so\n`http.request({ protocol: \"https:\", agent })` is covered. The Agent's\n`'free'` handler refuses the socket, so agent-base style Agents are\ncovered.\n- The unique name leaves the Agent's bookkeeping intact: no stale\n`agent.sockets` entry, no dead queue head, no wait behind an idle socket\nunder `maxTotalSockets` (Notes).\n- Verified:\n`test/js/node/http/node-https-agent-checkserveridentity-reuse.test.ts`.\nMain b8eacead4d fails 20 of 30. Node v26.5.1 passes the 21 it runs.\n\n### Background\n- `https.Agent` pools sockets in `freeSockets[name]`. `getName(options)`\nbuilds `name` from host, port and TLS options.\n- `_sessionCache` holds one TLS session per name. A new socket offers it\nto skip the full handshake.\n- `checkServerIdentity(hostname, cert)` is the certificate pinning hook.\nIt runs once per full handshake.\n- Cost, as in Node: a full handshake per such request, and `maxSockets`\ndoes not bound them. A callback on the Agent keeps reuse.\n\n<details><summary>Notes</summary>\n\n**Repro.** One Agent, vendored `agent1` certificate. Request 2 carries\n`checkServerIdentity: () => new Error(\"PIN MISMATCH\")`.\n\n```\n                                    bun canary 09bb54630      this branch        node v26.3.0   node v26.5.1\nrequest 1 default,    keepAlive     200 on conn 1, 0 calls    ERR PIN MISMATCH   200            ERR PIN MISMATCH\nrequest 1 default,    no keepAlive  200, session resumed      ERR PIN MISMATCH   200            ERR PIN MISMATCH\nrequest 1 permissive, keepAlive     200 on conn 1             ERR PIN MISMATCH   200            ERR PIN MISMATCH\nrequest 1 permissive, no keepAlive  200, session resumed      ERR PIN MISMATCH   200            ERR PIN MISMATCH\n```\n\nThe reverse direction and the queue have the same hole on stock bun. A\ndefault-check request for `servername: \"not-agent1\"` gets `200` after a\npermissive callback approved that name. With `maxSockets: 1`,\n`Agent#removeSocket` makes the socket of a queued request from the\noptions of the socket that closed, so the queued request runs the\ncallback of the request ahead of it.\n\n**The test file under each runtime.**\n\n| runtime | pass | fail | skipped |\n|---|---|---|---|\n| this branch (debug build) | 30 | 0 | 0 |\n| main b8eacead4d (has #36131), without this PR | 10 | 20 | 0 |\n| bun canary c6b7fcb5b (before #36131) | 6 | 24 | 0 |\n| node v26.5.1 (has 52a8ace880) | 21 | 0 | 8 (Bun-only) |\n| node v26.3.0 (does not) | 5 | 0 | 24 |\n\nThe cases that pass everywhere are controls: an Agent-level callback and\na request that passes `tls.checkServerIdentity` itself still share the\nsocket or the session, and `agent.sockets` has its entry while a proxy\ntunnel connects. The other cases are gated on the Node versions that\ncarry the fix (v22.23.2, v24.18.1, v26.5.1). Bun always runs them.\n\n**Difference 1: where the socket is refused.** Node marks the socket in\n`https.Agent`'s `createConnection` and refuses it in\n`https.Agent#keepSocketAlive`. An `https.Agent` subclass that replaces\n`createConnection`, and an `http.Agent` that borrows\n`https.Agent#getName` (agent-base: https-proxy-agent,\nsocks-proxy-agent), get the unique name without the mark. Three requests\nwith their own callback on a `keepAlive` Agent:\n\n```\n                                              parked sockets after\n                                              stock   straight port   node v26.5.1   this branch\nhttps.Agent                                   1       0               0              0\nhttp.Agent that borrows https getName         1       3               3              0\nhttps.Agent that replaces createConnection    1       3               3              0\n```\n\nA parked socket under a unique name is never taken. It counts against\n`maxTotalSockets` until the origin closes it. The request options are\nwhat make the name unique, and `installListeners()` already hands them\nto the `'free'` handler, so the handler reads the mark there. The socket\nmark and the `keepSocketAlive` override of the upstream patch are then\nnot needed.\n\n**Difference 2: where the mark is set.** Node sets it in\n`https.request()`. `http.request({ protocol: \"https:\", agent })` and\n`new http.ClientRequest()` reach the same Agent without it, and still\nshare on Node v26.5.1. Here the `ClientRequest` constructor sets it,\nafter it resolves the agent, so no route skips it.\n\n**Difference 3: what a failed socket creation leaves behind.**\n`Agent#addRequest` makes `this.sockets[name] = []` before a socket\nexists, in Node and here. If no socket arrives (a refused proxy tunnel,\na `createConnection` that throws) nothing in Node removes the entry.\nWith one name per Agent that was one stale entry. With one name per\nrequest it is one per failed request, and each key holds the PEM text of\n`ca`, `cert` and `key`. Five refused tunnels: stock 1 entry, straight\nport 5, node v26.5.1 5, this branch 0. The branch removes the empty\nentry in the error path of both creation callbacks and around a\n`createSocket()` that throws. A queued request whose socket could not be\nmade also leaves its queue. In Node it stays at the head until a socket\nof its name frees. No socket ever has the name of such a request, and\n`removeSocket()` only looks at the first queue, so every queue behind it\nstopped for good. The same path now gives a `createConnection()` that\nthrows to the queued request. Before, and in Node, that is an uncaught\nexception from a `'close'` handler.\n\n**Difference 4: an idle socket gives up its slot.** `new https.Agent({\nkeepAlive: true, maxTotalSockets: 1 })`, one finished request, then a\nrequest with its own callback. The pooled socket holds the only slot and\nthe new request cannot take it. Node v26.5.1 waits until the server\ncloses the idle socket. Stock bun reused the socket at once. Here\n`addRequest()` destroys one idle socket when such a request has to\nqueue. No other request does this, so the rest of the scheduling is\nNode's.\n\n**The tunnel path.** `establishTunnel()` in Bun attaches the session\ncache listeners to the tunneled socket. Upstream caches no session\nthere. Through an `https:` proxy the cached session is resumed, and on\nstock bun request 2 gets `200` without its callback. Through an `http:`\nproxy the session is cached and not resumed today (a bug in\n`tls.connect({ socket, session })` that is tracked apart from this PR).\nThe branch caches no session for a marked request on either path.\n\n**Costs in full.**\n- `maxSockets` is a limit per name, so it does not bound these requests:\n`maxSockets: 1` and 20 concurrent requests open 20 connections here and\non node v26.5.1, 1 on stock. `maxTotalSockets` still bounds them, to N+1\nin one case that Node's Agent has for every pair of names: the `'free'`\nhandler pools a socket at the limit, and `removeSocket()` then makes a\nsocket for a queued request of another name.\n- No Bun user reported this hole. The report that exists, #40308, asked\nfor more reuse with a callback on `fetch`, and #42692 (item B4) later\ngave that reuse up for the same reason as this PR.\n\n**Why Node's own regression test is not added.**\n`test-https-agent-checkserveridentity-reuse.js` ends with\n`second.socket.isSessionReused()` one promise hop after the response\n`'end'` of a `Connection: close` request. Bun has destroyed that socket\nby then (`Socket.prototype._final` ends in a `nextTick`, Node waits for\nthe shutdown callback), so the call reads `false`. The session is\nresumed: the same call at `'response'` reads `true`. Files under\n`test/js/node/test/parallel/` are not edited, so the file stays out. The\nnew test covers each of its cases, and it runs under Node.\n\n**Other open PRs.**\n- #36131 merged while this PR was open. It runs the identity check on\nresumed sessions too, so on main the four plain `keepAlive: false` cases\nof the new test already pass. The pooled socket, the queue, both proxy\npaths and the bookkeeping cases still fail there (20 of 30). The session\nrule of this PR stays: it keeps one cache entry per request out of the\n100-entry `_sessionCache`, and it matches the upstream patch. The new\ntest passes 30 of 30 on this branch rebased over #36131.\n- #42498 appends to the same tail of `getName()`. The second one to land\nhas a small text conflict. Both suffixes must stay, the per-request one\nlast.\n\n**Suites.** 182 files of\n`test/js/node/test/{parallel,sequential}/test-http*` that mention\nagents, sockets or keep-alive, `test-tls-check-server-identity.js`,\n`test-tls-client-resume*.js`, all of `node-http.test.ts` (159 pass),\n`node-https-checkServerIdentity.test.ts`,\n`node-http-agent-free-socket.test.ts`,\n`node-http-agent-tls-options.test.mts`, `node-http-proxy-url.test.ts`.\nTwo vendored files fail on the debug build with and without this change,\nand pass on the release canary: `test-https-timeout.js` (a 10 ms request\ntimeout never fires) and `test-http-agent-keepalive.js` (an assertion\nbehind a 1 ms timer).\n\n**Review history.** A review of the first version (the straight port)\nasked for four changes: no stale `agent.sockets` entries, a test through\nan `https:` proxy, the full cost statement, and its execution findings\n(the parked sockets above, the queue cases). The review on the PR then\nfound the `http.request()` route, the wait behind an idle socket, and\nthe dead queue head, and asked to keep Node's `agent.sockets` entry. All\nare in this version. The `maxSockets` cost and the N+1 case are not\nchanged. It also named two unrelated Node security fixes that Bun lacks\n(CVE-2026-48615, CVE-2026-48618). They are tracked apart from this PR.\n\n</details>\n\n<!-- robobun:evidence:begin -->\n\n---\n\n**[human-review]** gate passed \u00b7 iteration 0 \u00b7 5 files touched\n\n<details><summary>fails on main (without fix)</summary>\n\n```console\nASAN without fix: 24 FAILED\n$ BUN_DEBUG_QUIET_LOGS=1 bun scripts/build.ts --profile=debug --quiet test \"--reporter=junit\" \"--reporter-outfile=/tmp/pr_gate.xml\" test/js/node/http/node-https-agent-checkserveridentity-reuse.test.ts\nbun test v1.4.3 (c6b7fcb5b)\n\ntest/js/node/http/node-https-agent-checkserveridentity-reuse.test.ts:\n195 |         const calls: string[] = [];\n196 |         const first = await exchange({ checkServerIdentity: () => void calls.push(\"permissive\") });\n197 |         const second = await exchange({\n198 |           checkServerIdentity: () => (calls.push(\"rejecting\"), new Error(\"PIN MISMATCH\")),\n199 |         });\n200 |         assert.deepStrictEqual(\n                     ^\nAssertionError: Expected values to be strictly deep-equal:\n+ actual - expected\n\n  {\n    calls: [\n      'permissive',\n-     'rejecting'\n    ],\n    first: {\n      reusedSocket: false,\n      sessionReused: false,\n      status: 200\n    },\n    second: {\n+     reusedSocket: true,\n+     sessionReused: false,\n+     status: 200\n-     error: 'PIN MISMATCH'\n    }\n  }\n\n generatedMessage: true,\n     actual: {\n  first: [Object ...],\n  second: [Object ...],\n  calls: [ \"permissive\" ],\n},\n   expected: {\n  first: [Object ...],\n... (truncated)\n\nrelease without fix: 24 FAILED\nbun test v1.4.3-canary.1 (c6b7fcb5b)\n\ntest/js/node/http/node-https-agent-checkserveridentity-reuse.test.ts:\n195 |         const calls: string[] = [];\n196 |         const first = await exchange({ checkServerIdentity: () => void calls.push(\"permissive\") });\n197 |         const second = await exchange({\n198 |           checkServerIdentity: () => (calls.push(\"rejecting\"), new Error(\"PIN MISMATCH\")),\n199 |         });\n200 |         assert.deepStrictEqual(\n                     ^\nAssertionError: Expected values to be strictly deep-equal:\n+ actual - expected\n\n  {\n    calls: [\n      'permissive',\n-     'rejecting'\n    ],\n    first: {\n      reusedSocket: false,\n      sessionReused: false,\n      status: 200\n    },\n    second: {\n+     reusedSocket: true,\n+     sessionReused: false,\n+     status: 200\n-     error: 'PIN MISMATCH'\n    }\n  }\n\n generatedMessage: true,\n     actual: {\n  first: [Object ...],\n  second: [Object ...],\n  calls: [ \"permissive\" ],\n},\n   expected: {\n  first: [Object ...],\n  second: [Object ...],\n  calls: [ \"permissive\", \"rejecting\" ],\n},\n   operator: \"deepStrictEqual\",\n       diff: \"simple\",\n       code: \"ERR_ASSERTION\"\n\n      at /workspace/bun/test/js/node/ht\n... (truncated)\n```\n\n</details>\n\n<details><summary>passes on PR (with fix)</summary>\n\n```console\nASAN with fix: all passed\n$ BUN_DEBUG_QUIET_LOGS=1 bun scripts/build.ts --profile=debug --quiet test \"--reporter=junit\" \"--reporter-outfile=/tmp/pr_gate.xml\" test/js/node/http/node-https-agent-checkserveridentity-reuse.test.ts\nbun test v1.4.3 (c6b7fcb5b)\n\ntest/js/node/http/node-https-agent-checkserveridentity-reuse.test.ts:\n(pass) https.Agent({ keepAlive: true }) and a request's own checkServerIdentity > a rejecting callback does not ride the pooled socket of a permissive callback [1208.38ms]\n(pass) https.Agent({ keepAlive: true }) and a request's own checkServerIdentity > a rejecting callback does not ride the pooled socket of the default check [306.99ms]\n(pass) https.Agent({ keepAlive: true }) and a request's own checkServerIdentity > the default check does not ride the pooled socket of a permissive callback [285.12ms]\n(pass) https.Agent({ keepAlive: true }) and a request's own checkServerIdentity > http.request({ protocol: \"https:\", agent }) gets the same rule for the pooled socket [349.83ms]\n(pass) https.Agent({ keepAlive: true }) and a request's own checkServerIdentity > every request with its own callback gets its own connection, parked nowhere [257.69ms]\n(pass) https.Agent({ keepAlive\n... (truncated)\n\nrelease with fix: all passed\n$ bun scripts/build.ts --profile=release\n[configured] bun-profile \u2192 bun (stripped)\n  target       linux-x64-gnu\n  build type   Release\n  build dir    ./build/release\n  revision     6481f6ac8f\n  features     lto, baseline\n\n23 deps, 131 codegen, 1176 objects in 713ms\n\nninja: Entering directory `/workspace/bun/build/release'\n[1/1250] install /workspace/bun\nbun install v1.4.3-canary.1 (c6b7fcb5b)\n\nChecked 22 installs across 61 packages (no changes) [11.00ms]\n[2/1250] gen ErrorCode+*.h\n[3/1250] install /workspace/bun/packages/bun-error\nbun install v1.4.3-canary.1 (c6b7fcb5b)\n\nChecked 1 install across 2 packages (no changes) [3.00ms]\n[4/1250] gen bindgenv2\n[5/1250] fetch libjpeg-turbo\n[libjpeg-turbo] up to date\n[6/1223] install /workspace/bun/src/node-fallbacks\nbun install v1.4.3-canary.1 (c6b7fcb5b)\n\nChecked 111 installs across 104 packages (no changes) [7.00ms]\n[7/1223] fetch zlib\n[zlib] up to date\n[8/1223] gen node-fallbacks/react-refresh.js\nBundled 1 module in 10ms\n\n  react-refresh.js  4.81 KB  (entry point)\n\n[9/1223] esbuild bun-error\n\n  ../../build/release/codegen/bun-error/index.js       34.9kb\n  ../../build/release/codegen/bun-error/bun-error.css  12.8kb\n\n\u26a1 Do\n... (truncated)\n```\n\n</details>\n\n<details><summary>diff hotspot</summary>\n\n```\nsrc/js/internal/http.ts                            |   2 +\n src/js/node/_http_agent.ts                         |  63 ++-\n src/js/node/_http_client.ts                        |  20 +-\n src/js/node/https.ts                               |  17 +-\n ...e-https-agent-checkserveridentity-reuse.test.ts | 532 +++++++++++++++++++++\n 5 files changed, 623 insertions(+), 11 deletions(-)\n```\n\n</details>\n\n**gate history** \u00b7 1 passed \u00b7 0 rejected \u00b7 iteration 0\n\n<details><summary>evidence per changed file</summary>\n\n```\nfile                                                      reads  edits  tests\nsrc/js/internal/http.ts                                       0      0     40\nsrc/js/node/_http_agent.ts                                    2      0     42\nsrc/js/node/_http_client.ts                                   0      0     38\nsrc/js/node/https.ts                                          7      8     44\n\u2026http/node-https-agent-checkserveridentity-reuse.test.ts      1      4     37\n```\n\n</details>\n\n<!-- robobun:evidence:end -->",
      "8705d893b51decdf4bdce6c52b51ef65faabe279": "tls: normalize the hostname for server identity checks (#42977)\n\n### Problem\n- `tls.checkServerIdentity(\"foo\u3002bar.example.com\", cert)` accepts a\ncertificate for `*.example.com`. IDNA maps U+3002, U+FF0E and U+FF61 to\n\".\", so a resolver looks up `foo.bar.example.com`, two labels deep. Node\nfixed this as CVE-2026-48618.\n- The cause: a label split on ASCII \".\" only, in `splitHost`\n(`src/js/node/tls.ts:352`) and the native `match_hostname`\n(`src/boringssl/lib.rs:302`). Canary accepts it in `tls.connect`,\n`https.request`, `fetch` (`tls.serverName`) and `Bun.connect`.\n- Only a raw `host` or `servername` string reaches it. A URL host is\nalready ASCII.\n\n### Fix\n- Port nodejs/node 1efb4ff51a and its follow-up 1d87a24050:\n`checkServerIdentity` matches DNS names on\n`url.domainToASCII(hostname)`. IP literals and the error text keep the\nhostname as typed, because `domainToASCII(\"::1\")` is `\"\"`.\n`domainToASCII` moves to `internal/url`, as in Node.\n- `check_x509_server_identity` runs the same conversion when the host\nhas a non-ASCII byte. A host that does not convert matches nothing.\nASCII hosts are unchanged.\n- One deliberate difference from Node: `*` does not match an empty\nlabel. The conversion turns `\u3002example.com` into `.example.com`, which\nNode accepts for `*.example.com`.\n- Verified: `test/js/web/fetch/fetch.tls.wildcard.test.ts` (23 of 100\nfail on canary) and the vendored `test-tls-check-server-identity.js`,\nsynced to Node v26.8.2. Self-reviewed: 3 concerns raised, 1 addressed, 2\nkept with the reason in Notes.\n\n### Background\n- `checkServerIdentity` compares the host with each DNS name in the\ncertificate, label by label. A left-most `*` stands for exactly one\nlabel.\n- IDNA (UTS #46) maps a Unicode domain name to ASCII.\n`url.domainToASCII` applies it.\n- Bun has two matchers with Node's rules: the JS one in `node:tls`, and\na Rust port that every native TLS client calls.\n\n<details><summary>Notes</summary>\n\n**Repro**\n\n```js\nconst tls = require(\"tls\");\nconst err = tls.checkServerIdentity(\"foo\\u3002bar.example.com\", { subjectaltname: \"DNS:*.example.com\", subject: {} });\nconsole.log(err ? \"REJECTED: \" + err.reason : \"ACCEPTED\");\n```\n\n| runtime | output |\n| --- | --- |\n| Node v26.3.0 | `ACCEPTED` |\n| Bun canary 1.4.3 (09bb54630, c6b7fcb5b) | `ACCEPTED` |\n| Node v26.6.0, v26.8.2 | `REJECTED: Host: foo\u3002bar.example.com. is not\nin the cert's altnames: DNS:*.example.com` |\n| this branch | same as Node v26.8.2 |\n\nThe CVE fix shipped in Node v26.5.0. That release stopped matching IPv6\nhosts against IP SANs (nodejs/node#64144). v26.6.0 fixed it with\n1d87a24050. This PR ports both, so `::1` keeps matching `IP\nAddress:0:0:0:0:0:0:0:1`.\n\n**End to end, server certificate `DNS:*.example.com`, client dials\n127.0.0.1**\n\n| entry point | `foo\u3002bar.example.com` on canary | on this branch |\n| --- | --- | --- |\n| `tls.connect({ servername })` | authorized |\n`ERR_TLS_CERT_ALTNAME_INVALID` |\n| `https.request({ servername })` | 200 | `ERR_TLS_CERT_ALTNAME_INVALID`\n|\n| `fetch(url, { tls: { serverName } })` | 200 |\n`ERR_TLS_CERT_ALTNAME_INVALID` |\n| `Bun.connect({ tls: { serverName } })` | authorized |\n`ERR_TLS_CERT_ALTNAME_INVALID` |\n\nU+FF0E and U+FF61 give the same results. `bar.example.com` and\n`b\u00fccher.example.com` stay accepted on all four.\n\n**Comparison with Node on a 28-row table** (full stops in SAN and CN\npaths, trailing ideographic full stop, U-labels, IPv4 and IPv6 hosts,\nhosts that `domainToASCII` rejects). This branch gives the same verdict,\n`reason` and `host` as Node v26.6.0 on all 28 rows, and as Node v26.8.2\non 27. The other row is `xn--a.example.com` against\n`DNS:xn--a.example.com`: Node v26.7.0 moved to ada 4.0.0, where\n`domainToASCII` no longer rejects an invalid punycode label. Bun's\n`url.domainToASCII` still returns `\"\"` for it, as ada 3.4.4 does, so the\nhost matches nothing. That difference belongs to `node:url`.\n\n**Empty first label**\n\n`domainToASCII(\"\u3002example.com\")` is `.example.com`. Node's `check()` lets\n`*` match the empty first label, so Node accepts that host for\n`*.example.com`:\n\n| host against `DNS:*.example.com` | Node v26.3.0 | Node v26.8.2 | Bun\ncanary | this branch |\n| --- | --- | --- | --- | --- |\n| `.example.com` | accepted | accepted | accepted | rejected |\n| `\u3002example.com` | rejected | accepted | rejected | rejected |\n\nA plain port would make Bun newly accept `\u3002example.com`. A name with an\nempty label is not a name below `*.example.com` (OpenSSL's\n`wildcard_match` also needs one character), and glibc does not resolve\n`.localhost`. So both matchers reject an empty first label. This is the\none place where `check()` differs from Node, and the tests mark the\nrows.\n\n**Reach**\n- On Linux, Bun's resolver returns `ENOTFOUND` for a host with one of\nthese full stops, so a raw `host` does not dial. The check is reached\nthrough `servername`, a custom `lookup`, a proxy agent that connects\nfirst, or a direct call to `tls.checkServerIdentity`.\n- On Windows, the resolver already maps the full stop: on canary\na8e4e9042, `dns.lookup(\"127\u30020\u30020\u30021\")` returns `127.0.0.1` and\n`net.connect({ host: \"127\u30020\u30020\u30021\", port })` connects to 127.0.0.1. There\na raw `host` is enough.\n- #35590 adds the IDNA mapping to `dns.lookup` on every platform.\n\n**Native matcher**\n- Every native caller goes through `check_x509_server_identity`: fetch\nand its HTTPS proxy tunnel, `Bun.connect`, WebSocket, Postgres, MySQL,\nValkey. The conversion is in that one function.\n- `Bun__domainToASCII` wraps the same `parseDomainAsHost` that backs\n`url.domainToASCII`. It can run off the JS thread. The HTTP thread\nalready calls `WTF::URL` for redirects (`bun_url::href_from_string`).\n- The conversion only runs for a host with a non-ASCII byte.\n`tls.checkServerIdentity` converts every host, as Node does. So the two\nmatchers differ on an ASCII host that is not a valid URL host. Example:\n`a b.example.com` against `*.example.com` is rejected by\n`tls.checkServerIdentity` and still accepted by the native matcher. No\nresolver maps an ASCII name, so the label split is correct there. The\nconversion also rejects ASCII hosts that work today, for example `db.1`\n(a numeric last label must parse as IPv4), so the native ASCII path\nstays as it is.\n- A host is an IP address only as typed, as in Node. `\uff11\uff12\uff17.0.0.1`\nconverts to `127.0.0.1` and is still matched as a DNS name. One test\npins this for both matchers.\n- The native mismatch reason printed the host through the escaping that\nis meant for certificate names. `Bun.connect` showed `Host:\n\"foo\\u00e3\\^@\\^Bbar.example.com\".` for a UTF-8 host. It now prints the\nhost as typed, as Node does. The `Bun.connect` tests assert the full\nmessage.\n- `X509Certificate#checkHost`, the HTTP/3 client and `node:quic` use\nOpenSSL rules (`X509_check_host`), where `*` only spans letters, digits\nand \"-\". They already reject these hosts. The test table records\n`checkHost` as `undefined` for each new row.\n\n**Tests**\n- `fetch.tls.wildcard.test.ts`: `tls.connect` and `Bun.connect` end to\nend for the three code points, new rows in the `checkServerIdentity ==\nfetch` table, and a table for `tls.checkServerIdentity` (SAN, CN\nfallback, error text, `err.host`, trailing U+3002, U-label against an\nA-label SAN, IPv6 host, empty first label). Canary a8e4e9042: 77 pass,\n23 fail. With the fix: 100 pass.\n- `test/js/node/test/parallel/test-tls-check-server-identity.js` is\nbyte-identical to Node v26.8.2. It fails on canary at the U+3002 row.\n- Also ran with the debug build: `test/js/node/tls/`,\n`test/js/node/url/`, `fetch.tls.test.ts`, `fetch.tls.ipv6.test.ts`,\n`node-https-checkServerIdentity.test.ts`, `valkey-tls-verify.test.ts`,\n`test/js/bun/net/socket.test.ts`, `test/internal/source-lints/`, and the\n262 vendored `test-tls-*`, `test-https-*`, `test-x509-*` files.\n\n</details>\n\n<!-- robobun:evidence:begin -->\n\n---\n\n**[human-review]** gate passed \u00b7 iteration 0 \u00b7 7 files touched\n\n<details><summary>fails on main (without fix)</summary>\n\n```console\nASAN without fix: 23 FAILED\n$ BUN_DEBUG_QUIET_LOGS=1 bun scripts/build.ts --profile=debug --quiet test \"--reporter=junit\" \"--reporter-outfile=/tmp/pr_gate.xml\" test/js/web/fetch/fetch.tls.wildcard.test.ts\nbun test v1.4.3 (c6b7fcb5b)\n\ntest/js/web/fetch/fetch.tls.wildcard.test.ts:\n(pass) TLS wildcard hostname verification > should reject multi-label wildcard match (sub.foo.example.com vs *.example.com) [645.61ms]\n(pass) TLS wildcard hostname verification > should accept valid single-label wildcard match (foo.example.com vs *.example.com) [392.23ms]\n(pass) TLS wildcard hostname verification > should reject bare domain for wildcard cert (example.com vs *.example.com) [386.83ms]\n(pass) TLS wildcard hostname verification > should accept exact match for wildcard labels (bar.example.com vs *.example.com) [404.48ms]\n(pass) TLS wildcard hostname verification > should reject deeply nested subdomain (a.b.c.example.com vs *.example.com) [393.09ms]\n327 |       servername,\n328 |     });\n329 |     socket.on(\"secureConnect\", () => resolve({ authorized: socket.authorized }));\n330 |     socket.on(\"error\", resolve);\n331 |     try {\n332 |       expect(await promise).toMatchObject({\n                                \n... (truncated)\n\nrelease without fix: 7 FAILED\nbun test v1.4.3-canary.1 (dd5e81dd2)\n\ntest/js/web/fetch/fetch.tls.wildcard.test.ts:\n(pass) TLS certificate name matching: fetch() / checkServerIdentity / checkHost agree > checkServerIdentity == fetch > \"a.b.wild.test\" [7.83ms]\n(pass) TLS certificate name matching: fetch() / checkServerIdentity / checkHost agree > checkServerIdentity == fetch > \"wild.test\" [7.43ms]\n(pass) TLS certificate name matching: fetch() / checkServerIdentity / checkHost agree > checkServerIdentity == fetch > \"foo.wild.test\" [12.93ms]\n(pass) TLS certificate name matching: fetch() / checkServerIdentity / checkHost agree > checkServerIdentity == fetch > \"FOO.WILD.TEST\" [12.26ms]\n(pass) TLS wildcard hostname verification > should reject multi-label wildcard match (sub.foo.example.com vs *.example.com) [37.15ms]\n(pass) TLS wildcard hostname verification > should accept valid single-label wildcard match (foo.example.com vs *.example.com) [29.23ms]\n(pass) TLS wildcard hostname verification > should reject bare domain for wildcard cert (example.com vs *.example.com) [28.23ms]\n(pass) TLS certificate name matching: fetch() / checkServerIdentity / checkHost agree > checkServerIdentity == fetch > \"foo.wi\n... (truncated)\n```\n\n</details>\n\n<details><summary>passes on PR (with fix)</summary>\n\n```console\nASAN with fix: all passed\n$ BUN_DEBUG_QUIET_LOGS=1 bun scripts/build.ts --profile=debug --quiet test \"--reporter=junit\" \"--reporter-outfile=/tmp/pr_gate.xml\" test/js/web/fetch/fetch.tls.wildcard.test.ts\nbun test v1.4.3 (c6b7fcb5b)\n\ntest/js/web/fetch/fetch.tls.wildcard.test.ts:\n(pass) TLS wildcard hostname verification > should reject multi-label wildcard match (sub.foo.example.com vs *.example.com) [643.60ms]\n(pass) TLS wildcard hostname verification > should accept valid single-label wildcard match (foo.example.com vs *.example.com) [380.95ms]\n(pass) TLS wildcard hostname verification > should reject bare domain for wildcard cert (example.com vs *.example.com) [373.22ms]\n(pass) TLS wildcard hostname verification > should accept exact match for wildcard labels (bar.example.com vs *.example.com) [391.15ms]\n(pass) TLS wildcard hostname verification > should reject deeply nested subdomain (a.b.c.example.com vs *.example.com) [381.85ms]\n(pass) TLS wildcard hostname verification > should accept case-insensitive wildcard match (FOO.EXAMPLE.COM vs *.example.com) [257.09ms]\n(pass) TLS wildcard hostname verification > should accept mixed-case wildcard match (FoO.ExAmPlE.cOm vs *.example.com) [246.58m\n... (truncated)\n\nrelease with fix: all passed\n$ bun scripts/build.ts --profile=release\n[configured] bun-profile \u2192 bun (stripped) in 576ms (unchanged)\nninja: Entering directory `/workspace/bun/build/release'\n[1/126] gen cpp.rs (cppbind)\n[2/126] gen JS modules (bundle-modules)\nPreprocess modules (6548ms)\nBundle modules (44ms)\nPostprocesss modules (19ms)\nBundle Functions (402ms)\nGenerate Code (23ms)\n\n[7.04s] Bundled \"src/js\" for production\n  2600 kb\n  197 internal modules\n  13 native modules\n  50 internal functions across 16 files\n[2/9] cargo bun_runtime \u2192 libbun_runtime.a\n\u001b[1m\u001b[33mwarning\u001b[0m\u001b[1m: binary `bun_shim_impl` should have a kebab-case name\u001b[0m\n   \u001b[1m\u001b[94m|\u001b[0m\n\u001b[1m\u001b[94m 1\u001b[0m \u001b[1m\u001b[94m|\u001b[0m /workspace/bun/build/release/rust-target/.../bun_shim_impl\n   \u001b[1m\u001b[94m|\u001b[0m                                              \u001b[1m\u001b[33m^^^^^^^^^^^^^\u001b[0m\n   \u001b[1m\u001b[94m|\u001b[0m\n   \u001b[1m\u001b[94m= \u001b[0m\u001b[1mnote\u001b[0m: `cargo::non_kebab_case_bins` is set to `warn` by default\n\u001b[1m\u001b[96mhelp\u001b[0m: to change the binary name to `bun-shim-impl`, convert `bin.name`\n  \u001b[1m\u001b[94m--> \u001b[0msrc/install/windows-shim/Cargo.toml:41:8\n   \u001b[1m\u001b[94m|\u001b[0m\n\u001b[1m\u001b[94m41\u001b[0m \u001b[91m- \u001b[0mname = \u001b[91m\"bun_shim_impl\"\u001b[0m\n\u001b[1m\u001b[94m41\u001b[0m \u001b[92m+ \u001b[0mname = \u001b[92m\"\n... (truncated)\n```\n\n</details>\n\n<details><summary>diff hotspot</summary>\n\n```\nsrc/boringssl/lib.rs                               |  38 +++++-\n src/js/internal/url.ts                             |   5 +\n src/js/node/tls.ts                                 |  14 +-\n src/js/node/url.ts                                 |   3 +-\n src/jsc/bindings/NodeURL.cpp                       |   9 ++\n .../parallel/test-tls-check-server-identity.js     |  30 +++++\n test/js/web/fetch/fetch.tls.wildcard.test.ts       | 149 ++++++++++++++++++++-\n 7 files changed, 241 insertions(+), 7 deletions(-)\n```\n\n</details>\n\n**gate history** \u00b7 2 passed \u00b7 0 rejected \u00b7 iteration 0\n\n<details><summary>evidence per changed file</summary>\n\n```\nfile                                                      reads  edits  tests\nsrc/boringssl/lib.rs                                          6      6     25\nsrc/js/internal/url.ts                                        1      1     24\nsrc/js/node/tls.ts                                            4      4     26\nsrc/js/node/url.ts                                            1      1     24\nsrc/jsc/bindings/NodeURL.cpp                                  2      2     24\n\u2026js/node/test/parallel/test-tls-check-server-identity.js      0      0     32\ntest/js/web/fetch/fetch.tls.wildcard.test.ts                  4      7     24\n```\n\n</details>\n\n<!-- robobun:evidence:end -->"
    },
    "GHSA-PFWX-36V6-832X": {
      "31eff2e275426dd44911ada85e653f4ccafff72a": "test(install): serve GitHub tarball fixtures locally in the GHSA-pfwx-36v6-832x and lifecycle script tests\n\nAdds githubTarball() to test/harness.ts. It builds the tarball layout GitHub\nreturns for /repos/<owner>/<repo>/tarball/<ref>. The GHSA-pfwx-36v6-832x and\nlifecycle script tests serve that tarball from a local server through\nGITHUB_API_URL. isolated-install.test.ts drops its inline tar writer for the\nhelper.",
      "7bd4bbf5252d8b280f7470f507a1043b2fa855a4": "test(install): serve GitHub tarball fixtures locally in the GHSA-pfwx-36v6-832x and lifecycle script tests (#39115)",
      "25fd0b9b28edf3faf3134f4ccd04204149d06fd4": "test(install): serve GitHub tarball fixtures locally instead of fetching from api.github.com\n\nGHSA-pfwx-36v6-832x.test.ts and the git dependency lifecycle script tests\ninstalled real repositories from api.github.com, so any GitHub outage failed\nthem on every PR. Both now build the tarball GitHub would return with a new\ngithubTarball() harness helper and serve it from a local Bun.serve that the\ninstall is pointed at through GITHUB_API_URL.\n\nThe integrity tests also now assert the exact sha512 of the served bytes,\nthat a rejected tarball is not installed and leaves the lockfile alone, and\ncount tarball requests; each case runs against both the buffered and the\nstreaming extractor, which verify the integrity independently.",
      "fc444066c05011ac993104782328b63f7ea48616": "test(update-interactive): isolate install cache per test to fix Windows flake (#33964)\n\n`test/cli/update_interactive_install.test.ts` has been flaking on\nWindows lanes (2019 x64, 2019 x64-baseline, 11 aarch64) since it was\nintroduced in #24280. In the 71700-71800 build range alone it shows up\nin ~35 builds, usually passing on retry but occasionally going fully red\n(e.g. build 71752, build 71675).\n\n## Repro\n\nOn Windows with a cold cache, the unfixed file fails ~30% of the time:\n\n```\nOriginal test failures: 9 / 30\n```\n\nThe captured stderr is:\n\n```\nENOENT: failed opening cache/package/version dir for package is-number\n```\n\n## Cause\n\nThe two tests in the describe block run concurrently\n(`describe.concurrent`) and both transitively depend on `is-number` (via\n`is-even` -> `is-odd` -> `is-number` and `is-odd` -> `is-number`). Both\nspawned `bun install` processes share the default global install cache,\nand on Windows one of them hits ENOENT when the other is mid-write to\nthe same cache entry.\n\nThe original test also asserted the install exit code without draining\nstdout/stderr, so CI only ever showed `Expected: 0 / Received: 1` with\nno diagnostic.\n\n## Fix\n\nGive each test its own `BUN_INSTALL_CACHE_DIR` inside its temp dir. This\nis the same pattern used across `test/cli/install/` (e.g.\n`bun-run-dir.test.ts`, `GHSA-pfwx-36v6-832x.test.ts`,\n`isolated-install.test.ts`). Also drain and surface stdout/stderr on the\ninitial install so a future failure reports the actual error.\n\n## Verification\n\nOn Windows, 50 cold-cache iterations with the fix:\n\n```\nFixed test failures: 0 / 50\n```\n\n`bun bd test test/cli/update_interactive_install.test.ts` passes on\nLinux.\n\nThis is a test-only change; no `src/` behaviour is modified.\n\n<!-- robobun:evidence:begin -->\n\n---\n\n**[stamp-90s]** gate passed \u00b7 iteration 0 \u00b7 1 files touched\n\n<details><summary>passes on PR (with fix)</summary>\n\n```console\nTest-only change.\n\nDebug/ASAN (expected pass):\n$ bun bd test 'test/cli/update_interactive_install.test.ts'\n$ BUN_DEBUG_QUIET_LOGS=1 bun scripts/build.ts --profile=debug --quiet test test/cli/update_interactive_install.test.ts\ninfo: syncing channel updates for nightly-2026-05-06-x86_64-unknown-linux-gnu\ninfo: latest update on 2026-05-06 for version 1.97.0-nightly (e95e73209 2026-05-05)\ninfo: component rust-src is up to date\ninfo: checking for self-update (current version: 1.29.0)\nbun test v1.4.0 (c10a6210e)\n\ntest/cli/update_interactive_install.test.ts:\n(pass) bun update --interactive actually installs packages > should work with --latest flag [700.77ms]\n(pass) bun update --interactive actually installs packages > should update package.json AND install packages [791.58ms]\n\n 2 pass\n 0 fail\n 15 expect() calls\nRan 2 tests across 1 file. [2.86s]\nExit: 0\n```\n\n</details>\n\n<details><summary>diff hotspot</summary>\n\n```\ntest/cli/update_interactive_install.test.ts | 35 +++++++++++++++++++++++------\n 1 file changed, 28 insertions(+), 7 deletions(-)\n```\n\n</details>\n\n**gate history** \u00b7 1 passed \u00b7 0 rejected \u00b7 iteration 0\n\n<details><summary>evidence per changed file</summary>\n\n```\nfile                                         reads  edits  tests\ntest/cli/update_interactive_install.test.ts      2      1      0\n```\n\n</details>\n\n<!-- robobun:evidence:end -->",
      "f5d98191b7c0a605a4ee79c5e37a2933046728be": "fix(install): store and verify SHA-512 integrity hash for GitHub tarball dependencies (#27019)\n\n## Summary\n\n- Compute SHA-512 hash of GitHub tarball bytes during extraction and\nstore in `bun.lock`\n- Verify the hash on subsequent installs when re-downloading, rejecting\ntampered tarballs\n- Automatically upgrade old lockfiles without integrity by computing and\npersisting the hash\n- Maintain backward compatibility with old lockfile format (no integrity\nfield)\n\nFixes GHSA-pfwx-36v6-832x\n\n## Lockfile format change\n\n```\nBefore: [\"pkg@github:user/repo#ref\", {}, \"resolved-commit\"]\nAfter:  [\"pkg@github:user/repo#ref\", {}, \"resolved-commit\", \"sha512-...\"]\n```\n\nThe integrity field is optional for backward compatibility. Old\nlockfiles are automatically upgraded when the tarball is re-downloaded.\n\n## Test plan\n\n- [x] Fresh install stores SHA-512 integrity hash in lockfile\n- [x] Re-install with matching hash succeeds\n- [x] Re-install with mismatched hash rejects the tarball\n- [x] Old lockfile without integrity is auto-upgraded with hash on\nre-download\n- [x] Cache hits still work without re-downloading\n- [x] Existing GitHub dependency tests pass (10/10)\n- [x] Existing git resolution snapshot test passes\n- [x] Yarn migration snapshot tests pass\n\n\ud83e\udd16 Generated with [Claude Code](https://claude.com/claude-code)\n\n---------\n\nCo-authored-by: Claude Bot <claude-bot@bun.sh>\nCo-authored-by: Claude <noreply@anthropic.com>\nCo-authored-by: autofix-ci[bot] <114827586+autofix-ci[bot]@users.noreply.github.com>\nCo-authored-by: Jarred Sumner <jarred@jarredsumner.com>"
    },
    "CVE-2026-58044": {
      "99eadd33e6d206130e1aae2de842d061c9dbf351": "node:http: reject requests with more header fields than server.maxHeadersCount\n\nA lowered server.maxHeadersCount cut the header list the handler saw, after\nthe parser had framed the message with every field. A Content-Length past\nthe limit was hidden from req.headers and req.rawHeaders and still\ndelimited a body. Node rejects such a request since nodejs/node 821688aaa0\n(CVE-2026-58044).\n\nThe native server now takes the limit as the bound of the field loop in\nuWS getHeaders, so the existing parse error path answers: 'clientError'\nwith HPE_HEADER_OVERFLOW, 431 by default, no more parsing on the\nconnection. Trailer fields count from zero against the same limit. The\nvalue reaches the parser at listen() and on every later assignment.\n\nThe llhttp binding gets Node's TrackHeaderPair for request parsers, in the\nrevision that reads maxHeaderPairs once per header section\n(nodejs/node#64988). Responses keep truncating. This covers sockets passed\nto server.emit('connection') and the HTTP/1.1 connections of an allowHTTP1\nHTTP/2 server. That listener now also starts from Node's 2000-pair default\nand assembles the header blocks and trailers the parser flushes through\nkOnHeaders. Before, a request with 32 or more fields reached the handler\nwith only its last block, so Host and Content-Length were hidden there too.\n\nThe JS truncation of rawHeaders and rawTrailers is gone. The vendored Node\ntests follow upstream: two added, two updated, and\ntest-http-rawheaders-limit.js removed."
    },
    "CVE-2026-56850": {
      "6b19264837f813157eee6a03ba8c2a8e72945206": "node:https: key the Agent pool name by what object-valued and file TLS options hold\n\nhttps.Agent#getName() appended ca, cert, key, pfx, crl and dhparam with\nstring coercion. A { buf | pem, passphrase } entry, an ArrayBuffer and a\nBlob all coerce to \"[object ...]\", and the Bun-only certFile, keyFile and\ncaFile options were not in the name. Requests that present different client\ncertificates then shared one pool name, one keep-alive connection and one\ncached TLS session.\n\npoolKeyPart() keeps Node's name for strings, Buffers, TypedArrays and\narrays of those. It keys a pfx entry the way Node's getPfxAgentKey() does\n(nodejs/node 9f03017f38, CVE-2026-56850), keys a key entry by its pem, keys\nan ArrayBuffer by its bytes, and keys every other object by identity.\ncertFile, keyFile and caFile are labelled parts at the end of the name."
    },
    "CVE-2026-48619": {
      "2b47ed7181c5184ef85eebe490c4eb0f9ae1f184": "node:http2: cap the client originSet at maxOriginSetSize like node 26.4.0\n\nA server could grow a client session's originSet without bound by\nsending ORIGIN frames for the life of the session (CVE-2026-48619).\nAdd the maxOriginSetSize connect option (default 128) and destroy the\nsession with ERR_HTTP2_TOO_MANY_ORIGINS when the set would grow past it.",
      "45252298fdbb5fe102be265589e699bb7ded06f8": "node:http2: cap the client originSet with maxOriginSetSize\n\nA server can send ORIGIN frames with distinct origins without end. The\nclient added every origin to session.originSet, so the set grew for the\nlife of the session.\n\nhttp2.connect() now accepts options.maxOriginSetSize (default 128), as\nnode v26.4.0 does (CVE-2026-48619). The origin handler checks the size\nof the set before each add. The entry that goes past the limit destroys\nthe session with ERR_HTTP2_TOO_MANY_ORIGINS, and that frame emits no\n'origin' event.\n\nAdds test-http2-origin-set-max-size.mjs from node."
    },
    "CVE-2026-48931": {
      "f10bc786ef74aa4a4ee740b0ce01eb42d7a23399": "node:http: destroy an idle pooled keep-alive socket that receives unsolicited data (#42128)\n\n### Problem\n\n- A socket parked in `agent.freeSockets` (`keepAlive: true`) keeps\nreading while idle, and unsolicited bytes do not retire it. A whole\nresponse written there is attributed to the next request.\n- The cause is the Agent's `'free'` handler\n(`src/js/node/_http_agent.ts:123`). It pools the socket with nothing\nwatching the read side, where the parser is already detached. Node fixed\nthe same hole as CVE-2026-48931 (https://hackerone.com/reports/3582376).\nThis ports that one fix only.\n\n### Fix\n\n- The Agent marks a pooled socket with `kDestroyOnRead`, and\n`reuseSocket` clears it. A freed socket that already holds buffered\nreadable data is destroyed before it can be pooled or handed to a queued\nrequest.\n- In `node:net`, `pushDataToSocket` (the one function through which all\nthree handler tables feed the stream, from #35347) destroys a marked\nsocket instead of pushing the bytes.\n- Like node's guard, it adds no public stream listener (Notes).\n- Verified: `test/js/node/http/node-http-agent-free-socket.test.ts`, 8\n`node:test` cases that Bun runs in-process, plus one that runs the same\nfile under Node.js. 6 fail on released bun. On Node they pass from\nv26.4.0, where the upstream fix shipped (Notes). Also\n`test/js/node/http/`, `test/js/node/net/`, and the vendored http, https,\nnet, tls suites. Self-reviewed: 4 concerns, 3 addressed, 1 declined\n(Notes, \"Scope\").\n\n### Background\n\n- `Agent.freeSockets` holds idle keep-alive sockets per origin.\n`addRequest` takes one out and calls `reuseSocket`.\n- A pooled socket has no parser and no `'data'` listener: what it\nreceives goes nowhere, or reaches the next response's parser.\n- A bun `net.Socket` reads through a native handler table passed at dial\ntime. One table serves every socket that used it, so a per-socket hook\nmust live on the socket.\n\n<details><summary>Notes</summary>\n\n**Reproduction** (bun 1.4.3, linux x64). A raw origin answers each\nrequest with its own path, then writes a complete unsolicited response\non the idle pooled connection.\n\n| when the stray response arrives | stock bun | this branch |\n| --- | --- | --- |\n| the event loop polls before the next request | socket stays pooled,\nbytes discarded | socket destroyed, next request dials a fresh\nconnection |\n| the next request is issued in the same tick | next request reads\n`poison` | next request reads `poison` |\n\nNode v26.3.0 (before the guard shipped) behaves like stock bun in both\nrows.\n\n**Node versions.** The upstream fix first shipped in Node v26.4.0\n(`lib/_http_agent.js` at the v26.3.0 tag has no\n`installFreeSocketDataGuard`). Forced to run everywhere: Node v26.3.0\npasses 2 of 8 (the reuse cases) and fails the 6 guard cases, like stock\nBun; Node v26.4.0 passes 6 and fails the 2 queued-request cases (Node\nchecks buffered bytes only on the pool path); this branch passes 8. In\nthe file the guard cases skip on Node < 26.4.0 and the queued-request\ncase skips on Node, each with the reason.\n\n**No public listener.** Node's first version of this guard used a\n`'data'` listener plus `resume()`. node-fetch@2 reads\n`socket.listenerCount('data')` while a response closes and started\nreporting false `ERR_STREAM_PREMATURE_CLOSE` errors (nodejs/node#63989),\nso node reworked the guard onto the stream handle's internal `onread`\nhook. Bun has no per-socket equivalent of that hook: the handler table\ngiven to `Bun.connect` is one shared cell, and `socket.reload()` mutates\nit for every socket that shares it. Hence the per-socket flag, read in\n`pushDataToSocket`. The test asserts `listenerCount('data')` and\n`listenerCount('readable')` are still 0 on a free socket, as node's\ndoes. The handler table built for the `onread` socket option keeps its\nown `data` callback: it bypasses the stream, and `node:http` cannot use\nsuch a socket.\n\n**The same-tick row is the residual race, and node has it too.** The\nstray bytes are still unread in the kernel when `addRequest` hands the\nsocket out, so no check in JS can see them. Node's own test says as much\n(\"in a real attack, there is always time between the poison arriving and\nthe next client request\"). Closing it needs a peek of the read side at\ncheckout, which is what #41987 does for `fetch()`'s pool in Rust.\nUnrelated to this change: when bytes that win that race are not a valid\nresponse, the client's parse error surfaces as an uncaught exception\ninstead of an `'error'` event on the request, on stock bun and on this\nbranch alike.\n\n**The upstream test is not vendored yet.**\n`test/parallel/test-http-agent-free-socket-data-guard.js` injects the\nstray bytes with `req.socket.write()` on a `node:http` server. Bun's\nserver leaves that socket corked after the request\n(`socket.writableCorked === 1`, the bytes sit in the writable buffer),\nwhich is #35664. Once that lands, the upstream file can be vendored as\nis and the bespoke cases trimmed. The new cases drive a raw `net`/`tls`\nserver instead, which is also how they cover `https.Agent`.\n\n**Scope.** CVE-2026-48931 shipped in a Node security release together\nwith other advisories. This PR does not examine or claim anything about\nthe others. The self-review asked for a public tracking issue that lists\nthem against bun; I left that to the maintainers, since it amounts to\npublishing an unverified vulnerability list.\n\n**Already-buffered bytes.** Node's `installFreeSocketDataGuard` destroys\na socket whose `readableLength > 0`, but the caller still pushes it into\n`freeSockets`, where `'close'` prunes it a tick later (and `addRequest`\ncan pop it first under `lifo`), and the check does not run at all when\nthe freed socket goes straight to a request queued in `agent.requests`.\nHere the check runs right after the `writable` check in the `'free'`\nhandler, so both hand-off paths share it and a destroyed socket is never\npooled. For the queued path, the destroyed socket's `'close'` reaches\n`removeSocket`, which dials a new connection for the waiting request.\nThe \"holds unsolicited data\" cases cover both paths: they `push()` the\nstray bytes in the response's `'end'` handler, which is after the parser\ndetached and one tick before `'free'`.\n\n**Disarm point.** `reuseSocket` clears the flag, as in Node, where\n`Agent.prototype.reuseSocket` is the only caller of\n`removeFreeSocketDataGuard` and nothing else restores `_handle.onread`\n(`initSocketHandle` runs only for a new or reconnected socket). A\nsubclass that replaces `reuseSocket` without calling the parent loses\nreused sockets on both runtimes.\n\n**TLS.** The guard sees decrypted application data only, so a\npost-handshake `NewSessionTicket` on an idle pooled socket does not trip\nit. The `https` cases cover a parked TLS socket that is poisoned, one\nfreed with buffered bytes (pooled and queued paths), and one that is\nreused.\n\n**Cost.** One symbol-property load per received chunk. The `src/` diff\nis 28 lines. `kDestroyOnRead` is initialized in the `Socket` constructor\nso the read path stays monomorphic.\n\n**Pre-existing failures in this container, with and without this diff**\n(each one rechecked against main's `src/js` on the same build):\n`test-http-agent-keepalive.js` (`agent.sockets[name]` is not cleaned up\nafter the server closes the socket),\n`test-http-client-timeout-option.js`, the `test-http(s)-proxy-request*`\nfamily, one subprocess case of `node-http-syscall-fault.test.ts`, 10\n`node-net.test.ts` cases that fail here with `ECONNREFUSED`,\n`test-net-server-async-dispose.mjs`,\n`test-net-connect-custom-lookup-non-string-address.mjs`,\n`test-tls-client-allow-partial-trust-chain.js`.\n\n</details>\n\n<!-- robobun:evidence:begin -->\n\n---\n\n**[human-review]** gate passed \u00b7 iteration 2 \u00b7 4 files touched\n\n<details><summary>fails on main (without fix)</summary>\n\n```console\nASAN without fix: 6 FAILED\n$ BUN_DEBUG_QUIET_LOGS=1 bun scripts/build.ts --profile=debug --quiet test \"--reporter=junit\" \"--reporter-outfile=/tmp/pr_gate.xml\" test/js/node/http/node-http-agent-free-socket.test.ts\nbun test v1.4.3 (4ff919377)\n\ntest/js/node/http/node-http-agent-free-socket.test.ts:\n157 |         assert.strictEqual(freeSocket.listenerCount(\"readable\"), 0);\n158 | \n159 |         serverSockets[0].write(poisonedResponse);\n160 | \n161 |         await pollUntil(() => freeSocket.destroyed && agent.freeSockets[name] === undefined);\n162 |         assert.strictEqual(freeSocket.destroyed, true);\n                     ^\nAssertionError: Expected values to be strictly equal:\n\nfalse !== true\n\n generatedMessage: true,\n     actual: false,\n   expected: true,\n   operator: \"strictEqual\",\n       diff: \"simple\",\n       code: \"ERR_ASSERTION\"\n\n      at /workspace/bun/test/js/node/http/node-http-agent-free-socket.test.ts:162:16\n      at withAgent (/workspace/bun/test/js/node/http/node-http-agent-free-socket.test.ts:118:11)\n      at /workspace/bun/test/js/node/http/node-http-agent-free-socket.test.ts:150:13\n      at node:test:1781:26\n      at executeTestNode (node:test:1785:63)\n      at processTicksAndRejec\n... (truncated)\n\nrelease without fix: all passed\nbun test v1.4.3-canary.1 (9f655b715)\n\ntest/js/node/http/node-http-agent-free-socket.test.ts:\n(pass) http.Agent free keep-alive socket over http > destroys a free socket that receives unsolicited data [22.64ms]\n(pass) http.Agent free keep-alive socket over http > does not pool a socket that holds unsolicited data when it is freed [3.12ms]\n(pass) http.Agent free keep-alive socket over http > does not hand a freed socket that holds unsolicited data to a queued request [2.17ms]\n(pass) http.Agent free keep-alive socket over http > reuses a free socket that received nothing [1.64ms]\n(pass) http.Agent free keep-alive socket over https > destroys a free socket that receives unsolicited data [41.32ms]\n(pass) http.Agent free keep-alive socket over https > does not pool a socket that holds unsolicited data when it is freed [5.10ms]\n(pass) http.Agent free keep-alive socket over https > does not hand a freed socket that holds unsolicited data to a queued request [4.67ms]\n(pass) http.Agent free keep-alive socket over https > reuses a free socket that received nothing [3.29ms]\n(pass) Node.js compatibility > all tests pass in Node.js [156.92ms]\n\n 9 pass\n 0 fail\nRan 9 tests across 1\n... (truncated)\n```\n\n</details>\n\n<details><summary>passes on PR (with fix)</summary>\n\n```console\nASAN with fix: all passed\n$ BUN_DEBUG_QUIET_LOGS=1 bun scripts/build.ts --profile=debug --quiet test \"--reporter=junit\" \"--reporter-outfile=/tmp/pr_gate.xml\" test/js/node/http/node-http-agent-free-socket.test.ts\nbun test v1.4.3 (4ff919377)\n\ntest/js/node/http/node-http-agent-free-socket.test.ts:\n(pass) http.Agent free keep-alive socket over http > destroys a free socket that receives unsolicited data [1158.89ms]\n(pass) http.Agent free keep-alive socket over http > does not pool a socket that holds unsolicited data when it is freed [172.89ms]\n(pass) http.Agent free keep-alive socket over http > does not hand a freed socket that holds unsolicited data to a queued request [115.53ms]\n(pass) http.Agent free keep-alive socket over http > reuses a free socket that received nothing [100.60ms]\n(pass) http.Agent free keep-alive socket over https > destroys a free socket that receives unsolicited data [474.62ms]\n(pass) http.Agent free keep-alive socket over https > does not pool a socket that holds unsolicited data when it is freed [200.09ms]\n(pass) http.Agent free keep-alive socket over https > does not hand a freed socket that holds unsolicited data to a queued request [147.77ms]\n(pass) http.Agent fre\n... (truncated)\n\nrelease with fix: all passed\n$ bun scripts/build.ts --profile=release\n[configured] bun-profile \u2192 bun (stripped)\n  target       linux-x64-gnu\n  build type   Release\n  build dir    ./build/release\n  revision     400b9a922c\n  features     baseline\n\n23 deps, 131 codegen, 1172 objects in 697ms\n\nninja: Entering directory `/workspace/bun/build/release'\n[1/146] fetch picohttpparser\n[picohttpparser] up to date\n[2/146] fetch WebKit (prebuilt)\n[WebKit] up to date\n[3/146] gen ZigGeneratedClasses.{cpp,h,rs}\nFound 2 classes from /workspace/bun/src/jsc/resolve_message.classes.ts\n  - ResolveMessage (15 fields)\n  - BuildMessage (10 fields)\nFound 1 classes from /workspace/bun/src/runtime/api/Archive.classes.ts\n  - Archive (4 fields, 1 class fields)\nFound 2 classes from /workspace/bun/src/runtime/api/BunObject.classes.ts\n  - ResourceUsage (8 fields)\n  - Subprocess (20 fields)\nFound 1 classes from /workspace/bun/src/runtime/api/cron.classes.ts\n  - CronJob (5 fields)\nFound 3 classes from /workspace/bun/src/runtime/api/filesystem_router.classes.ts\n  - FileSystemRouter (5 fields)\n  - FrameworkFileSystemRouter (2 fields)\n  - MatchedRoute (8 fields)\nFound 1 classes from /workspace/bun/src/runtime/api/Glob.classes.ts\n\n... (truncated)\n```\n\n</details>\n\n<details><summary>diff hotspot</summary>\n\n```\nsrc/js/internal/net/symbols.ts                     |   3 +\n src/js/node/_http_agent.ts                         |  12 +\n src/js/node/net.ts                                 |  14 +-\n .../node/http/node-http-agent-free-socket.test.ts  | 251 +++++++++++++++++++++\n 4 files changed, 279 insertions(+), 1 deletion(-)\n```\n\n</details>\n\n**gate history** \u00b7 3 passed \u00b7 2 rejected \u00b7 iteration 2\n\n<details><summary>evidence per changed file</summary>\n\n```\nfile                                                   reads  edits  tests\nsrc/js/internal/net/symbols.ts                             3      4      6\nsrc/js/node/_http_agent.ts                                 5     10      8\nsrc/js/node/net.ts                                        13     15      7\ntest/js/node/http/node-http-agent-free-socket.test.ts      0      0      2\n```\n\n</details>\n\n<!-- robobun:evidence:end -->"
    },
    "CVE-2026-9277": {
      "f910baf68bc6548ad43955e39b903a57cb04b44b": "build(deps): bump the actions group across 1 directory with 4 updates (#40632)\n\nBumps the actions group with 4 updates in the / directory:\n[anthropics/claude-code-action/base-action](https://github.com/anthropics/claude-code-action),\n[vmactions/freebsd-vm](https://github.com/vmactions/freebsd-vm),\n[docker/setup-buildx-action](https://github.com/docker/setup-buildx-action)\nand [docker/login-action](https://github.com/docker/login-action).\n\nUpdates `anthropics/claude-code-action/base-action` from 1.0.183 to\n1.0.198\n<details>\n<summary>Release notes</summary>\n<p><em>Sourced from <a\nhref=\"https://github.com/anthropics/claude-code-action/releases\">anthropics/claude-code-action/base-action's\nreleases</a>.</em></p>\n<blockquote>\n<h2>v1.0.198</h2>\n<p><strong>Full Changelog</strong>: <a\nhref=\"https://github.com/anthropics/claude-code-action/compare/v1.0.197...v1.0.198\">https://github.com/anthropics/claude-code-action/compare/v1.0.197...v1.0.198</a></p>\n<h2>v1.0.197</h2>\n<h2>What's Changed</h2>\n<ul>\n<li>fix: neutralize the checkout credential under actions/checkout v6+\ninclude layout (<a\nhref=\"https://redirect.github.com/anthropics/claude-code-action/issues/1510\">#1510</a>)\nby <a href=\"https://github.com/TemRevil\"><code>@\u200bTemRevil</code></a> in\n<a\nhref=\"https://redirect.github.com/anthropics/claude-code-action/pull/1526\">anthropics/claude-code-action#1526</a></li>\n</ul>\n<h2>New Contributors</h2>\n<ul>\n<li><a href=\"https://github.com/TemRevil\"><code>@\u200bTemRevil</code></a>\nmade their first contribution in <a\nhref=\"https://redirect.github.com/anthropics/claude-code-action/pull/1526\">anthropics/claude-code-action#1526</a></li>\n</ul>\n<p><strong>Full Changelog</strong>: <a\nhref=\"https://github.com/anthropics/claude-code-action/compare/v1.0.196...v1.0.197\">https://github.com/anthropics/claude-code-action/compare/v1.0.196...v1.0.197</a></p>\n<h2>v1.0.196</h2>\n<h2>What's Changed</h2>\n<ul>\n<li>fix(security): unify secret redaction in public comment outputs by\n<a href=\"https://github.com/Juwan-Hwang\"><code>@\u200bJuwan-Hwang</code></a>\nin <a\nhref=\"https://redirect.github.com/anthropics/claude-code-action/pull/1693\">anthropics/claude-code-action#1693</a></li>\n<li>fix: strip unused ALL_INPUTS environment variable from Claude\nsubprocess env by <a\nhref=\"https://github.com/Juwan-Hwang\"><code>@\u200bJuwan-Hwang</code></a> in\n<a\nhref=\"https://redirect.github.com/anthropics/claude-code-action/pull/1692\">anthropics/claude-code-action#1692</a></li>\n<li>fix(github): match bot actors in comment filters using GraphQL\n__typename by <a\nhref=\"https://github.com/Neal006\"><code>@\u200bNeal006</code></a> in <a\nhref=\"https://redirect.github.com/anthropics/claude-code-action/pull/1616\">anthropics/claude-code-action#1616</a></li>\n</ul>\n<h2>New Contributors</h2>\n<ul>\n<li><a\nhref=\"https://github.com/Juwan-Hwang\"><code>@\u200bJuwan-Hwang</code></a>\nmade their first contribution in <a\nhref=\"https://redirect.github.com/anthropics/claude-code-action/pull/1693\">anthropics/claude-code-action#1693</a></li>\n</ul>\n<p><strong>Full Changelog</strong>: <a\nhref=\"https://github.com/anthropics/claude-code-action/compare/v1.0.195...v1.0.196\">https://github.com/anthropics/claude-code-action/compare/v1.0.195...v1.0.196</a></p>\n<h2>v1.0.195</h2>\n<p><strong>Full Changelog</strong>: <a\nhref=\"https://github.com/anthropics/claude-code-action/compare/v1.0.194...v1.0.195\">https://github.com/anthropics/claude-code-action/compare/v1.0.194...v1.0.195</a></p>\n<h2>v1.0.194</h2>\n<h2>What's Changed</h2>\n<ul>\n<li>Harden delete_files MCP tool: validate paths within repo root\n(consistency with commit_files) by <a\nhref=\"https://github.com/GabrielOnRails\"><code>@\u200bGabrielOnRails</code></a>\nin <a\nhref=\"https://redirect.github.com/anthropics/claude-code-action/pull/1636\">anthropics/claude-code-action#1636</a></li>\n<li>Only limit fetch depth when the checkout is already shallow by <a\nhref=\"https://github.com/winklemad\"><code>@\u200bwinklemad</code></a> in <a\nhref=\"https://redirect.github.com/anthropics/claude-code-action/pull/1647\">anthropics/claude-code-action#1647</a></li>\n<li>fix(base-action): add ~/.local/bin to $GITHUB_PATH after\nauto-install by <a\nhref=\"https://github.com/anishesg\"><code>@\u200banishesg</code></a> in <a\nhref=\"https://redirect.github.com/anthropics/claude-code-action/pull/1643\">anthropics/claude-code-action#1643</a></li>\n<li>feat(context): include diffHunk in PR review comment context by <a\nhref=\"https://github.com/rishavnaskar\"><code>@\u200brishavnaskar</code></a>\nin <a\nhref=\"https://redirect.github.com/anthropics/claude-code-action/pull/1584\">anthropics/claude-code-action#1584</a></li>\n<li>ci: skip Claude-backed test jobs on fork PRs by <a\nhref=\"https://github.com/ashwin-ant\"><code>@\u200bashwin-ant</code></a> in <a\nhref=\"https://redirect.github.com/anthropics/claude-code-action/pull/1655\">anthropics/claude-code-action#1655</a></li>\n<li>fix: bump shell-quote to 1.8.4 to remediate CVE-2026-9277 by <a\nhref=\"https://github.com/doshidhaval\"><code>@\u200bdoshidhaval</code></a> in\n<a\nhref=\"https://redirect.github.com/anthropics/claude-code-action/pull/1557\">anthropics/claude-code-action#1557</a></li>\n<li>fix(images): bound attachment downloads by <a\nhref=\"https://github.com/abhinavkr26104\"><code>@\u200babhinavkr26104</code></a>\nin <a\nhref=\"https://redirect.github.com/anthropics/claude-code-action/pull/1625\">anthropics/claude-code-action#1625</a></li>\n</ul>\n<h2>New Contributors</h2>\n<ul>\n<li><a\nhref=\"https://github.com/GabrielOnRails\"><code>@\u200bGabrielOnRails</code></a>\nmade their first contribution in <a\nhref=\"https://redirect.github.com/anthropics/claude-code-action/pull/1636\">anthropics/claude-code-action#1636</a></li>\n<li><a href=\"https://github.com/winklemad\"><code>@\u200bwinklemad</code></a>\nmade their first contribution in <a\nhref=\"https://redirect.github.com/anthropics/claude-code-action/pull/1647\">anthropics/claude-code-action#1647</a></li>\n<li><a\nhref=\"https://github.com/doshidhaval\"><code>@\u200bdoshidhaval</code></a>\nmade their first contribution in <a\nhref=\"https://redirect.github.com/anthropics/claude-code-action/pull/1557\">anthropics/claude-code-action#1557</a></li>\n</ul>\n<p><strong>Full Changelog</strong>: <a\nhref=\"https://github.com/anthropics/claude-code-action/compare/v1.0.193...v1.0.194\">https://github.com/anthropics/claude-code-action/compare/v1.0.193...v1.0.194</a></p>\n<h2>v1.0.193</h2>\n<p><strong>Full Changelog</strong>: <a\nhref=\"https://github.com/anthropics/claude-code-action/compare/v1.0.192...v1.0.193\">https://github.com/anthropics/claude-code-action/compare/v1.0.192...v1.0.193</a></p>\n<h2>v1.0.192</h2>\n<h2>What's Changed</h2>\n<ul>\n<li>fix(mcp): paginate GitHub Actions results by <a\nhref=\"https://github.com/abhinavkr26104\"><code>@\u200babhinavkr26104</code></a>\nin <a\nhref=\"https://redirect.github.com/anthropics/claude-code-action/pull/1629\">anthropics/claude-code-action#1629</a></li>\n<li>fix(summary): keep every text block in structured tool results by <a\nhref=\"https://github.com/Neal006\"><code>@\u200bNeal006</code></a> in <a\nhref=\"https://redirect.github.com/anthropics/claude-code-action/pull/1619\">anthropics/claude-code-action#1619</a></li>\n</ul>\n<!-- raw HTML omitted -->\n</blockquote>\n<p>... (truncated)</p>\n</details>\n<details>\n<summary>Commits</summary>\n<ul>\n<li><a\nhref=\"https://github.com/anthropics/claude-code-action/commit/3f854a8fb5146b39d5cbf8b57f70d80810e1366f\"><code>3f854a8</code></a>\nchore: bump Claude Code to 2.1.238 and Agent SDK to 0.3.238</li>\n<li><a\nhref=\"https://github.com/anthropics/claude-code-action/commit/5ee796a55f92566ecd7e39d70dd613abcbea0d7c\"><code>5ee796a</code></a>\nchore: bump Claude Code to 2.1.237 and Agent SDK to 0.3.237</li>\n<li><a\nhref=\"https://github.com/anthropics/claude-code-action/commit/cff8d3c8f08ae322e07a1c98ef1b882dc32ddc46\"><code>cff8d3c</code></a>\nfix(git-config): neutralize checkout credential in include-based config\n(<a\nhref=\"https://redirect.github.com/anthropics/claude-code-action/issues/1526\">#1526</a>)</li>\n<li><a\nhref=\"https://github.com/anthropics/claude-code-action/commit/e2a4b761cd77a1138a5b41410eda9b28581f9bcd\"><code>e2a4b76</code></a>\nchore: bump Claude Code to 2.1.236 and Agent SDK to 0.3.236</li>\n<li><a\nhref=\"https://github.com/anthropics/claude-code-action/commit/65b50df0838d26293e18bb62f71492d3c11dc82d\"><code>65b50df</code></a>\nfix(github): match bot actors in comment filters using GraphQL\n__typename (<a\nhref=\"https://redirect.github.com/anthropics/claude-code-action/issues/1\">#1</a>...</li>\n<li><a\nhref=\"https://github.com/anthropics/claude-code-action/commit/0a80d21df70c4ad2af73b28e4aad2ed725e2eb75\"><code>0a80d21</code></a>\nfix: strip unused ALL_INPUTS environment variable from Claude subprocess\nenv ...</li>\n<li><a\nhref=\"https://github.com/anthropics/claude-code-action/commit/54eadc2f72a6e4673b0b6b4568bf3df417fa3659\"><code>54eadc2</code></a>\nfix(security): unify secret redaction in public comment outputs (<a\nhref=\"https://redirect.github.com/anthropics/claude-code-action/issues/1693\">#1693</a>)</li>\n<li><a\nhref=\"https://github.com/anthropics/claude-code-action/commit/d40ddef4c030e508327d6e35a9c45f3368482c50\"><code>d40ddef</code></a>\nchore: bump Claude Code to 2.1.235 and Agent SDK to 0.3.235</li>\n<li><a\nhref=\"https://github.com/anthropics/claude-code-action/commit/459ad358ae43fea66bfefd0a1f8d840b4b9791fb\"><code>459ad35</code></a>\nchore: bump Claude Code to 2.1.234 and Agent SDK to 0.3.234</li>\n<li><a\nhref=\"https://github.com/anthropics/claude-code-action/commit/d721746d683d812e669ce117cebe55a85fbd9c3e\"><code>d721746</code></a>\nfix: bound image attachment downloads (<a\nhref=\"https://redirect.github.com/anthropics/claude-code-action/issues/1625\">#1625</a>)</li>\n<li>Additional commits viewable in <a\nhref=\"https://github.com/anthropics/claude-code-action/compare/be7b93b1907a4abad570368f3c74b6fe3807510b...3f854a8fb5146b39d5cbf8b57f70d80810e1366f\">compare\nview</a></li>\n</ul>\n</details>\n<br />\n\nUpdates `vmactions/freebsd-vm` from 1.5.2 to 1.5.3\n<details>\n<summary>Release notes</summary>\n<p><em>Sourced from <a\nhref=\"https://github.com/vmactions/freebsd-vm/releases\">vmactions/freebsd-vm's\nreleases</a>.</em></p>\n<blockquote>\n<h2>v1.5.3</h2>\n<p>support major release:</p>\n<pre><code>release: 15\n<p>release: 14</p>\n<p>release: 13</p>\n<p>release: 12</p>\n<p></code></pre></p>\n</blockquote>\n</details>\n<details>\n<summary>Commits</summary>\n<ul>\n<li><a\nhref=\"https://github.com/vmactions/freebsd-vm/commit/83b151f58c6047089f4c80eb5ba2039d158ce093\"><code>83b151f</code></a>\nUpdate vmactions/freebsd-vm from base-vm</li>\n<li><a\nhref=\"https://github.com/vmactions/freebsd-vm/commit/e9ea2cef1135a17c880e9ed9956c983d58421ece\"><code>e9ea2ce</code></a>\nGenerated from base-vm</li>\n<li><a\nhref=\"https://github.com/vmactions/freebsd-vm/commit/7f9ed700a9d1068fdd604600931b019737d9c783\"><code>7f9ed70</code></a>\nBump versions from upstream releases</li>\n<li><a\nhref=\"https://github.com/vmactions/freebsd-vm/commit/59e13a97c0c998e5f36c10a292ebc91115d531e7\"><code>59e13a9</code></a>\nUpdate vmactions/freebsd-vm from base-vm</li>\n<li><a\nhref=\"https://github.com/vmactions/freebsd-vm/commit/f28fc2743d290ed0b5020dca36fb2db6cc9753aa\"><code>f28fc27</code></a>\nGenerated from base-vm</li>\n<li><a\nhref=\"https://github.com/vmactions/freebsd-vm/commit/c267d2fbb461730ef00253a968e161e09e955c43\"><code>c267d2f</code></a>\nUpdate version to v1.5.2</li>\n<li><a\nhref=\"https://github.com/vmactions/freebsd-vm/commit/a56f3bab0836eed8238cd15313a0936e1e3cba4d\"><code>a56f3ba</code></a>\nUpdate vmactions/freebsd-vm from base-vm</li>\n<li><a\nhref=\"https://github.com/vmactions/freebsd-vm/commit/4a50e076570c30a595e08b5247f2df77ef296bd1\"><code>4a50e07</code></a>\nGenerated from base-vm</li>\n<li><a\nhref=\"https://github.com/vmactions/freebsd-vm/commit/ff9b16bb634ef0047e04bf18b61f9ec311f8f544\"><code>ff9b16b</code></a>\nUpdate vmactions/freebsd-vm from base-vm</li>\n<li><a\nhref=\"https://github.com/vmactions/freebsd-vm/commit/d6a581b740959e3b804b0711b8fc5200d63e9907\"><code>d6a581b</code></a>\nGenerated from base-vm</li>\n<li>Additional commits viewable in <a\nhref=\"https://github.com/vmactions/freebsd-vm/compare/77ed28d336d03fe19a3f4f7266c1d2c4714dd79d...83b151f58c6047089f4c80eb5ba2039d158ce093\">compare\nview</a></li>\n</ul>\n</details>\n<br />\n\nUpdates `docker/setup-buildx-action` from 4.2.0 to 4.3.0\n<details>\n<summary>Release notes</summary>\n<p><em>Sourced from <a\nhref=\"https://github.com/docker/setup-buildx-action/releases\">docker/setup-buildx-action's\nreleases</a>.</em></p>\n<blockquote>\n<h2>v4.3.0</h2>\n<ul>\n<li>Bump <code>@\u200bdocker/actions-toolkit</code> from 0.92.0 to 0.95.0 in\n<a\nhref=\"https://redirect.github.com/docker/setup-buildx-action/pull/595\">docker/setup-buildx-action#595</a></li>\n<li>Bump brace-expansion from 1.1.13 to 1.1.18 in <a\nhref=\"https://redirect.github.com/docker/setup-buildx-action/pull/600\">docker/setup-buildx-action#600</a></li>\n<li>Bump js-yaml from 5.2.0 to 5.3.0 in <a\nhref=\"https://redirect.github.com/docker/setup-buildx-action/pull/585\">docker/setup-buildx-action#585</a></li>\n<li>Bump postcss from 8.5.10 to 8.5.25 in <a\nhref=\"https://redirect.github.com/docker/setup-buildx-action/pull/598\">docker/setup-buildx-action#598</a></li>\n<li>Bump undici from 6.27.0 to 6.28.0 in <a\nhref=\"https://redirect.github.com/docker/setup-buildx-action/pull/601\">docker/setup-buildx-action#601</a></li>\n</ul>\n<p><strong>Full Changelog</strong>: <a\nhref=\"https://github.com/docker/setup-buildx-action/compare/v4.2.0...v4.3.0\">https://github.com/docker/setup-buildx-action/compare/v4.2.0...v4.3.0</a></p>\n</blockquote>\n</details>\n<details>\n<summary>Commits</summary>\n<ul>\n<li><a\nhref=\"https://github.com/docker/setup-buildx-action/commit/37fe631027851001ddb9b187196cc803df7f5f0e\"><code>37fe631</code></a>\nMerge pull request <a\nhref=\"https://redirect.github.com/docker/setup-buildx-action/issues/595\">#595</a>\nfrom docker/dependabot/npm_and_yarn/docker/actions-to...</li>\n<li><a\nhref=\"https://github.com/docker/setup-buildx-action/commit/b5c4f91922681cc7c58d15ab7838986951f09d19\"><code>b5c4f91</code></a>\n[dependabot skip] chore: update generated content</li>\n<li><a\nhref=\"https://github.com/docker/setup-buildx-action/commit/3e93b637c6430ba8fa896fad44d3aa6821899d63\"><code>3e93b63</code></a>\nbuild(deps): bump <code>@\u200bdocker/actions-toolkit</code> from 0.92.0 to\n0.95.0</li>\n<li><a\nhref=\"https://github.com/docker/setup-buildx-action/commit/e527031b32c86649307d5d492506855f90470604\"><code>e527031</code></a>\nMerge pull request <a\nhref=\"https://redirect.github.com/docker/setup-buildx-action/issues/600\">#600</a>\nfrom docker/dependabot/npm_and_yarn/brace-expansion-1...</li>\n<li><a\nhref=\"https://github.com/docker/setup-buildx-action/commit/c68814b33cb66f1f7538e546190d410ae557a640\"><code>c68814b</code></a>\n[dependabot skip] chore: update generated content</li>\n<li><a\nhref=\"https://github.com/docker/setup-buildx-action/commit/3f891b01bd5012a434f582800366972569aa1886\"><code>3f891b0</code></a>\nbuild(deps): bump brace-expansion from 1.1.13 to 1.1.18</li>\n<li><a\nhref=\"https://github.com/docker/setup-buildx-action/commit/787db26fcde8ddcabd49a81472318028f7113962\"><code>787db26</code></a>\nMerge pull request <a\nhref=\"https://redirect.github.com/docker/setup-buildx-action/issues/585\">#585</a>\nfrom docker/dependabot/npm_and_yarn/js-yaml-5.2.1</li>\n<li><a\nhref=\"https://github.com/docker/setup-buildx-action/commit/f7793687c711790ca336bd4934f1b1bf5f778e17\"><code>f779368</code></a>\n[dependabot skip] chore: update generated content</li>\n<li><a\nhref=\"https://github.com/docker/setup-buildx-action/commit/7d5e60413489a33d28077e11d71c668580cfaf8d\"><code>7d5e604</code></a>\nbuild(deps): bump js-yaml from 5.2.0 to 5.3.0</li>\n<li><a\nhref=\"https://github.com/docker/setup-buildx-action/commit/292c2fb3837a12d3ac2d1e47bbc5c00712bad939\"><code>292c2fb</code></a>\nMerge pull request <a\nhref=\"https://redirect.github.com/docker/setup-buildx-action/issues/590\">#590</a>\nfrom docker/dependabot/github_actions/actions/setup-n...</li>\n<li>Additional commits viewable in <a\nhref=\"https://github.com/docker/setup-buildx-action/compare/bb05f3f5519dd87d3ba754cc423b652a5edd6d2c...37fe631027851001ddb9b187196cc803df7f5f0e\">compare\nview</a></li>\n</ul>\n</details>\n<br />\n\nUpdates `docker/login-action` from 4.5.2 to 4.6.0\n<details>\n<summary>Release notes</summary>\n<p><em>Sourced from <a\nhref=\"https://github.com/docker/login-action/releases\">docker/login-action's\nreleases</a>.</em></p>\n<blockquote>\n<h2>v4.6.0</h2>\n<ul>\n<li>Harden buildx scoped config path handling by <a\nhref=\"https://github.com/crazy-max\"><code>@\u200bcrazy-max</code></a> in <a\nhref=\"https://redirect.github.com/docker/login-action/pull/1059\">docker/login-action#1059</a></li>\n<li>Bump <code>@\u200baws-sdk/client-ecr</code> and\n<code>@\u200baws-sdk/client-ecr-public</code> to 3.1095.0 in <a\nhref=\"https://redirect.github.com/docker/login-action/pull/1051\">docker/login-action#1051</a></li>\n<li>Bump js-yaml from 5.2.1 to 5.2.2 in <a\nhref=\"https://redirect.github.com/docker/login-action/pull/1057\">docker/login-action#1057</a></li>\n<li>Bump postcss from 8.5.10 to 8.5.22 in <a\nhref=\"https://redirect.github.com/docker/login-action/pull/1056\">docker/login-action#1056</a></li>\n</ul>\n<p><strong>Full Changelog</strong>: <a\nhref=\"https://github.com/docker/login-action/compare/v4.5.2...v4.6.0\">https://github.com/docker/login-action/compare/v4.5.2...v4.6.0</a></p>\n</blockquote>\n</details>\n<details>\n<summary>Commits</summary>\n<ul>\n<li><a\nhref=\"https://github.com/docker/login-action/commit/dbcb813823bdd20940b903addbd779551569679f\"><code>dbcb813</code></a>\nMerge pull request <a\nhref=\"https://redirect.github.com/docker/login-action/issues/1051\">#1051</a>\nfrom docker/dependabot/npm_and_yarn/aws-sdk-dependen...</li>\n<li><a\nhref=\"https://github.com/docker/login-action/commit/5bcb015ee6ec720ecdeaef2dc1164122e9b209fc\"><code>5bcb015</code></a>\n[dependabot skip] chore: update generated content</li>\n<li><a\nhref=\"https://github.com/docker/login-action/commit/b30b2f2d3196c1714318ba0c3c3bec211d949752\"><code>b30b2f2</code></a>\nbuild(deps): bump the aws-sdk-dependencies group across 1 directory with\n2 up...</li>\n<li><a\nhref=\"https://github.com/docker/login-action/commit/9087f1e6d666fe0292409e3c819680c18526e108\"><code>9087f1e</code></a>\nMerge pull request <a\nhref=\"https://redirect.github.com/docker/login-action/issues/1057\">#1057</a>\nfrom docker/dependabot/npm_and_yarn/js-yaml-5.2.2</li>\n<li><a\nhref=\"https://github.com/docker/login-action/commit/0009830ea169ca16c24c0ea4cac1c325bfa3aee4\"><code>0009830</code></a>\n[dependabot skip] chore: update generated content</li>\n<li><a\nhref=\"https://github.com/docker/login-action/commit/23255232d3e43c8f0052d9a0dba82a515a88ce92\"><code>2325523</code></a>\nbuild(deps): bump js-yaml from 5.2.1 to 5.2.2</li>\n<li><a\nhref=\"https://github.com/docker/login-action/commit/4ec1d4a769e8b05a89a7396551dc38b329211688\"><code>4ec1d4a</code></a>\nMerge pull request <a\nhref=\"https://redirect.github.com/docker/login-action/issues/1056\">#1056</a>\nfrom docker/dependabot/npm_and_yarn/postcss-8.5.22</li>\n<li><a\nhref=\"https://github.com/docker/login-action/commit/5fc99ba47bca274c5a499688f71c7ea79c0ea1b3\"><code>5fc99ba</code></a>\nMerge pull request <a\nhref=\"https://redirect.github.com/docker/login-action/issues/1053\">#1053</a>\nfrom docker/dependabot/github_actions/aws-actions/co...</li>\n<li><a\nhref=\"https://github.com/docker/login-action/commit/e512bd59d16c53d79ea5c0f0e345fe554453c4bb\"><code>e512bd5</code></a>\nMerge pull request <a\nhref=\"https://redirect.github.com/docker/login-action/issues/1052\">#1052</a>\nfrom docker/dependabot/github_actions/codeql-actions...</li>\n<li><a\nhref=\"https://github.com/docker/login-action/commit/a146c91b8f371700d323bae808af7cbdc2766ed5\"><code>a146c91</code></a>\nMerge pull request <a\nhref=\"https://redirect.github.com/docker/login-action/issues/1059\">#1059</a>\nfrom crazy-max/harden-buildx-scope-paths</li>\n<li>Additional commits viewable in <a\nhref=\"https://github.com/docker/login-action/compare/371161bbe7024a29a25c5e19bfcbc0804fe9ad2c...dbcb813823bdd20940b903addbd779551569679f\">compare\nview</a></li>\n</ul>\n</details>\n<br />\n\n\nDependabot will resolve any conflicts with this PR as long as you don't\nalter it yourself. You can also trigger a rebase manually by commenting\n`@dependabot rebase`.\n\n[//]: # (dependabot-automerge-start)\n[//]: # (dependabot-automerge-end)\n\n---\n\n<details>\n<summary>Dependabot commands and options</summary>\n<br />\n\nYou can trigger Dependabot actions by commenting on this PR:\n- `@dependabot rebase` will rebase this PR\n- `@dependabot recreate` will recreate this PR, overwriting any edits\nthat have been made to it\n- `@dependabot show <dependency name> ignore conditions` will show all\nof the ignore conditions of the specified dependency\n- `@dependabot ignore <dependency name> major version` will close this\ngroup update PR and stop Dependabot creating any more for the specific\ndependency's major version (unless you unignore this specific\ndependency's major version or upgrade to it yourself)\n- `@dependabot ignore <dependency name> minor version` will close this\ngroup update PR and stop Dependabot creating any more for the specific\ndependency's minor version (unless you unignore this specific\ndependency's minor version or upgrade to it yourself)\n- `@dependabot ignore <dependency name>` will close this group update PR\nand stop Dependabot creating any more for the specific dependency\n(unless you unignore this specific dependency or upgrade to it yourself)\n- `@dependabot unignore <dependency name>` will remove all of the ignore\nconditions of the specified dependency\n- `@dependabot unignore <dependency name> <ignore condition>` will\nremove the ignore condition of the specified dependency and ignore\nconditions\n\n\n</details>\n\nSigned-off-by: dependabot[bot] <support@github.com>\nCo-authored-by: dependabot[bot] <49699333+dependabot[bot]@users.noreply.github.com>"
    },
    "CVE-2024-28182": {
      "a4971c6d09b34aa7446eb7d9f0e0ff8c685bb625": "node:http2: cap CONTINUATION frames per header block (CVE-2024-28182)\n\nThe inbound engine bounded a header block under reassembly only by its\nbyte size, so a peer sending HEADERS without END_HEADERS followed by\nzero-length CONTINUATION frames could hold the session in header-block\nstate indefinitely: nothing ever errored, the request stayed pending, and\nthe receiver spent a core parsing 9-byte frames. This is the bug nghttp2\nfixed as CVE-2024-28182; node errors after the 8th CONTINUATION.\n\nCount CONTINUATION frames on the in-flight block record and fail the\nconnection once a block exceeds nghttp2's NGHTTP2_DEFAULT_MAX_CONTINUATIONS\n(8), the same way node does: GOAWAY(INTERNAL_ERROR) on the wire and an\nNghttpError with errno -905 (\"Too many CONTINUATION frames following a\nHEADER frame\") on the session. The counter lives on HeaderBlockInFlight,\nso every HEADERS or PUSH_PROMISE that opens a block starts from zero and\nthe budget is per block, not per session. Applies to clients and servers\nalike since they share the engine.\n\nTests pin the exact boundary on both sides (8 accepted, the 9th rejected\neven when it carries END_HEADERS), the per-block reset (response and\ntrailers each using the full budget on one stream), the PUSH_PROMISE\nopener, and the GOAWAY error code a server emits.",
      "a5c86aec746ce69fb480d37a455640d4fd2cc475": "fetch(h2): cap CONTINUATION frames per header block (CVE-2024-28182) (#36411)\n\n### Problem\n- A hostile HTTP/2 origin can pin a `fetch()` forever: it answers with\n`HEADERS` without `END_HEADERS`, then streams zero-length `CONTINUATION`\nframes. The promise never settles and the client burns CPU parsing\n9-byte frame headers (CVE-2024-28182 class; the `node:http2` side is\n#36410).\n- `src/http/h2_client/dispatch.rs`, `FT_CONTINUATION` arm: the only\nbound on a header block is `LOCAL_MAX_HEADER_LIST_SIZE` on accumulated\nbytes. An empty payload never advances that, so nothing ends the\nsequence.\n\n### Fix\n- Add `continuation_count: u8` to `ClientSession`, zeroed at the top of\nthe `FT_HEADERS` arm and incremented in `FT_CONTINUATION`; past\n`LOCAL_MAX_CONTINUATIONS` (8) the connection fails with\n`HTTP2EnhanceYourCalm`, which `error_code_for` already maps to\n`ENHANCE_YOUR_CALM` on the GOAWAY.\n- The value and behaviour match nghttp2\n(`NGHTTP2_DEFAULT_MAX_CONTINUATIONS`) and node 22: 8 frames accepted,\nthe 9th rejected even when it carries `END_HEADERS`.\n- One reset at the top of `FT_HEADERS` is sufficient because\n`dispatch_frame` already rejects any non-`CONTINUATION` frame while a\nblock is open, so every `HEADERS` that reaches the arm starts a new\nblock; this covers the tracked-stream and orphan (already-RST'd stream)\nbranches alike.\n- 8 x 16 KiB (default `MAX_FRAME_SIZE`) is 128 KiB of compressed\nheaders, below the existing 256 KiB byte cap, so the pre-existing\n`CONTINUATION flood is bounded` test now sees `HTTP2EnhanceYourCalm`;\nits assertion is updated.\n- Verified in `test/js/web/fetch/fetch-http2-adversarial.test.ts` (20/20\nwith the fix; 3 fail on the released binary):\n- `zero-length CONTINUATION flood is rejected`: the repro, times out\nwithout the fix.\n- `header block split across the full CONTINUATION budget is accepted`:\n8 frames deliver a 200.\n- `9th CONTINUATION is rejected even if it ends the block`: pins the cap\nat exactly 8 and the comparison at `>`.\n- `CONTINUATION budget resets for each header block on a stream`: a 103\nblock and the final 200 block on one stream each use all 8 frames; fails\nwith the reset removed (checked by removing it and rebuilding).\n- `fetch-http2-client.test.ts` still 62/62.\n\n### Background\n- An HTTP/2 header block starts in a `HEADERS` frame and, when\n`END_HEADERS` is not set, continues in `CONTINUATION` frames on the same\nstream; until one arrives with `END_HEADERS`, no other frame is allowed\non the connection (RFC 9113 4.3, 6.10). A response may carry several\nblocks on one stream: 1xx informational blocks, the final response, and\ntrailers.\n- HPACK state is connection-wide, so the fetch client also assembles and\ndecodes blocks for streams it has already reset (the \"orphan\" path in\n`dispatch.rs`) to keep the decoder in sync; the cap has to apply there\ntoo.\n- `ENHANCE_YOUR_CALM` is the HTTP/2 error code for \"peer is generating\nexcessive load\"; `HTTP2EnhanceYourCalm` is the `fetch()` error the\nclient already uses for its other flood limits.\n\n<!-- robobun:evidence:begin -->\n\n---\n\n**no test proof** \u00b7 iteration 5 \u00b7 Platform-specific test(s) that do not\nrun on this machine. Deferring to CI, which covers all platforms:\ntest/js/web/fetch/fetch-http2-adversarial.test.ts\n\n<!-- robobun:evidence:end -->"
    },
    "CVE-2024-36139": {
      "c0caf8a6949e58146e6dfcf4c8de1c93b54f50a1": "Rewrite node:path natively on JSString storage; match Node.js v26 exactly\n\nnode:path is now a line-for-line port of Node's lib/path.js in Path.cpp that\nreads each argument's Latin-1 or UTF-16 backing store directly instead of\nconverting every input to UTF-8 and every result back.\n\n- Results that are slices of an input (basename, dirname, extname, parse) are\n  returned as substrings sharing the input's buffer; results that must be\n  assembled (join, resolve, normalize, relative) are built once in a stack\n  buffer of the inputs' width, and an unchanged input is returned as-is.\n- isAbsolute, posix toNamespacedPath and format live in path.ts, where the JIT\n  can inline them; the .bind() wrappers around every export are gone.\n- Behaviour now matches Node.js v26.3.0: Windows reserved device names\n  (CON/COM1/...) in normalize and join, the CVE-2024-36139 traversal fixes,\n  \\\\.\\ and \\\\?\\ device roots in resolve/toNamespacedPath, Unicode-aware\n  case folding in win32.relative, Node's argument names and error messages,\n  and Node's property order and function names on the module object. The\n  upstream test-path-*.js files are re-synced verbatim; the one remaining\n  deviation (path.resolve does not call a monkey-patched process.cwd) keeps\n  its explanatory comment.\n- test/js/node/path/node-path-parity.test.ts checks 1540 generated inputs\n  against outputs recorded from Node, plus long inputs and many-argument calls.\n- bench/snippets/path.mjs covers every function on posix and win32 with\n  Latin-1, UTF-16 and 2000-character inputs. On an M-series Mac the geometric\n  mean is 3.5x faster than the previous implementation (no case slower than\n  1.27x) and 2.2x faster than Node.\n\nThe old Rust implementation (src/runtime/node/path.rs), its per-VM scratch\nbuffers in rare_data, and the Bun__Path__* / Bun__Node__Path_joinWTF exports\nare removed; the two C++ callers use Bun::NodePath::dirname/join directly.",
      "955457f9d1182c7090b9b96084a7e76914e800fd": "node:path: port Node's drive and reserved device name guards to win32 normalize and join\n\npath.win32.normalize now prefixes a relative result with .\\ when it would\notherwise look like a drive (c:) or end a segment with a colon (CON:), and\nrecognizes reserved DOS device names (CON, PRN, AUX, NUL, COM1-9, LPT1-9 and\nsuperscript variants) as device roots. path.win32.join skips normalization\nwhen a joined part is a reserved device name, leaving .. segments\nuncollapsed. This matches Node's behavior since the CVE-2024-36139 fix.\n\nFixes #35610",
      "4da3f3bb53a042be1331b360aee87554f8d94f86": "path: port Node v26 win32 device-root and reserved-name handling\n\npath.win32 was still on the Node v21.6.1 algorithm and was missing three\nupstream changes.\n\nDevice roots. \\\\.\\X and \\\\?\\X are device roots, not UNC shares, so\nresolve() and normalize() must not append a trailing separator:\npath.win32.resolve('\\\\\\\\?\\\\foo') returned '\\\\\\\\?\\\\foo\\\\'\nwhere Node returns '\\\\\\\\?\\\\foo'. toNamespacedPath inherited the same\nextra separator through resolve.\n\nCVE-2024-36139. path.win32.normalize('test/../C:/Windows') returned\n'C:\\\\Windows' - a relative path normalizing into a different drive root.\nNode returns '.\\\\C:\\\\Windows'. normalize now applies the same guard: when\nthe input is relative, has no device, and contains a colon, the result is\nprefixed with '.\\\\'.\n\nReserved device names. CON, PRN, AUX, NUL, COM1-9 and LPT1-9 (including the\nsuperscript COM\\u00b9 spellings) are recognised in normalize's root match and\nin join, which skips normalization entirely when any joined segment is a\nreserved name so that '..' after 'CON:' is not resolved away.\n\nRestores four upstream tests to verbatim v26.3.0: test-path-normalize,\ntest-path-makelong, test-path-join, test-url-urltooptions.\n\nurlToHttpOptions now rejects non-objects with ERR_INVALID_ARG_TYPE, matching\nNode's validateObject(url, 'url', kValidateObjectAllowObjects).\n\nThe Bun-owned to-namespaced-path test asserted the old trailing separator;\nits two expectations now match real Node output.",
      "ed1812b7f017f4860ebd1a6434f24e48cc83ad83": "path: normalize reserved Windows device names; domain: node's thrown-error ordering\n\npath.win32.normalize() did not know about reserved DOS device names, so\n`CON:` came back as `CON:` instead of `.\\CON:.`, and a relative path with a\ncolon such as `foo:/bar` was not made explicitly relative (CVE-2024-36139).\nPort the v26.3.0 algorithm: reserved names form their own root, `\\\\.\\` and\n`\\\\?\\` device roots are recognized (including `\\\\?\\COM1:`), and both the\nreserved and colon cases prefix the result with `.\\`. The reserved-name list\ncovers the superscript COM\u00b9/\u00b2/\u00b3 and LPT\u00b9/\u00b2/\u00b3 spellings in both the UTF-8 and\nUTF-16 representations.\n\nnode:domain routed a thrown error straight to the 'error' handler while the\ndomain was still active. Node exits the domain first and clears the stack\nafterwards, so `process.domain` is undefined inside the handler and null once\nthe tick ends.\n\nutil.debuglog() ignored its documented second argument and exposed no\n`enabled` property; both now follow lib/internal/util/debuglog.js.\n\nVendored from Node v26.3.0, each verified with a canary (appending a throw to\na copy must make the run fail):\n  test-pipe-unref.js\n  test-stdout-close-catch.js\n  test-timers-reset-process-domain-on-throw.js\n  test-path-win32-normalize-device-names.js (runs on the Windows lane; the\n    table it asserts is covered on every platform by normalize.test.js)"
    },
    "CVE-2016-5134": {
      "1695d67790e4eddab98ee68bb00fad253c084bbb": "PAC hardening: sanitize URL, bound WinHTTP timeouts, exclude IPv6 from <local>\n\n- Pass only scheme://host/ to WinHttpGetProxyForUrl so a rogue WPAD PAC\n  cannot receive userinfo/path/query (CVE-2016-5134 class).\n- WinHttpSetTimeouts(5s) on the auto-proxy session so an unreachable PAC\n  URL cannot freeze the calling thread for the 60s WinHTTP default.\n- Factor the <local> check into is_simple_hostname() shared between\n  SystemProxy::resolve and ProxySettings::resolve; exclude ':' so IPv6\n  literals are not treated as intranet names."
    },
    "CVE-2021-22939": {
      "c3fcd1ad4a60e6ee3092fb804f2f59cc1075dc40": "node:tls: only an explicit false for rejectUnauthorized disables verification\n\nNode's CVE-2021-22939 fix checks `rejectUnauthorized !== false`: every\nother value, including null, keeps certificate verification on. Bun's\nclient connect paths stored the raw option value and then checked it\nfor truthiness, so rejectUnauthorized: null silently bypassed\nverification where Node rejects. Normalize to a real boolean at each\nstore site (the TLSSocket constructor, Server setSecureContext, and\nthe three Socket.prototype.connect TLS paths). The bindgen layer\nalready rejects 0/\"\"/1 with ERR_INVALID_ARG_TYPE, so null was the\nonly non-boolean that reached these sites.\n\nAlso: declare `session` with let before the try in the resumption\ntest instead of hoisting with var."
    },
    "CVE-2021-23222": {
      "9a4d8ab835fe65ea013745e9c5c8233f16ed085d": "Bun.SQL(postgres): send plaintext startup when sslmode=prefer and server declines TLS\n\nWhen the server answers the SSLRequest with 'N', the prefer branch only\nset TlsStatus::SslNotAvailable and looped back, never writing a\nStartupMessage. The connection then idled until connectionTimeout. This\nmeant any non-SSL Postgres (the docker default) was unreachable via a\nstandard libpq ?sslmode=prefer URL.\n\nCall start() to send the plaintext StartupMessage on the same socket\nand return Ok(()) so any bytes that arrive alongside the 'N' are\ndiscarded rather than dispatched (the libpq CVE-2021-23222 shape).\n\nUpdate the tls_ctx leak test's mock server to answer the\nStartupMessage in a second round trip instead of packing AuthOk after\n'N' in one write, which depended on the old continue behaviour."
    },
    "CVE-2024-27980": {
      "2c21d71ae746e16d465a7171a7595b9eec88132c": "debug-adapter: pass shell:true when resolved path is a batch file\n\nThe previous commit resolved bare command names via PATH/PATHEXT but\nstill spawned the resolved path directly. That fixes the bare-name\nlookup but not the CVE-2024-27980 hardening: Node's ProcessWrap::Spawn\nrejects any options.file ending in .bat/.cmd with EINVAL regardless of\nwhether the path is absolute. The check is suffix-based \u2014 see\nnode:src/util-inl.h IsWindowsBatchFile.\n\nFor the reported npm-wrapper scenario \u2014 where bun.cmd is the only\ndiscoverable entry on PATH \u2014 the resolved path is C:\\...\\bun.cmd and\nspawn still fails.\n\nChange resolveCommand to return { command, useShell } and have the\nadapter pass shell: useShell to spawn. With shell:true Node rewrites\nthe file to cmd.exe and wraps the invocation as /d /s /c \"...\", so\nthe batch-file check in the C++ layer sees cmd.exe and succeeds.\n\nExpanded the regression test to assert useShell is set correctly for\nbare names, absolute paths, and reversed PATHEXT orderings.",
      "dafbb6b7f0003f76f568cac1fc0319766bf8bac6": "debug-adapter: resolve command via PATH/PATHEXT on Windows\n\nchild_process.spawn on Windows does not apply PATHEXT resolution, and\npost-CVE-2024-27980 it refuses to launch .cmd/.bat files unless shell:\ntrue is set. Bun installed via the npm wrapper on Windows lands on PATH\nas bun.cmd, so the debug adapter's default spawn(\"bun\", ...) call fails\nwith EINVAL.\n\nResolve bare command names to an absolute .exe/.cmd path before\nspawning. Avoids the shell: true escaping surface and keeps ENOENT\nerror messages intact when the command is genuinely missing.\n\nFixes #29636"
    },
    "CVE-2023-4863": {
      "ed75c88a5ca1b54345083721a0dd185ed61dedc5": "Bun.Image \u2014 Sharp-shaped image pipeline (jpeg/png/webp, resize, rotate, modulate) (#30032)\n\n## What\n\n`Bun.Image`: decode \u2192 transform \u2192 encode for JPEG/PNG/WebP/GIF/BMP (+\nHEIC/AVIF/TIFF on macOS/Windows) with no native module install.\nStatically links the reference codecs and a highway-SIMD resize kernel;\nsystem backends (ImageIO+vImage, WIC) for the OS-native formats.\n\n```ts\nawait Bun.file(\"photo.jpg\")\n  .image()\n  .resize(1024, 1024, { fit: \"inside\" })\n  .rotate(90)\n  .webp({ quality: 85 })\n  .write(\"thumb.webp\");\n\nreturn new Response(new Bun.Image(upload).resize(200).jpeg());\n```\n\n## Benchmarks vs sharp 0.34.5\n\nCI release build (`bunx bun-pr 30032`), 50 iters, linux/x64,\n`sharp.concurrency(1)`:\n\n| op | Bun.Image | sharp | speedup | \u0394RSS bun | \u0394RSS sharp |\n|---|---:|---:|---:|---:|---:|\n| `metadata()` | **0.004 ms** | 0.28 ms | **70\u00d7** | 0 MB | 2.5 MB |\n| 1080p PNG \u2192 resize 400\u00d7400 inside \u2192 jpeg q80 | **28.6 ms** | 39.5 ms |\n**1.38\u00d7** | 0 MB | 8.9 MB |\n| 1080p PNG \u2192 resize 800\u00d7600 \u2192 webp q80 | **82.7 ms** | 110.1 ms |\n**1.33\u00d7** | 0 MB | 2.5 MB |\n| 1080p PNG \u2192 jpeg q80 (no resize) | 34.3 ms | 33.8 ms | 0.98\u00d7 | 8.4 MB\n| 12 MB |\n| 1080p JPEG \u2192 resize 400\u00d7400 inside \u2192 jpeg | **10.2 ms** | 12.5 ms |\n**1.22\u00d7** | 0 MB | 0 MB |\n| 1080p JPEG \u2192 resize 200\u00d7200 inside \u2192 webp | **10.6 ms** | 11.8 ms |\n1.12\u00d7 | 0 MB | 0 MB |\n| 1080p JPEG \u2192 webp q80 | **175 ms** | 208 ms | **1.19\u00d7** | 1.4 MB | 7\nMB |\n| **4K** JPEG \u2192 800\u00d7450 \u2192 jpeg | **35.8 ms** | 45.5 ms | **1.27\u00d7** | 0.5\nMB | 0 MB |\n| **4K** JPEG \u2192 1920\u00d71080 \u2192 jpeg | **57.2 ms** | 69.9 ms | **1.22\u00d7** |\n12 MB | 21 MB |\n| **12MP** JPEG \u2192 400\u00d7300 \u2192 jpeg | **34.3 ms** | 39.4 ms | **1.15\u00d7** |\n0.5 MB | 0 MB |\n| **12MP** JPEG \u2192 1024\u00d7768 \u2192 webp | **138 ms** | 165 ms | **1.20\u00d7** |\n3.1 MB | 8.4 MB |\n\n(x64 with libjpeg-turbo C-only \u2014 no NASM yet; arm64 has NEON wired so\nJPEG is better still. Reproduce: `cd bench/image && bun install && bunx\nbun-pr 30032 && bun-30032 bench-resize.mjs --sharp`.)\n\nWhy it's faster: i16 fixed-point resize (`ReorderWidenMulAccumulate` \u2192\nPMADDWD/SMLAL, 2\u00d7 lanes vs f32); JPEG decodes at the smallest M/8 IDCT\nscale \u2265 target; `metadata()` is a sync header read; constructor borrows\nthe ArrayBuffer (`pin()` per-task) instead of memcpy; output bytes go\nstraight to JS via the codec's own free, no dupe; resize scratch is one\ncaller-allocated arena, zero `malloc` in the kernel.\n\n[Visual side-by-side\ngist](https://gist.github.com/Jarred-Sumner/8d91b281650b9d75028afb3799c1c3f0)\n\u2014 12 real photos \u00d7 4 resize configs \u00d7 3 formats, sharp left / Bun.Image\nright.\n\n## Surface\n\n- **Input**: path string \u00b7 ArrayBuffer/TypedArray (borrowed zero-copy,\npinned per-task; resizable rejected) \u00b7 Blob/BunFile/S3File \u00b7 `data:` URL\n\u00b7 `Bun.file(\u2026).image()` / `Blob#image()`\n- **Chainable** (fixed-slot `Pipeline`; setters overwrite, not stack):\n`.resize(w, h?, {filter, fit, withoutEnlargement})` \u00b7\n`.rotate(90|180|270)` \u00b7 `.flip()` \u00b7 `.flop()` \u00b7 `.modulate({brightness,\nsaturation})` \u00b7 `.jpeg({quality, progressive})` \u00b7\n`.png({compressionLevel, palette, colors, dither})` \u00b7 `.webp({quality,\nlossless})` \u00b7 `.heic({quality})` \u00b7 `.avif({quality})`\n- **Filters** (full Sharp set + MKS): `nearest` `box` `bilinear` `cubic`\n`mitchell` `lanczos2` `lanczos3` `mks2013` `mks2021`\n- **Terminals** (off-thread except metadata): `.bytes()` \u00b7 `.buffer()` \u00b7\n`.blob()` \u00b7 `.toBase64()` \u00b7 `.dataurl()` \u00b7 `.placeholder()` (thumbhash)\n\u00b7 `.metadata()` \u00b7 `.write(dest)`\n- **Body init**: `new Response(image)` / `new Request({body: image})`\nencode and set `Content-Type`\n- **Options**: `{maxPixels}` decompression-bomb guard (header-checked\npre-alloc, all codecs + system backends + resize output), `{autoOrient}`\nEXIF Orientation, `{backend: \"auto\"|\"system\"|\"bun\"}`\n\n## Dependencies\n\n| lib | version | license | build | CVE status |\n|---|---|---|---|---|\n| libjpeg-turbo | 3.1.4 | BSD-3/IJG/zlib | DirectBuild, 8-bit; arm64\nNEON intrinsics on, x64 C-only | clean (rdbmp.c stubbed) |\n| libspng | 0.7.4 | BSD-2 | DirectBuild, single TU, vendored zlib | zero\never assigned (OSS-Fuzz since inception) |\n| libwebp | 1.6.0 | BSD-3 | DirectBuild, full enc/dec/dsp/sharpyuv,\nper-file -mavx2/-msse4.1 | clean (CVE-2023-4863 fixed in 1.3.2) |\n\n`LICENSE.md` updated. `src/image/README.md` documents layout +\nhow-to-extend.\n\n## System backends\n\n| | macOS (ImageIO + vImage) | Windows (WIC) | Linux |\n|---|---|---|---|\n| JPEG/PNG/WebP | static codecs (output identical across platforms) |\nstatic codecs | static |\n| GIF/BMP | static `codec_gif.zig`/`codec_bmp.zig` | static | static |\n| **TIFF** | \u2705 decode | \u2705 decode | `UnsupportedOnPlatform` |\n| **HEIC** | \u2705 dec+enc 10.13+ | \u2705 dec+enc (HEIF Image Extensions;\nquality wired via IPropertyBag2) | `UnsupportedOnPlatform` |\n| **AVIF** | \u2705 dec; enc on Apple Silicon only (Intel has no AV1 encoder)\n| \u2705 dec+enc (AV1 Video Extension) | `UnsupportedOnPlatform` |\n\nCoreGraphics dispatch lives entirely in C++\n(`image_coregraphics_shim.cpp`); decode goes through\n`vImageBuffer_InitWithCGImage` for straight-alpha (no premul roundtrip),\nwrapped in an explicit autoreleasepool per WorkPool call. WIC encode\nthreads `ImageQuality`/`HeifCompressionMethod` through a C++ shim so the\nSDK's own VARIANT layout is authoritative. Symbol resolution stays lazy\n(dlsym/CoCreateInstance), zero startup cost.\n\n## Tests \u2014 204 across four suites\n\n`image.test.ts` (~80) \u2014 happy path: per-format round-trip, all 9\nfilters, rotate/flip, fit:inside/withoutEnlargement, EXIF auto-orient,\nmodulate, palette+dither PNG, compressionLevel,\n`.buffer()/.blob()/.toBase64()/.write()`, `new Response(image)`,\n`Bun.file().image()`, GIF/BMP/TIFF, HEIC/AVIF sniff + per-platform\nencode.\n\n`image-adversarial.test.ts` (~58) \u2014 format confusion (lying magic bytes,\npolyglots), truncation sweep at every byte offset, single-byte XOR fuzz\nat every offset, hostile header dimensions (0\u00d70, 2\u00b3\u00b9-1, 2\u00b3\u00b2-1), zlib\nbombs in IDAT, hostile EXIF IFD0, hostile BMP BITFIELDS / biSize wrap,\n**GIFLIB-CVE-2016-3177-style LZW** (code > avail), 255-byte GIF\nsub-block, lossless roundtrip on LCG noise, rotate\u2074=id, leak plateau\nunder repetition, throwing Proxy on every option object, re-entrant\ngetters, detached/resizable ArrayBuffer, SharedArrayBuffer, `data:` URL,\n100 concurrent terminals.\n\n`image-kernels.test.ts` (~38) \u2014 algorithm pins: flat-field DC gain (all\n9 filters), 1\u00d71 upscale, same-size identity, mitchell monotone over a\nstep, **nearest downscale stays discrete**, impulse spread ordering,\nanisotropy, FS dithered ramp tracks source mean, FS determinism, 1\u00d7N/N\u00d71\ndither edge cases.\n\n`image-vs-sharp.test.ts` (29) \u2014 pixel-level cross-reference against\npre-baked Sharp/libvips output. mitchell/bilinear/cubic/lanczos2:\nMAE<0.5 max\u22645 (centre/support math correct). lanczos3/mks*: MAE<1.0\nmax\u226416 (libvips quantises offsets to 64 bins; we compute exact per-pixel\nweights \u2014 both correct). Hard-edge sources omitted by design (those\nmeasure phase/pre-shrink, not kernel).\n\n## Known follow-ups\n\n- x64 libjpeg-turbo NASM SIMD \u2014 `nasm` not in build images; arm64 NEON\nis on.\n- `new Response(image)` encodes on the calling thread; a `.Locked` body\nwould stream from the WorkPool.\n- Integer pre-shrink for non-JPEG would close the remaining gap on huge\nPNG/WebP downscales.\n\n---------\n\nCo-authored-by: autofix-ci[bot] <114827586+autofix-ci[bot]@users.noreply.github.com>"
    },
    "CVE-2016-3177": {
      "ed75c88a5ca1b54345083721a0dd185ed61dedc5": "Bun.Image \u2014 Sharp-shaped image pipeline (jpeg/png/webp, resize, rotate, modulate) (#30032)\n\n## What\n\n`Bun.Image`: decode \u2192 transform \u2192 encode for JPEG/PNG/WebP/GIF/BMP (+\nHEIC/AVIF/TIFF on macOS/Windows) with no native module install.\nStatically links the reference codecs and a highway-SIMD resize kernel;\nsystem backends (ImageIO+vImage, WIC) for the OS-native formats.\n\n```ts\nawait Bun.file(\"photo.jpg\")\n  .image()\n  .resize(1024, 1024, { fit: \"inside\" })\n  .rotate(90)\n  .webp({ quality: 85 })\n  .write(\"thumb.webp\");\n\nreturn new Response(new Bun.Image(upload).resize(200).jpeg());\n```\n\n## Benchmarks vs sharp 0.34.5\n\nCI release build (`bunx bun-pr 30032`), 50 iters, linux/x64,\n`sharp.concurrency(1)`:\n\n| op | Bun.Image | sharp | speedup | \u0394RSS bun | \u0394RSS sharp |\n|---|---:|---:|---:|---:|---:|\n| `metadata()` | **0.004 ms** | 0.28 ms | **70\u00d7** | 0 MB | 2.5 MB |\n| 1080p PNG \u2192 resize 400\u00d7400 inside \u2192 jpeg q80 | **28.6 ms** | 39.5 ms |\n**1.38\u00d7** | 0 MB | 8.9 MB |\n| 1080p PNG \u2192 resize 800\u00d7600 \u2192 webp q80 | **82.7 ms** | 110.1 ms |\n**1.33\u00d7** | 0 MB | 2.5 MB |\n| 1080p PNG \u2192 jpeg q80 (no resize) | 34.3 ms | 33.8 ms | 0.98\u00d7 | 8.4 MB\n| 12 MB |\n| 1080p JPEG \u2192 resize 400\u00d7400 inside \u2192 jpeg | **10.2 ms** | 12.5 ms |\n**1.22\u00d7** | 0 MB | 0 MB |\n| 1080p JPEG \u2192 resize 200\u00d7200 inside \u2192 webp | **10.6 ms** | 11.8 ms |\n1.12\u00d7 | 0 MB | 0 MB |\n| 1080p JPEG \u2192 webp q80 | **175 ms** | 208 ms | **1.19\u00d7** | 1.4 MB | 7\nMB |\n| **4K** JPEG \u2192 800\u00d7450 \u2192 jpeg | **35.8 ms** | 45.5 ms | **1.27\u00d7** | 0.5\nMB | 0 MB |\n| **4K** JPEG \u2192 1920\u00d71080 \u2192 jpeg | **57.2 ms** | 69.9 ms | **1.22\u00d7** |\n12 MB | 21 MB |\n| **12MP** JPEG \u2192 400\u00d7300 \u2192 jpeg | **34.3 ms** | 39.4 ms | **1.15\u00d7** |\n0.5 MB | 0 MB |\n| **12MP** JPEG \u2192 1024\u00d7768 \u2192 webp | **138 ms** | 165 ms | **1.20\u00d7** |\n3.1 MB | 8.4 MB |\n\n(x64 with libjpeg-turbo C-only \u2014 no NASM yet; arm64 has NEON wired so\nJPEG is better still. Reproduce: `cd bench/image && bun install && bunx\nbun-pr 30032 && bun-30032 bench-resize.mjs --sharp`.)\n\nWhy it's faster: i16 fixed-point resize (`ReorderWidenMulAccumulate` \u2192\nPMADDWD/SMLAL, 2\u00d7 lanes vs f32); JPEG decodes at the smallest M/8 IDCT\nscale \u2265 target; `metadata()` is a sync header read; constructor borrows\nthe ArrayBuffer (`pin()` per-task) instead of memcpy; output bytes go\nstraight to JS via the codec's own free, no dupe; resize scratch is one\ncaller-allocated arena, zero `malloc` in the kernel.\n\n[Visual side-by-side\ngist](https://gist.github.com/Jarred-Sumner/8d91b281650b9d75028afb3799c1c3f0)\n\u2014 12 real photos \u00d7 4 resize configs \u00d7 3 formats, sharp left / Bun.Image\nright.\n\n## Surface\n\n- **Input**: path string \u00b7 ArrayBuffer/TypedArray (borrowed zero-copy,\npinned per-task; resizable rejected) \u00b7 Blob/BunFile/S3File \u00b7 `data:` URL\n\u00b7 `Bun.file(\u2026).image()` / `Blob#image()`\n- **Chainable** (fixed-slot `Pipeline`; setters overwrite, not stack):\n`.resize(w, h?, {filter, fit, withoutEnlargement})` \u00b7\n`.rotate(90|180|270)` \u00b7 `.flip()` \u00b7 `.flop()` \u00b7 `.modulate({brightness,\nsaturation})` \u00b7 `.jpeg({quality, progressive})` \u00b7\n`.png({compressionLevel, palette, colors, dither})` \u00b7 `.webp({quality,\nlossless})` \u00b7 `.heic({quality})` \u00b7 `.avif({quality})`\n- **Filters** (full Sharp set + MKS): `nearest` `box` `bilinear` `cubic`\n`mitchell` `lanczos2` `lanczos3` `mks2013` `mks2021`\n- **Terminals** (off-thread except metadata): `.bytes()` \u00b7 `.buffer()` \u00b7\n`.blob()` \u00b7 `.toBase64()` \u00b7 `.dataurl()` \u00b7 `.placeholder()` (thumbhash)\n\u00b7 `.metadata()` \u00b7 `.write(dest)`\n- **Body init**: `new Response(image)` / `new Request({body: image})`\nencode and set `Content-Type`\n- **Options**: `{maxPixels}` decompression-bomb guard (header-checked\npre-alloc, all codecs + system backends + resize output), `{autoOrient}`\nEXIF Orientation, `{backend: \"auto\"|\"system\"|\"bun\"}`\n\n## Dependencies\n\n| lib | version | license | build | CVE status |\n|---|---|---|---|---|\n| libjpeg-turbo | 3.1.4 | BSD-3/IJG/zlib | DirectBuild, 8-bit; arm64\nNEON intrinsics on, x64 C-only | clean (rdbmp.c stubbed) |\n| libspng | 0.7.4 | BSD-2 | DirectBuild, single TU, vendored zlib | zero\never assigned (OSS-Fuzz since inception) |\n| libwebp | 1.6.0 | BSD-3 | DirectBuild, full enc/dec/dsp/sharpyuv,\nper-file -mavx2/-msse4.1 | clean (CVE-2023-4863 fixed in 1.3.2) |\n\n`LICENSE.md` updated. `src/image/README.md` documents layout +\nhow-to-extend.\n\n## System backends\n\n| | macOS (ImageIO + vImage) | Windows (WIC) | Linux |\n|---|---|---|---|\n| JPEG/PNG/WebP | static codecs (output identical across platforms) |\nstatic codecs | static |\n| GIF/BMP | static `codec_gif.zig`/`codec_bmp.zig` | static | static |\n| **TIFF** | \u2705 decode | \u2705 decode | `UnsupportedOnPlatform` |\n| **HEIC** | \u2705 dec+enc 10.13+ | \u2705 dec+enc (HEIF Image Extensions;\nquality wired via IPropertyBag2) | `UnsupportedOnPlatform` |\n| **AVIF** | \u2705 dec; enc on Apple Silicon only (Intel has no AV1 encoder)\n| \u2705 dec+enc (AV1 Video Extension) | `UnsupportedOnPlatform` |\n\nCoreGraphics dispatch lives entirely in C++\n(`image_coregraphics_shim.cpp`); decode goes through\n`vImageBuffer_InitWithCGImage` for straight-alpha (no premul roundtrip),\nwrapped in an explicit autoreleasepool per WorkPool call. WIC encode\nthreads `ImageQuality`/`HeifCompressionMethod` through a C++ shim so the\nSDK's own VARIANT layout is authoritative. Symbol resolution stays lazy\n(dlsym/CoCreateInstance), zero startup cost.\n\n## Tests \u2014 204 across four suites\n\n`image.test.ts` (~80) \u2014 happy path: per-format round-trip, all 9\nfilters, rotate/flip, fit:inside/withoutEnlargement, EXIF auto-orient,\nmodulate, palette+dither PNG, compressionLevel,\n`.buffer()/.blob()/.toBase64()/.write()`, `new Response(image)`,\n`Bun.file().image()`, GIF/BMP/TIFF, HEIC/AVIF sniff + per-platform\nencode.\n\n`image-adversarial.test.ts` (~58) \u2014 format confusion (lying magic bytes,\npolyglots), truncation sweep at every byte offset, single-byte XOR fuzz\nat every offset, hostile header dimensions (0\u00d70, 2\u00b3\u00b9-1, 2\u00b3\u00b2-1), zlib\nbombs in IDAT, hostile EXIF IFD0, hostile BMP BITFIELDS / biSize wrap,\n**GIFLIB-CVE-2016-3177-style LZW** (code > avail), 255-byte GIF\nsub-block, lossless roundtrip on LCG noise, rotate\u2074=id, leak plateau\nunder repetition, throwing Proxy on every option object, re-entrant\ngetters, detached/resizable ArrayBuffer, SharedArrayBuffer, `data:` URL,\n100 concurrent terminals.\n\n`image-kernels.test.ts` (~38) \u2014 algorithm pins: flat-field DC gain (all\n9 filters), 1\u00d71 upscale, same-size identity, mitchell monotone over a\nstep, **nearest downscale stays discrete**, impulse spread ordering,\nanisotropy, FS dithered ramp tracks source mean, FS determinism, 1\u00d7N/N\u00d71\ndither edge cases.\n\n`image-vs-sharp.test.ts` (29) \u2014 pixel-level cross-reference against\npre-baked Sharp/libvips output. mitchell/bilinear/cubic/lanczos2:\nMAE<0.5 max\u22645 (centre/support math correct). lanczos3/mks*: MAE<1.0\nmax\u226416 (libvips quantises offsets to 64 bins; we compute exact per-pixel\nweights \u2014 both correct). Hard-edge sources omitted by design (those\nmeasure phase/pre-shrink, not kernel).\n\n## Known follow-ups\n\n- x64 libjpeg-turbo NASM SIMD \u2014 `nasm` not in build images; arm64 NEON\nis on.\n- `new Response(image)` encodes on the calling thread; a `.Locked` body\nwould stream from the WorkPool.\n- Integer pre-shrink for non-JPEG would close the remaining gap on huge\nPNG/WebP downscales.\n\n---------\n\nCo-authored-by: autofix-ci[bot] <114827586+autofix-ci[bot]@users.noreply.github.com>"
    },
    "GHSA-JFHR-4V9P-9RM4": {
      "ede635b8a921e95f0d847641a48e4d448baf9a7f": "fix(install): store tarball integrity hash in lockfile for HTTPS dependencies (#27018)\n\n## Summary\n- HTTPS/URL tarball dependencies were not having their integrity hash\nstored in the lockfile, allowing a malicious server to change the\ntarball contents without detection on subsequent installs\n- Now computes a sha512 hash from the downloaded tarball bytes during\nextraction and stores it in both the binary lockfile and text bun.lock\n- The hash is verified on re-download, matching the behavior of npm\nregistry packages\n- Old lockfiles without integrity hashes continue to work (backward\ncompatible)\n\n## Changes\n- `src/install/integrity.zig`: Added `Integrity.forBytes()` to compute\nsha512 from raw bytes\n- `src/install/install.zig`: Added `integrity` field to `ExtractData`\nstruct\n- `src/install/PackageManagerTask.zig`: Compute hash from tarball bytes\nfor both remote and local tarball tasks\n- `src/install/PackageManager/processDependencyList.zig`: Set\n`package.meta.integrity` from computed hash\n- `src/install/lockfile/bun.lock.zig`: Serialize/deserialize integrity\nfor `remote_tarball` and `local_tarball` types\n\n## Test plan\n- [x] Integrity hash is stored in text lockfile for tarball URL deps\n- [x] Integrity hash is consistent/deterministic across reinstalls\n- [x] Integrity mismatch (changed tarball content) causes install\nfailure\n- [x] Old lockfiles without integrity still install successfully\n(backward compat)\n- [x] Fresh installs produce integrity hashes\n- [x] All 12 existing tarball tests pass (no regressions)\n- [x] Tests fail with `USE_SYSTEM_BUN=1` (confirms fix is effective)\n\nFixes GHSA-jfhr-4v9p-9rm4\n\n\ud83e\udd16 Generated with [Claude Code](https://claude.com/claude-code)\n\n---------\n\nCo-authored-by: Claude Bot <claude-bot@bun.sh>\nCo-authored-by: Claude <noreply@anthropic.com>\nCo-authored-by: autofix-ci[bot] <114827586+autofix-ci[bot]@users.noreply.github.com>\nCo-authored-by: Jarred Sumner <jarred@jarredsumner.com>"
    },
    "CVE-2018-7160": {
      "7f57c9c74733f39da5557c6bf73edee4634e87d7": "fix(debugger): use crypto.randomUUID() for WebSocket auth token\n\nReplace Math.random().toString(36) with crypto.randomUUID() for generating\nthe debugger WebSocket path token. Math.random() is not cryptographically\nsecure and produces predictable output, making it possible for local\nattackers to guess the token and connect to the debugger via Chrome DevTools\nProtocol. This is the same class of vulnerability as Node.js CVE-2018-7160.\n\nCo-Authored-By: Claude <noreply@anthropic.com>"
    }
  }
}